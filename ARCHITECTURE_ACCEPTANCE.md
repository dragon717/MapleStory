# 架构与资源验收标准

更新：2026-09-05。本文件记录用户已确认的原则、首版范围和以后评判实现的标准。**当前前后端已实现且构建通过；阶段A真实网络双机器人7项检查通过，代表性浏览器资源验收已完成，阶段B用户验收待执行。** 资源的已有实测证据与游戏实现状态分开。

## 1. 已确认原则与未定技术

| 已确认原则 | 待实现选择，不得冒充已确认 |
| --- | --- |
| Web TypeScript / JavaScript 前端，Rust 后端；一台主机、局域网多设备不同账号 | Phaser / Pixi、Vite、ECS、数据库和具体库版本 |
| Rust 维护权威世界状态及规则，按战斗、背包、任务等模块组织 | 模拟 tick、发送频率、误差阈值、数据库写入频率 |
| 浏览器负责输入、画面、UI、镜头、动画、声音；可先播放本地动作反馈 | 客户端预测实现程度、插值参数、断线角色保留时长 |
| 原始素材是材料及拼装说明，离线导出后仍需 Web 代码实现纸娃娃、地图、动画与交互 | 具体地图、角色外观、机器人路线与节奏 |
| 先跑通 MVP，后做性能优化；按需加载地图依赖和动态实体资源 | 图集格式、分包大小、容量指标与最终内容版本 |

最终交互、位置移动、属性、命中、伤害、掉落归属、任务和存档由 Rust 判定。后端应有清晰业务模块，不是一个接收所有消息并包办所有逻辑的巨大事件函数。

## 2. 已确认的最初 MVP

**范围：两个及多个不同账号、一张地图、局域网在线联机、角色移动和普攻。** 提供真实登录界面与完整进入地图流程。首版不把技能、任务、掉落、PVP 作为通过门槛；这些列入后续阶段。

机器人是程序化客户端，发送与玩家相同的正常登录、输入和普攻请求，通过相同 Rust 规则处理。禁止直接修改后端世界状态来伪造联机成功；不依赖 AI 实时操控机器人。具体地图、路线和节奏留作实现配置，不能在文档中冒充用户已选。

### 阶段 A：开发自测

| 编号 | 验收场景 | 通过条件 | 必须保存的证据 | 当前状态 |
| --- | --- | --- | --- | --- |
| M1 | 两个自动化机器人使用不同账号登录 | 两个账号各自认证，角色身份互不覆盖，经过真实客户端网络链路进入同一地图 | 两端账号/角色 ID、会话日志、服务端入图记录；凭据脱敏 | 网络自测通过，见 evidence/bot-selftest.json |
| M2 | 两机器人按预设程序移动和普攻 | 双方看到对方的位置、朝向及普攻动作；服务端接收正常请求并按共同规则推进 | 两端画面或录屏、关联输入序号/动作实例与服务端时序日志 | 网络状态与动作检查通过；浏览器样例表现已验证 |
| M3 | 一方发出无权限角色操作或伪造伤害字段 | 不能控制另一账号角色，不能让任意客户端数值写入权威状态；非法消息被明确拒绝或忽略并记录 | 请求样例、拒绝响应或日志、操作前后权威状态对照 | 网络自测通过，见 evidence/bot-selftest.json |
| M4 | 一个连接断开并重新登录 / 重连 | 断线后不会无限保持移动输入；重入取得服务端权威状态，身份保持独立 | 断线/重连日志与两端状态记录 | 网络自测通过，见 evidence/bot-selftest.json |
| M5 | 检查账号数量设计 | 服务端没有仅为两个测试账号硬编码的登录槽位或角色分支，可配置添加其它独立账号 | 账号创建/配置和会话代码审查；额外账号连接记录（实施时安排） | 无双账号特例，demo与新注册qa账号也已真实连接 |

移动同步与普攻表现是首版核心。当前普攻仅权威动作接受与广播，不增加受击对象；以后实现命中或扣血，必须由服务端判定。此处不额外要求怪物掉落、技能、任务或 PVP。

### 阶段 B：交付用户验收

| 编号 | 验收场景 | 通过条件 | 必须保存的证据 | 当前状态 |
| --- | --- | --- | --- | --- |
| U1 | 用户在局域网设备打开页面 | 看到登录界面，凭自己的账号独立完成登录和进入地图；不需要代理代操作或手动注入状态 | 可访问的主机地址、简短启动/登录步骤、用户实际进入记录 | 入口/机器人已就绪，待用户本人验收 |
| U2 | 用户进入同一地图 | 能看到一个独立机器人账号，机器人自动执行预设程序化移动和普攻 | 用户端画面/录屏、机器人自动启动与动作日志 | 入口/机器人已就绪，待用户本人验收 |
| U3 | 用户自行移动与普攻 | 自己操作有正常反馈，机器人作为另一客户端能收到相应状态/动作；身份和控制互不混淆 | 用户验收记录与两端对应日志 | 入口/机器人已就绪，待用户本人验收 |
| U4 | 无 AI 实时介入 | 启动机器人程序后即持续按预设运行，不靠模型逐次发动作；停止/重启方法明确 | 机器人程序入口、配置、无人干预运行记录 | 程序机器人已运行；用户验收待执行 |

阶段 A 与 B 分别保存记录。开发者双机器人通过不能替代用户从登录界面独立进入的验收；“本机两个状态对象在动”也不能代替实际联机链路证据。

## 3. 资源完整性分四关

| 关卡 | 验收场景 | 通过条件 | 必须保存的证据 | 当前状态 |
| --- | --- | --- | --- | --- |
| R1 下载完整 | 对原始包和解压结果核验 | 下载字节符合预期，压缩包 CRC 通过，所有源文件有大小/hash；不是 LFS 指针或下载页面 | 原 URL/版本、zip 清单、CRC、每文件 SHA256 | 17/17成员完整性通过；见资源事实报告 |
| R2 引用完整 | 扫描选定内容及依赖 | 地图、角色、动作、音效跨文件引用可解析；UOL/动作重定向不丢失；缺失/循环明确报错 | 依赖清单、解析失败列表、导出规则版本 | 尚未证明全量引用闭合 |
| R3 解码可用 | 图片与音频导出/解码 | PNG 能解码并保持尺寸/透明度；音频真实可解码；逐帧数据/原点/锚点/层级被保留 | 解码结果、失败列表、样例及覆盖范围，区分抽样和全量 | 音频3980/3980完整解码通过；图片代表性抽检通过，非全量Canvas |
| R4 运行呈现正确 | 在目标浏览器显示/播放 | 地图与纸娃娃位置层级正确、动画节奏匹配、声音可播放、加载无遗漏 | 浏览器截图/录屏、网络记录、元数据对照和人工检查 | 代表性外观/单地图开发验证通过，限制见 evidence/MVP_ACCEPTANCE.md |

前一关通过不能冒充后一关通过。文件数量、目录名、作者 README 或一次截图都不能证明全量完整。完整底包的取得与一张 MVP 地图的呈现是两项独立验收，不互相替代。

## 4. 实现必须满足的资源与时序标准

| 编号 | 验收场景 | 通过条件 | 证据要求 | 当前状态 |
| --- | --- | --- | --- | --- |
| A1 | 角色站立、移动、普攻，改变朝向 | 头、身体与装备的锚点相连；层级、帽子/头发遮挡正确，不因帧变更跳位 | 原始与导出 origin/map/z 对照、相同动作逐帧截图 | 代表性外观/单地图开发验证通过，限制见 evidence/MVP_ACCEPTANCE.md |
| A2 | 播放完整普攻动作 | 使用逐帧 delay 和动作重定向，不用固定 FPS 覆盖原始节奏；特殊 delay 解释明确 | 元数据与实播时间线对照，误差口径实施时记录 | 原delay检查与浏览器动作通过；未宣称完整原版逐帧一致 |
| A3 | 播放地图 BGM 和普攻音效 | 文件可解码，用户进入后浏览器可正常播放，切图/退出不残留重复声音 | 解码日志、浏览器播放记录、声音生命周期检查 | 解码/加载与播放路径通过；主观听音待用户 |
| A4 | 动作带音效或特效 | 依据明确的动作事件/时点触发；服务端命中不从美术播放时长自动推算 | 事件与时点定义、前后端日志/录屏对照 | 未实施 / 未验收 |
| A5 | 初次进入与动态实体出现 | 只加载本地图和必要角色等依赖；之后按需取新增实体资源，不强制玩家下载完整底包 | 浏览器 Network 记录、manifest 依赖列表、后续请求对照 | 当前manifest仅125活动PNG与2音频；新外观按需加载尚未实施 |
| A6 | 人为缺少一个依赖资源 | 明确报告缺少的资源 ID/路径及版本，不静默空白或无限 loading | 受控失败样例、错误提示和日志 | 未实施 / 未验收 |
| A7 | 读取地图布局与碰撞 | 地面与原始 foothold 等布局相符，角色视觉与落脚点一致 | 导出结构、碰撞调试叠层与角色截图 | 代表性外观/单地图开发验证通过，限制见 evidence/MVP_ACCEPTANCE.md |

单地图仍应保留以后动态实体按需加载的正常路径，但不提前实现其它地图、全职业、热更新或资源编辑器。

## 5. 模块边界和后续业务标准

| 编号 | 验收场景 | 通过条件 | 证据要求 | 适用阶段 / 当前状态 |
| --- | --- | --- | --- | --- |
| B1 | 阅读一次输入到结算调用链 | network 负责接收与校验，world 推进状态，combat/quests/inventory 各自处理业务；前端只发意图与呈现 | 代码路径图、请求到状态/事件的示例 | 首版 / 已拆network→world→combat，网络自测通过 |
| B2 | 新增复用已有行为的技能 | 主要新增配置与资源引用；新机制才增加通用处理器，不修改所有旧技能 | 具体 diff 和修改位置说明 | 后续 / 非首版门槛 |
| B3 | 重复提交同一任务或拾取 | 任务状态/消耗/奖励事务一致，重复请求不重复发奖 | 重放请求、数据库事务和前后状态记录 | 后续 / 非首版门槛 |
| B4 | 持久化失败或主机重启 | 已确认存档策略内的数据一致，失败可见；不宣称保存成功但丢失结果 | 故障注入与恢复记录 | 对应业务实现时 / 未实施 |
| B5 | 短时动作结束、死亡、切图、断线 | 其临时效果、计时器、订阅、音效句柄按生命周期清理 | 实例数/订阅数检查、长时间重复动作记录 | 实现相应场景时 / 未实施 |

## 6. 验收记录模板

每次记录：日期、代码提交、内容包版本/hash、浏览器/设备、执行人或自动程序、验收编号、步骤、预期、实际、日志/截图路径、结果（通过/失败/未执行）和已知限制。

不得填写尚未实施的“通过”。技术方案可迭代，但修改已确认的权威边界、首版范围和验收阶段必须明确记录用户决定；新增库或内部文件重排不自动改变验收标准。

文档导航：[计划](PLAN.md) · [共同契约](SHARED_ARCHITECTURE.md) · [前端](FRONTEND_ARCHITECTURE.md) · [后端](BACKEND_ARCHITECTURE.md) · [参考项目分级](REFERENCE_PROJECTS.md)

## 本轮实测位置

- `evidence/bot-selftest.json`：真实HTTP注册登录/WS输入与事件，7项通过，未存凭据。
- `references/evidence/gms83-audio-decode.json`：3980个唯一输出完整解码，0失败。
- `references/evidence/gms83-verification.md`：包/解析/引用/解码的覆盖边界。
- `BUSINESS_DEVELOPMENT.md`：模型阶段政策和后续业务修改入口。

局域网地址已绑定0.0.0.0:3000；另一台物理设备的可达性、用户操作与最终视觉验收没有由本机测试代替。
