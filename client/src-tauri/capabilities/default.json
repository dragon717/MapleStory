{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "最小权限：只允许主窗口使用 Tauri 核心默认能力。不授予文件系统、shell、HTTP 客户端或任意原生命令——外壳唯一的原生职责是「创建窗口 + 注入来源」。",
  "windows": ["main"],
  "permissions": ["core:default"]
}
