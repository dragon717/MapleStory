# 冒险岛复刻 TMS273：Web 大前端、实时渲染与热更新改造计划

> 编写日期：2026-09-11  
> 目标技术栈：TypeScript / JavaScript / Three.js；对接现有 Rust 权威后端。  
> 统一启动入口：项目根目录 `启动3010.command`。  
> 文档状态：联网调研后的实施计划，**不是已经完成的代码改造或测试报告**。

## 0. 决策摘要

**不需要把“静态包”改成某种“动态网页”，而是需要把源码开发、浏览器运行、服务器同步、构建发布四条链路分清楚。**

本轮已经明确的问题是：正在测试构建产物 `dist-tms273`，修改源码不会自动改变这份产物。因此，当前浏览器表现不能直接用来判断刚改的源码是否正确。

推荐落地形态：

```text
开发：TS / JS 源码 → Vite 开发服务器 → 浏览器自动更新
运行：服务器世界状态 → 客户端状态 → Three.js 连续绘制画面
发布：同一套源码 → 校验与构建 → dist-tms273 → 静态资源服务
```

Vite 官方明确区分开发服务器和生产构建；生产构建输出静态资源，并不意味着这些资源运行后只能展示静态画面。Three.js 的渲染循环与是否使用开发服务器是不同层次的问题。[S01][S02][S07]

本项目建议采用以下决策：

| 决策 | 本次选择 | 不采用的方向 |
| --- | --- | --- |
| 日常开发入口 | 源码入口 + Vite 开发服务器 | 始终访问旧 `dist-tms273` |
| 自动更新策略 | 先自动整页刷新，再对高频视觉模块做选择性 HMR | 一开始承诺所有代码无损热替换 |
| 前端组织 | TS 优先、兼容既有 JS、模块化客户端 | 为改构建链一次性重写全部业务 |
| 渲染技术 | Three.js 负责适合它的渲染内容，保留现有可用部分 | 把“热更新改造”等同于“全场景 3D 重做” |
| 网络与状态 | 独立连接层、权威状态镜像、可重建表现层 | 每次改材质都重新登录 |
| 生产部署 | 继续构建静态资源，版本化发布 | 把 Vite 开发服务器暴露给正式玩家 |
| 启动方式 | 只扩展 `启动3010.command` | 增加平行且互相冲突的启动脚本 |

**第一阶段的最小成功标准：通过统一脚本打开开发页面，修改一处真实 UI 源码，浏览器自动出现变化，不执行前端全量构建，不重启 Rust。**

### 0.1 已知与待核实边界

已知输入：当前测试 `dist-tms273`；希望采用 TS / JS / Three.js；保留 Rust 权威世界与统一启动脚本约束。

本次没有取得可检查的项目源码：当前容器没有挂载项目，连接的 GitHub 仓库检索未找到匹配项，`dist-tms273` 代码检索返回服务错误。因此，下文的 `src/`、配置文件位置、`/api`、`/ws`、3011 等是**建议或示例**，不是对现有仓库实现的断言。

实施时先执行 P0，确定真实目录、路由、端口和资源链路；沿用现有正确实现，不能因为文档用了示例名称就新建第二套客户端。

---

## 1. 先统一“实时”的含义

| 能力 | 玩家或开发者感知 | 主要实现位置 |
| --- | --- | --- |
| 开发自动刷新 | 保存代码后页面更新，但可能重新进入场景 | 构建工具、开发服务器 |
| 热模块替换 HMR | 修改局部模块，尽量不刷新页面 | 构建工具 + 客户端模块生命周期 |
| 运行时实时渲染 | 人物、技能、水面、光效持续运动 | Three.js 与客户端帧循环 |
| 多人世界实时同步 | 其他玩家、怪物、世界事件更新 | Rust 协议与客户端网络层 |
| 数据或资源热更新 | 修改视觉配置、纹理后当前场景更新 | 配置校验、资源缓存、显式应用逻辑 |
| 正式版本更新 | 已部署代码或内容变更可安全交付玩家 | 版本兼容、发布与回滚机制 |

这些能力不能相互代替。加一个 WebSocket 不会让源码自动生效；加一个 Three.js 渲染循环也不会让浏览器重新读取构建前的 TS 文件。

HMR 还不等于“旧对象自动拥有新类的方法”。Vite 的 HMR 边界需要明确接受更新；已有实例、旧闭包、资源引用与副作用如何替换，仍然由应用管理。[S04]

## 2. 常见方法比较与选择理由

| 方法 | 适合的场景 | 对本项目的判断 |
| --- | --- | --- |
| 手动构建，再刷新页面 | 发布验收、低频维护 | 保留，但不再是日常开发主路径 |
| `build --watch` + 自动刷新 | 暂时无法拆分的旧构建链 | 仅作过渡；仍然要生成构建产物 |
| 开发服务器 + 整页自动刷新 | 从静态产物测试迁移到源码测试 | 最先落地，复杂度最低 |
| Vite + 明确的 HMR 边界 | UI、视觉参数、局部场景快速迭代 | 推荐作为稳定目标 |
| 后端 HTML + 注入 Vite 模块 | 已有后端模板或强绑定页面入口 | 有真实约束时采用 |
| 编辑器实时改参数 | 水面、技能、相机、地图装饰调试 | 后续增强，不代替代码开发链 |

Vite 支持监听构建，但 `build --watch` 的结果是重新构建，不是自动获得应用级状态保留；其后端集成文档也给出了开发时加载 Vite 模块、生产时使用构建 manifest 的分离方式。[S02][S05]

**选择理由：当前最高权重变量是“浏览器到底在运行哪份代码”，不是先选更多框架。先打通源码到画面的反馈链，再逐步降低热更新对游戏会话的干扰。**

---

## 3. P0：查清当前真实启动与加载链路

### 3.1 必须形成的现状清单

| 核查项 | 要记录的证据 | 可能影响的设计 |
| --- | --- | --- |
| 浏览器入口 | 完整入口路径、响应 HTML、主要脚本 URL | 是否误入旧静态包 |
| `启动3010.command` | 调用链、工作目录、启动与重启顺序 | 如何增加 dev / build / preview 模式 |
| 前端源码根目录 | 实际 `package.json`、HTML、TS/JS 入口 | Vite root 与入口配置 |
| 构建工具与版本 | 锁文件、构建脚本、Vite/其他工具版本 | 复用现有工具，不盲目升级 |
| 3010 监听者 | 进程身份、命令、工作目录 | 谁做开发入口与反向代理 |
| Rust 前端服务方式 | 读磁盘、嵌入资源、模板或代理 | 是否需要先解除编译期资源嵌入 |
| HTTP / WebSocket | 真实路径、认证方式、Origin 限制 | 同源代理与重连策略 |
| 渲染器 | 当前 Canvas/WebGL/Three.js 代码与循环入口 | 是否存在重复渲染循环 |
| 资源管线 | WZ/图片/JSON 的转换、输出与加载方式 | 资源更新是否还依赖导出步骤 |
| 缓存 | HTTP、Service Worker、Cache Storage、客户端缓存 | 是否读到陈旧资源 |

可使用以下只读搜索辅助定位；它不是新的启动方式：

```bash
# 在已确认的项目根目录执行；结果仍需逐项阅读。
rg -n \
  --glob '!**/node_modules/**' \
  --glob '!**/target/**' \
  --glob '!**/dist-tms273/**' \
  --glob '!**/.git/**' \
  'dist-tms273|@vite/client|WebGLRenderer|setAnimationLoop|requestAnimationFrame|new WebSocket|include_bytes!|include_str!' .
```

### 3.2 诊断必须覆盖的具体情况

如果页面加载的是构建后的哈希 JS，继续追查它属于哪个构建版本，不能只凭文件名判断新旧。如果 Rust 使用 `include_bytes!` 或 `include_str!` 嵌入资源，磁盘上的文件变化也可能不会改变运行中的二进制，开发模式应改为源码服务或显式外部资源读取。

如果项目有多套入口，例如普通 Web、TMS273、实验性 3D 客户端，必须明确本轮测试对应哪一套。不得改了实验入口，却继续用 TMS273 的旧产物验收。

如果已经安装并配置 Vite，优先修正统一脚本启动目标、HTML 入口和代理；**不要再次创建空项目再搬业务**。

### 3.3 P0 交付物

产出 `docs/dev/current-entry-audit.md`，包含启动链、源码入口、构建输出、资源链路、实际端口和浏览器证据。只有完成这张映射表，才能进入改造。

---

## 4. 推荐开发拓扑：一个访问入口，两个内部职责

### 4.1 默认建议

保留浏览器访问端口 3010。开发模式由 Vite 接收前端请求，Rust 在仅供本地开发使用的内部端口运行。

```text
浏览器：127.0.0.1:3010
                 │
                 ▼
         Vite 源码开发服务器
           ├── HTML / TS / JS / CSS → 当前源码
           ├── 开发 HMR 连接        → Vite
           ├── /api/...             → Rust 127.0.0.1:3011
           ├── /ws                  → Rust 127.0.0.1:3011
           └── 游戏资源路径          → 按 P0 结果复用现有资源服务
```

3011 是建议值，实施前检查占用情况，并将 Rust 监听地址配置化。前端不得因此到处硬编码后端端口。`/api` 和 `/ws` 同样需要映射到仓库真实路由。

Vite 提供 HTTP / WebSocket 代理和 `strictPort`。开发模式使用固定端口并启用 `strictPort`，防止端口被占用时悄悄跑到另一个端口、浏览器却仍在访问旧进程。[S03]

### 4.2 只有确有约束时使用的替代方案

若 3010 必须由 Rust 绑定，且改变绑定者会影响现有启动或鉴权，可保留 Rust 3010，将前端开发请求代理至内部 Vite。必须完整转发 HMR WebSocket，处理 HTML、模块、资源路径与 Origin，不能只转发首页。

若现有后端必须生成 HTML，可按 Vite 后端集成方案，在开发页面引入开发客户端与源码入口；生产页面仍使用构建 manifest。[S05]

以上是条件分支，不要求同时建设两套。没有硬约束时，采用 4.1，避免为了开发热更新编写额外的 Rust 代理框架。

### 4.3 构建预览与正式环境

```text
浏览器
   │
   ▼
现有静态资源服务 / Rust / 部署网关
   ├── HTML、哈希 JS/CSS → 指定版本的 dist-tms273
   ├── 版本化游戏资源
   └── HTTP / WebSocket → Rust 权威世界
```

正式环境不运行 Vite 开发服务器。`vite preview` 是本地预览工具，不是官方建议的生产服务器。本项目的端到端构建验收优先复用实际静态服务链路，而不把开发代理成功当作生产路由成功。[S06]

---

## 5. 改造统一启动脚本，而不是引入平行启动入口

### 5.1 拟新增的模式契约

**下列参数是需要实现的目标接口，不表示现有脚本已经支持。**

| 调用 | 目标行为 |
| --- | --- |
| `./启动3010.command` | 默认进入 dev，启动源码前端与所需后端 |
| `./启动3010.command dev` | 同上，显式声明开发模式 |
| `./启动3010.command build` | 执行校验并生成新版本产物；不自动替换正在运行的版本 |
| `./启动3010.command preview` | 运行明确选定的已构建版本；不隐式重新构建，不提供 HMR |
| `./启动3010.command check` | 统一执行检查或调度验收，具体子选项在实施时确定 |
| `./启动3010.command status` | 显示项目、模式、端口、进程与版本 |
| `./启动3010.command stop` | 只停止由本项目脚本管理的进程 |

根 `.command` 可调用内部编排模块，但这些模块不是面向开发者的新启动入口。CI 和自动化测试也通过同一个脚本启动集成环境。

### 5.2 dev 的行为

进入已定位的项目根目录，校验工具版本和配置，按锁文件使用项目依赖。按需编译并启动 Rust，启动 Vite 和独立的 TS 类型检查任务。前端普通源码变化不得触发 Cargo 编译、前端生产打包或整个服务重启。

Rust 源码变化的自动重启不是 P1 前置条件。后续加入监听时，采用“新代码先编译，成功后再切换”的流程，避免监听器先停掉旧服务再发现编译失败。

### 5.3 进程与失败处理约束

保留既有原则：**编译失败不停止当前正常服务。** 但这不等于承诺任意启动失败都能无缝恢复，更不等于内存世界可在重启后自动恢复。

编排器必须记录启动模式、PID、进程身份、项目路径与实例标识；捕获退出信号，清理它自己启动的子进程。不得通过全局 `pkill node`、`pkill cargo` 或按端口盲杀未知进程处理冲突。

当 3010 已被本项目另一种模式占用时，先完成可提前执行的检查与编译，再做受控模式切换。单一固定端口的切换可以有短暂中断，不应描述为零停机。未知占用者必须报错，不自动更换端口。

服务就绪检查不能只是“端口打开”。至少验证前端模式身份、后端健康状态和真实游戏 WebSocket 握手。失败时报告哪一层失败，不自动回退到旧静态包并宣称开发模式成功。

### 5.4 必须显示“当前运行的是什么”

建议开发 HUD 和脚本日志显示：

```text
MODE: DEV_SOURCE 或 BUILT_PREVIEW
frontend instance / build ID
启动时 source revision + dirty 状态
最近一次成功热更新序号与时间
后端实例 ID / server epoch
协议版本 / 地图规则版本 / 资源 manifest 版本
HMR 连接状态 / 游戏连接状态
```

Git commit 不能单独证明未提交源码已经生效。开发模式还应记录真正完成应用的热更新序号；构建模式使用生成时写入产物的 build ID，不用请求发生时的当前 Git HEAD 冒充产物版本。

---

## 6. Vite 与 TypeScript 配置基线

### 6.1 先兼容现有仓库，再固定工具链

核实 Node、包管理器、Vite、TypeScript、Three.js 和相关插件的实际版本，提交锁文件与工具版本约束。新 TS 代码采用严格检查；既有 JS 可以分批迁移，不要求把全仓库改完才能开始热更新。

Vite 会转译 TS，但不负责完整类型检查。因此开发时应有独立检查任务，构建与 CI 必须将 `tsc --noEmit` 或项目既有等价检查作为门禁。[S08]

不要复制与锁定版本不匹配的“最新版配置”。截至本次查阅，当前 Vite 文档已将部分 HMR WebSocket 配置迁移至 `server.ws`，旧项目可能仍使用 `server.hmr` 的对应字段。简单本地拓扑优先使用默认 HMR 连接，减少跨版本配置面。[S03]

### 6.2 Vite 配置示意

下列内容是适合合入**实际前端根目录现有配置**的基线。代理路径、资源路由和输出目录必须经 P0 核实；环境变量由统一脚本传入。未包含现有插件的配置，不能拿它直接覆盖完整配置文件。

```ts
import { defineConfig } from 'vite';

export default defineConfig({
  // 示例以根路径部署；已有子路径部署须沿用真实 base。
  base: '/',

  server: {
    host: '127.0.0.1',
    port: 3010,
    strictPort: true,
    hmr: { overlay: true },

    // 示例路径；保留真实后端路径，不盲目 rewrite。
    proxy: {
      '^/api(?:/|$)': {
        target: 'http://127.0.0.1:3011',
      },
      '^/ws(?:/|\\?|$)': {
        target: 'http://127.0.0.1:3011',
        ws: true,
      },
    },

    watch: {
      ignored: [
        '**/dist-tms273/**',
        '**/.artifacts/**',
        '**/target/**',
        '**/.runtime/**',
      ],
    },
  },

  build: {
    // 必须由统一脚本传入经过校验的全新 staging 目录。
    // 仅作独立配置示例时才退回这个相对目录。
    outDir: process.env.TMS273_OUT_DIR ?? 'dist-tms273',
    emptyOutDir: false,
    manifest: true,
    sourcemap: false,
  },
});
```

这里故意不设置 `cors: true`、`allowedHosts: true` 或 `rewriteWsOrigin: true`。代理不代替游戏服务器的 Origin 校验、认证和权限验证；开发源地址需要显式加入本地允许范围，不应因此关闭生产安全校验。[S03]

staging 路径必须由脚本校验：非空、位于约定产物目录、不是源码根目录、不是当前在服版本，且本次构建前为空。不能在旧目录上不断叠加产物，留下已经不存在的 chunk。

### 6.3 HTML 与 JS/TS 入口

开发 HTML 指向真实源码模块，例如：

```html
<!-- 示例；main.ts 必须替换为 P0 确认的 TMS273 源码入口。 -->
<script type="module" src="/src/main.ts"></script>
```

如果 HTML 由 Vite 提供，使用其标准转换流程。若由后端生成 HTML，按官方后端集成方式处理开发客户端注入，不自行拼接不完整的 HMR 初始化。[S05]

新代码默认用 TS，浏览器运行编译后的 JS；不要维护内容相同的 `.ts` 和手工 `.js` 双份业务实现。前端依赖里仅保留一套实际使用的 Three.js 版本，并检查对应类型定义与插件是否兼容。

### 6.4 文件监听与大资源目录

优先采用正常文件系统事件。只有确认环境存在漏事件且无法直接解决时，才针对性启用轮询；Vite 文档提示轮询会增加 CPU 使用。[S03]

若项目处于云同步目录，先验证文件是否完整落地以及监听是否可靠。不要把所有问题都归咎于缓存，也不要默认扫描完整 WZ 资源树、`node_modules` 或 Rust 构建目录。大资源更新应以转换产物和 manifest 为观察对象。

---

## 7. Web 大前端结构：状态与表现可独立演化

### 7.1 职责划分

```text
浏览器输入
   │
   ▼
命令层 ───────────────→ Rust 权威世界
   │                         │
   │ 可选本地预测             │ 快照 / 增量 / 事件
   ▼                         ▼
客户端状态 / 网络时间 / 协议校验
                  │
                  ▼
            表现适配层
             ├── Three.js 场景 / 动画 / 粒子 / 水面
             ├── DOM / CSS 界面
             └── 音频与局部视觉反馈
```

前端可以承担复杂交互、预测、动画、编辑器与可视化，但不能因此把背包、任务完成、奖励发放、碰撞规则和共享世界状态的最终裁决迁到客户端。

### 7.2 建议逻辑模块

以下是职责边界，不是强制搬目录清单：

```text
现有前端根目录/
  src/
    main.ts                  # 组合与启动，不堆业务
    runtime/                 # 稳定宿主、时钟、生命周期
    network/                 # 连接、协议、重连、消息校验
    state/                   # 权威镜像与独立 UI 状态
    render/                  # Three.js 适配与实体表现
    ui/                      # 背包、任务、聊天、操作界面
    assets/                  # URL 映射、加载、缓存、引用所有权
    dev/                     # HMR、诊断、开发参数工具
  tests/
```

不为“大前端”预先引入微前端、复杂 monorepo、额外 ECS、消息总线或全新状态管理框架。已存在的正确架构优先复用。若 HUD 已经使用 React/Vue，保留；若没有，不把引入框架作为 HMR 的前提。

### 7.3 Three.js 使用边界

对于保持冒险岛横版玩法的内容，建议从正交相机、分层场景、精灵或平面网格、局部 3D 特效开始。Three.js 不要求玩法立刻转成完全三维。

地图层次、屏幕坐标与世界坐标转换、精灵锚点、缩放、透明排序、像素采样策略需要写成测试约定。先用一张真实地图对齐角色、平台、NPC 和点击位置，再扩展水体与场景装饰。

若当前渲染器已经能稳定运行，P1 只改开发链路。后续按场景或效果逐步接入 Three.js，避免同时更换构建链、渲染器、碰撞逻辑和网络协议。

DOM 界面按状态变化更新；角色运动与特效在渲染循环更新。不要每帧重建整个背包或任务 DOM，也不要把 Three.js 的大量可变对象直接塞进界面框架的深层响应式状态。

---

## 8. HMR 的核心：选择性保留会话，明确重建表现

### 8.1 更新分级

| 修改类型 | 建议处理 | 状态处理 |
| --- | --- | --- |
| CSS、已有框架支持的局部 UI | 使用工具链原生 HMR | 尽量保留界面状态 |
| 相机、颜色、粒子参数、水面视觉参数 | 显式应用新参数 | 保留角色与网络连接 |
| 单个场景表现模块、材质构造逻辑 | 重建对应表现范围 | 保留权威镜像、恢复相机/UI |
| 纹理或模型内容 | 重新加载版本化资源，完成后替换 | 旧资源在替换成功后释放 |
| 表现层数据结构变化 | 校验迁移；不兼容时重建 | 不强行复用旧实例 |
| 稳定宿主、协议解码器、网络核心 | 第一版允许整页刷新 | 重新认证并取得服务器快照 |
| 权威地图规则、NPC 任务逻辑、奖励配置 | 后端版本化加载或重启 | 以服务器确认的版本为准 |
| Rust 源码 | 成功编译后受控重启 | 不称为前端 HMR |

这是本项目建议的支持边界，不是 Vite 自动提供的游戏状态保留保证。[S04]

### 8.2 稳定宿主与可替换表现层

稳定宿主拥有 Renderer、唯一帧循环、游戏连接、输入层和客户端状态。局部表现层拥有当前地图的场景对象、动画、局部事件订阅与独占资源。

建议契约：

```ts
export interface PresentationSnapshot {
  schemaVersion: 1;
  camera: { x: number; y: number; zoom: number };
  ui: { activePanel: string | null };
}

export interface Presentation {
  // 只驱动表现，不在这里提交奖励或推进服务器任务。
  update(deltaSeconds: number): void;
  resize(width: number, height: number): void;
  capture(): PresentationSnapshot;
  restore(snapshot: PresentationSnapshot): void;
  dispose(): void;
}
```

状态快照只保存可安全恢复的本地表现信息。角色位置、背包、任务与世界节点状态继续从服务器镜像取得；不能把 HMR 快照写回服务器作为真值。

`dispose()` 必须可重复调用且只释放归本实例所有的资源。不能把整个 Runtime 丢进 `window` 或 `import.meta.hot.data` 后永久复用，结果新代码一直调用旧类实例。

### 8.3 热替换事务

建议采用：

```text
更新模块到达
   → 校验接口与 snapshot 版本
   → PREPARE：离屏创建候选表现并加载所需资源
   → 校验候选仍是最新 generation
   → COMMIT：在一帧边界切换当前表现引用
   → RETIRE：释放旧表现
```

候选准备期间不注册全局输入、不发送游戏命令、不抢占主循环。准备失败时清理候选并保留旧画面；提交必须尽量小且可预测。如果无法保证局部恢复，明确降级为整页刷新，不吞错后继续运行半新半旧的对象图。

连续保存时，使用 generation 或取消标记丢弃过期异步结果。不能出现“第二次修改已经生效，第一次慢请求完成后又覆盖回来”。地图已经切换或表现已销毁时，旧纹理加载回调也必须丢弃。

### 8.4 Vite 接入原则

在稳定模块中显式接受直接依赖，例如 `import.meta.hot.accept('./render/create-presentation', callback)`，回调使用传入的新模块导出交给表现管理器。模块路径与真实入口一致，并保留 Vite 静态分析能识别的写法。[S04]

不要在入口写一个空 `import.meta.hot.accept()`，只为了让页面“不刷新”。这可能拦住更新传播，却没有把新实现应用到现有场景。

使用 `import.meta.hot.dispose()` 清理模块自身副作用；使用 `import.meta.hot.data` 时只保存有版本且可验证的必要数据。无法局部更新时可传播失效，允许安全刷新。开发代码放在 `import.meta.hot` / `import.meta.env.DEV` 条件内，构建后检查其未成为生产入口。[S04][S11]

### 8.5 必须清理的对象

| 资源 | 所有者与处理要求 |
| --- | --- |
| 帧循环 | 由稳定宿主唯一管理；销毁宿主时停止 |
| Canvas / Renderer | 不在普通场景 HMR 时重复创建；最终退出时销毁 |
| Geometry / Material / Texture | 按所有权显式 `dispose()` |
| RenderTarget / 后处理资源 | 由对应效果或表现层释放 |
| DOM 与全局事件 | 保存解绑句柄，销毁时解除 |
| 网络订阅 | 表现层解除订阅，稳定连接不重复建立 |
| Timer / Observer / Worker | 清理或终止；禁止遗留回调 |
| 异步资源加载 | 可取消则取消，否则校验 generation 后忽略 |
| 共享纹理图集 | 使用引用所有权，不能由一个场景误销毁 |

Three.js 官方明确说明 GPU 资源需要显式清理；只从场景中移除对象不构成完整资源释放。[S09]

---

## 9. 地图、资源与配置也要“改得动”

### 9.1 先把表现数据与权威规则分开

```text
地图表现：背景、装饰、层级、颜色、光效、视觉水波
地图规则：平台、碰撞、传送点、NPC 交互范围、怪物出生与任务条件
```

表现数据可做本地即时预览。规则数据变更必须由 Rust 加载并确认版本，随后让前端更新对应状态；不能让客户端平台已经移动，服务器仍按原位置判碰撞。

例如：改变水面颜色可直接热应用；改变浮力对角色移动的影响，属于规则修改。改变 NPC 名牌样式可热应用；新增任务 NPC 或更改任务完成条件，不是只刷新前端 JSON 就完成。

### 9.2 区分资源是否进入模块依赖图

Vite 对导入的资源提供 URL 处理与生产哈希；`public` 目录的文件通常按原名复制。通过任意 URL `fetch()` 的 JSON、运行时拼接的图片 URL，不等于已经进入模块依赖图。[S10]

| 数据来源 | 建议方案 |
| --- | --- |
| 通过模块导入的小型视觉配置 | 接受模块更新后显式调用应用函数 |
| `.glsl?raw` 等导入的 shader 文本 | 重建或更新材质并验证编译结果 |
| 大型地图 JSON / WZ 转换产物 | 资源 manifest + 当前地图定向重载 |
| 后端提供的世界配置 | 后端版本确认后通知前端 |
| 图片、纹理、图集 | URL 版本变更 + 显式替换 GPU 纹理 |

纹理文件改了，即使浏览器已经重新下载，现有 Material 也可能仍持有旧 Texture。资源管理器需要真正安装新资源，而不仅仅是清空 HTTP 缓存。

### 9.3 开发资源更新链路

建议只对确有需要的大资源管线增加开发插件：

```text
资源编辑 / 增量导出
   → 更新 manifest（最后原子写入）
   → 开发服务器检测变化
   → 发送包含 asset ID、版本、影响地图的开发事件
   → 仅受影响且当前加载的内容重新获取
   → 校验完成后替换，旧资源随后释放
```

Vite 插件 API 支持开发服务器与浏览器之间的自定义事件。具体接入应匹配仓库锁定版本，不必在 P1 就自研通用资源热更系统。[S12]

不要为了一个图标修改重新导出整套 WZ。也不要把全部大型原始资源导入 JS bundle，或在每次前端构建时复制整套素材。保留现有合法可用的资源加载链，优先建立稳定逻辑 ID 到版本 URL 的映射。

### 9.4 按需加载而非全量启动

登录所需 UI 与基础资源先加载；地图进入后加载该地图和当前实体所需资源；相邻地图按预算预取。缓存必须有容量或数量边界，切图时释放不再引用的资源。

这是性能改进方向，不得阻塞 P1 的源码开发入口修复。先记录实际包大小、资源请求数与首个可交互帧，再决定拆包与图集策略。

---

## 10. 实时渲染与后台生命周期

### 10.1 一套渲染循环，不用网络包直接驱动画面帧率

Three.js 当前文档建议使用 `renderer.setAnimationLoop()` 管理动画循环。[S07]

本项目建议：网络层接收快照、增量与事件；状态层维护权威镜像；表现层按浏览器帧节奏插值并渲染。是否使用本地预测取决于现有移动协议，不在本次改造中重写移动架构。

渲染时间差用于动画、插值和局部视觉模拟；奖励、任务、资源恢复、共享世界推进不得依赖浏览器绘制次数。视觉模拟可以限制单帧时间差以避免长暂停后的爆炸性推进，但这不能代替服务器时间与重连校准。

### 10.2 后台状态不等于退出游戏

浏览器可能暂停隐藏页面的动画回调并限制计时器；`blur` 也不等同于页面已经不可见。[S13]

因此，渲染循环、网络会话、暂离策略分开管理。失去焦点时清理卡住的按键状态，不直接销毁世界连接。恢复可见时重新设置视觉时间基准，并根据服务器版本与时间取得必要的状态校准。

保留项目已有的“10 分钟完整会话/之后暂离驻留”产品方向，但不能将其解释为浏览器后台持续满帧运行的保证。浏览器冻结、系统休眠或网络中断时，由服务器维护约定的角色状态和恢复语义；本次 HMR 改造不能破坏这套机制。

### 10.3 性能与输入基线

建议记录帧间隔分布、长帧、draw calls、纹理/几何体数量、活动实体数、资源加载耗时与游戏连接次数。`renderer.info` 可提供渲染与对象数量统计，但不是精确显存字节计量器。[S07]

建立设备像素比上限与画质档位，避免高分辨率屏幕默认无限放大渲染负担。优先处理重复分配、重复订阅和不受控的粒子/纹理数量，再考虑 Worker、WebGPU 或复杂渲染框架。

输入层统一处理键盘、鼠标、触摸、UI 焦点与中文输入法组合状态。聊天输入中不应触发角色移动；场景 HMR 后一次按键只能发出一次对应命令。

---

## 11. 缓存、协议与发布安全

### 11.1 开发环境排除陈旧来源

排查顺序建议为：**入口与模式 → 当前进程 → 模块更新 → 资源版本 → 浏览器/Service Worker 缓存 → GPU 与应用内缓存**。

不要把“手工清所有缓存”作为正常开发步骤。Service Worker 可以拦截请求并从自身缓存响应，仅关闭浏览器 HTTP 缓存不代表绕过它。[S15]

建议开发模式不注册项目生产 Service Worker。若旧 Service Worker 已控制同一开发源，提供仅作用于本项目注册范围与缓存前缀的一次性迁移流程；注销后还要考虑现有受控页面的刷新。不得无差别清除同源其他应用数据。

生产缓存与开发缓存策略分开。开发时禁止使用 CDN 的旧构建地址替代本地源码，也不要每帧追加随机查询参数“防缓存”。

### 11.2 生产缓存策略

| 资源 | 建议策略 |
| --- | --- |
| HTML 入口与版本入口 | `Cache-Control: no-cache`，使用验证机制及时取得新入口 |
| 内容哈希 JS/CSS/图片 | 长缓存与 `immutable`，同一 URL 不更改内容 |
| 非哈希 `public` 文件 | 明确版本 URL 或使用验证缓存，不能无条件永久缓存 |
| 玩家私有 API | 按敏感性采用私有缓存或 `no-store`，不得误入公共静态缓存 |
| 地图与资源 manifest | 有版本、校验与更新策略 |

`no-cache` 表示使用前重新验证，不是完全不存储；`no-store` 才是不存储该响应。不要将两者混为一谈。[S14]

新版本部署后，旧页面仍可能请求旧的懒加载 chunk。保留仍处于支持窗口内的旧资源；发现版本不兼容时显示可理解的更新提示，不在战斗中无限强制刷新。[S02]

### 11.3 构建与回滚

```text
锁定依赖
  → 类型/静态/单元检查
  → 新 staging 目录构建
  → 生成 build-info 与资源清单
  → 产物检查与浏览器冒烟
  → 标记为可选发布版本
  → 显式切换服务指向
```

保留 `dist-tms273` 作为对外约定名称或当前版本入口，其背后可以映射到版本目录。不要在正在服务的目录中边删除边构建。

构建失败、资源校验失败、协议不兼容时，不切换当前版本。回滚包括前端产物与相容的资源版本；若后端协议或持久化结构不向后兼容，单纯切回前端目录不构成完整回滚。

### 11.4 协议与开发权限

游戏消息入口进行运行时校验；TS 类型不是对远端输入的验证。连接握手至少能够辨别客户端协议版本、后端实例/世界 epoch 与内容规则版本。

表现层热更新保留连接时，不能重复订阅或重复发送命令。整页刷新或 Rust 重启后，由服务器确认是否恢复会话并提供快照；旧命令重放需要已有幂等或序号规则，不盲目重发奖励、交易或任务提交。

`VITE_*` 变量会进入客户端构建，不能存放数据库凭据、服务端密钥或 GM 密钥。[S11] 开发参数面板默认只改本地表现；权威规则编辑必须经过有权限的后端接口，并限定开发环境。

---

## 12. 测试计划：同时证明“改动生效”与“构建可用”

### 12.1 两条独立验收跑道

```text
DEV_SOURCE：验证源码反馈、HMR、会话与资源生命周期
BUILT_PREVIEW：验证真正构建产物、路由、资源路径与发布兼容
```

两条跑道都通过，才能称为完成。开发模式通过不等于生产包通过；旧静态包通过更不等于新源码通过。

Playwright 支持通过 `webServer` 启动待测应用。本项目应让它调用统一脚本，并额外校验运行模式与 build/instance ID；CI 不复用未知服务，专用 HMR 测试也不修改开发者正在工作的源码目录。[S16]

### 12.2 开发入口与 HMR 用例

| 编号 | 操作 | 必须观察到的结果 |
| --- | --- | --- |
| D01 | 使用统一脚本进入 dev | 显示 DEV_SOURCE；加载真实入口与开发客户端 |
| D02 | 修改实际任务 UI 文案 | 浏览器自动出现新文案，Rust PID 不变 |
| D03 | 修改 CSS | 样式更新，页面会话不重建 |
| D04 | 修改水面或技能视觉参数 | 明确支持的参数即时应用，游戏连接不重建 |
| D05 | 修改已建立边界的表现模块 | 新实现生效，角色镜像与相机按契约保留 |
| D06 | 制造并修复语法错误 | 显示错误，修复后可恢复，不隐瞒失败 |
| D07 | 制造候选资源加载失败 | 释放候选，保留旧可用表现或明确安全刷新 |
| D08 | 连续快速保存多个版本 | 最后版本生效，旧异步结果不能覆盖 |
| D09 | 修改网络核心或不兼容状态结构 | 安全刷新/重连，取得服务器快照 |
| D10 | 外部进程占用 3010 | 脚本明确失败，不悄悄换端口或误用旧服务 |

验证 HMR 时要区分工具连接与游戏连接。页面存在 Vite WebSocket 是正常的；“游戏连接保持一个”不能被误写成“整个页面只能有一个 WebSocket”。

### 12.3 资源与生命周期压力用例

在同一真实地图循环执行不少于 30 次“参数更新、表现重建或切图”，记录 Canvas 数、主循环数、游戏连接数、输入订阅数及资源数量趋势。

期望主循环与主 Canvas 保持唯一，网络连接不因局部视觉改动重复建立，资源数量在缓存预热后进入有界区间。Three.js 内部缓存不一定回到零，因此观察稳定趋势与所有权，而不是强制每次精确等于初始数值。[S07][S09]

补充验证：纹理更新后画面确实换成新纹理；移除使用者后资源能回收；共享图集不会因销毁一个实体而让其他实体消失；切图期间完成的旧异步请求不会重新挂载旧地图对象。

### 12.4 构建产物验收

| 编号 | 检查 | 通过条件 |
| --- | --- | --- |
| B01 | 页面版本 | 浏览器 build ID 与选定产物元数据一致 |
| B02 | 开发残留 | 不加载 `@vite/client`、源码入口或开发事件通道 |
| B03 | 路由与资源 | 实际静态服务下 HTTP / WS / 图片 / shader 正常 |
| B04 | 错误路由 | API、WS、缺失 JS/图片不能误返回 SPA HTML |
| B05 | 刷新与深链接 | 需要支持的页面路径可以直接打开或刷新 |
| B06 | 主流程 | 登录、选角、进图、移动、NPC、任务、聊天正常 |
| B07 | 场景能力 | 技能、纹理、水面或其他已接入效果正常 |
| B08 | 新旧版本 | 旧页面懒加载不因立刻删 chunk 而失效 |
| B09 | 无源码依赖 | 无开发服务器时，静态产物仍可正常运行 |
| B10 | 回滚 | 能切回相容旧版本且有真实验收记录 |

资源缺失必须返回可诊断的错误，不能一律回退 `index.html`。这是排查“JS 请求拿到 HTML”与地图资源静默缺失的必要检查。

### 12.5 多人、后台与权威状态回归

使用两个独立浏览器会话：其中一个热更新表现时，另一个不应看到重复角色、重登闪烁或错误位移。奖励、任务完成、物品拾取不能因 HMR、重连或重放事件执行两次。

覆盖窗口失焦、隐藏后恢复、断网恢复和后端重启。验证原有 10 分钟暂离语义没有被绑定到渲染循环，也没有因为 dev HMR 事件被重置。

### 12.6 性能目标与测量方法

以下只是**建议的初始验收目标，尚未测量，不是工具保证**：

| 指标 | 初始目标与方法 |
| --- | --- |
| 小改动反馈 | 预热后连续 20 次局部修改，保存至首个确认新版本的帧，p95 争取 ≤ 2 秒 |
| 局部更新影响 | 支持的 CSS/表现 HMR 不重启 Rust，不新建游戏连接 |
| 渲染体验 | 在声明的目标设备、分辨率和地图负载下争取稳定 60 FPS，记录长帧 |
| 生命周期 | 30 次以上更新后没有持续增长的活动循环、连接、事件订阅与未释放资源 |
| 正确性 | 开发模式和构建模式主流程均通过 |

HMR 的 `afterUpdate` 事件不等于画面已经完成更新。需要由应用在新表现真正提交或渲染后记录应用级标记。

视觉回归固定测试地图、输入序列、时钟、随机种子、视口和渲染环境。Playwright 官方提示截图结果可能因操作系统、硬件和运行环境而变化，因此不以跨机器像素完全一致替代行为测试。[S17]

---

## 13. 分阶段实施与交付

### P0：现状取证，不改业务

- [ ] 找到 TMS273 真实源码入口、构建脚本与 3010 监听进程。
- [ ] 记录 HTTP / WS、资源转换、静态包加载和缓存链路。
- [ ] 输出 `docs/dev/current-entry-audit.md`。

**验收：能够用证据解释“改了哪个源码，为什么当前页面没有变化”。**

### P1：打通源码开发入口

- [ ] 复用或引入 Vite，将真实客户端源码纳入开发服务。
- [ ] 扩展统一脚本，默认 dev，明确分离 build 与 preview。
- [ ] 配置 HTTP / WS / 资源路由与严格端口。
- [ ] 加入模式与实例标识、错误覆盖层、独立类型检查。
- [ ] 允许 JS/TS 变化先自动整页刷新，不追求所有状态保留。

**验收：一处真实 UI 修改自动生效；不构建 dist，不重启 Rust；明确不再误测旧包。**

### P2：建立 Three.js 生命周期与高频 HMR 边界

- [ ] 明确 Renderer、网络、输入和表现层的所有权。
- [ ] 保证唯一帧循环、Canvas 与游戏连接。
- [ ] 选择一个高频模块试点，例如技能表现或水面效果。
- [ ] 实现候选准备、提交、旧资源释放与失败降级。
- [ ] 加入带版本的相机/UI 快照，拒绝不兼容复用。

**验收：支持的视觉改动不重新登录；连续更新不会累积资源或重复命令。**

### P3：补齐地图与资源更新

- [ ] 划分表现数据与权威规则数据。
- [ ] 对实际资源管线增加版本 manifest 和增量更新。
- [ ] 支持当前地图定向刷新与纹理显式替换。
- [ ] 增加异步 generation、防过期回调和共享资源所有权检查。

**验收：修改真实地图表现或资源可看到更新，且不存在前后端规则版本悄悄分叉。**

### P4：构建验收、自动化与回滚

- [ ] 建立独立的 DEV_SOURCE 与 BUILT_PREVIEW 测试。
- [ ] 产出不可变 staging、build-info、资源清单。
- [ ] 完成缓存、旧 chunk 保留与模式误用防护。
- [ ] 执行多人、后台、主流程和生命周期回归。
- [ ] 编写 `docs/dev/tms273-web-development.md` 与真实验收记录。

**验收：源码开发和静态交付都能被重复验证，且有相容版本的回滚路径。**

### P5：可选的创作工具，不作为本次闭环前置条件

根据 P1–P4 的实际收益，再考虑场景检查器、参数滑杆、碰撞调试叠层、技能时间轴、资源预览器，以及配置修改的审阅式落盘。

开发面板的目标是加速高频创作，不是另造完整游戏引擎。Worker、WebGPU、SSR、微前端、全量 ECS 与大型编辑器都不作为本次改造的必需项。

### 建议交付顺序

先完成 P0 + P1，单独提交；随后完成一个 P2 试点并做压力测试；再扩展 P3；最后完成 P4 的发布闭环。不要把所有重构塞进一个无法定位问题的大提交。

---

## 14. 常见失败模式与处置

| 现象 | 优先排查 | 不应采用的“修复” |
| --- | --- | --- |
| 保存后没有变化 | 是否 DEV_SOURCE、真实入口、HMR 链路与边界 | 无止境手动清缓存 |
| 修改后仍调用旧逻辑 | 旧实例、闭包、空 accept 边界 | 永久缓存整个 Runtime |
| 每次修改都重登 | 网络层是否被场景模块拥有 | 屏蔽所有更新传播 |
| 越改越卡 | 重复帧循环、事件、纹理和 RenderTarget | 把内存增长称为正常预热而不测量 |
| 地图看起来改了但走不过去 | 服务器碰撞/规则版本未更新 | 客户端绕过权威校验 |
| dev 正常，构建包失败 | base、资源路径、代理差异、动态加载 | 把生产也改成 Vite dev |
| 浏览器读到旧包 | 当前进程、build ID、HTML/SW 缓存 | 用当前 Git HEAD 假装旧包版本 |
| 构建失败把服务停了 | 编排顺序与版本切换 | 先 kill 后编译 |
| 新包发布后旧用户报 chunk 错 | 旧资源保留策略 | 每次错误都无限刷新 |
| 隐藏窗口后世界异常 | 渲染/网络/会话是否耦合 | 依赖后台持续绘制保证在线 |

回滚以阶段为单位。P1 出现问题时，可以通过统一脚本显式进入已验证的 BUILT_PREVIEW；这是应急验收入口，不是把旧包伪装成热更新模式。

---

## 15. 可交给编码代理的执行任务

```text
任务：将冒险岛复刻 TMS273 从“日常测试 dist-tms273 静态产物”改为
“默认源码开发 + 自动更新 + 选择性 Three.js HMR”，同时保留静态构建交付。

硬约束：
1. 所有集成启动都通过项目根目录 启动3010.command；扩展它，不增加平行启动流程。
2. 先阅读现有源码、脚本、锁文件、路由、资源加载与 Rust 服务配置。
3. dist-tms273 是构建产物，不手工修改生成 JS 来代替修改源码。
4. 默认浏览器访问入口保留 3010；端口绑定者变化须显式实现并验证。
5. 编译失败不停止当前正常服务；不按端口盲杀未知进程。
6. 普通前端改动不触发 Cargo 编译、不执行全量前端生产构建。
7. 前端负责表现与交互，Rust 继续维护权威规则和共享状态。
8. 不为热更新顺手重写整个渲染器、协议、任务系统或地图数据。

先执行 P0 + P1：
A. 产出现状入口映射，证明浏览器当前读取哪份代码。
B. 复用现有构建工具；有 Vite 就修入口与脚本，没有再最小引入。
C. 建立 dev / build / preview 的明确模式和版本标识。
D. 接通 HTTP、游戏 WebSocket 和真实资源路径。
E. 保存一处真实 UI 源码，证明浏览器自动显示修改。
F. 再证明静态构建仍能通过真实静态服务运行。

之后再做 P2：
选择一个技能或水面表现模块建立 HMR 边界。
保留稳定连接与权威镜像，显式重建表现并释放资源。
无兼容更新能力的模块允许安全整页刷新。

交付报告必须包含：
实际修改的文件、真实执行的统一脚本命令、运行模式、实例/build ID、
浏览器资源与连接证据、实际通过的用例、失败或未运行的用例。

禁止将“文档写完”“TypeScript 编译通过”或“旧 dist 能打开”描述为热更新已验证。
```

---

## 16. 官方资料与适用范围

以下资料用于核实工具能力与限制；模块边界、阶段划分、端口拓扑和指标目标是本文针对项目提出的工程设计，并非官方对该项目的保证。查阅日期均为 2026-09-11。

[S01] Vite — Getting Started。开发服务器与生产构建的分工。  
`https://vite.dev/guide/`

[S02] Vite — Building for Production。静态构建、构建监听、部署后旧 chunk 问题。  
`https://vite.dev/guide/build`

[S03] Vite — Server Options。端口、严格端口、HTTP/WS 代理、HMR/WebSocket 配置、监听与开发服务器安全。  
`https://vite.dev/config/server-options`

[S04] Vite — HMR API。accept、dispose、data、invalidate 与更新边界。  
`https://vite.dev/guide/api-hmr`

[S05] Vite — Backend Integration。开发时模块加载、生产 manifest 集成。  
`https://vite.dev/guide/backend-integration`

[S06] Vite — Deploying a Static Site。构建预览与生产服务器的边界。  
`https://vite.dev/guide/static-deploy`

[S07] Three.js — WebGLRenderer。动画循环、Renderer 生命周期、渲染统计。  
`https://threejs.org/docs/pages/WebGLRenderer.html`

[S08] Vite — Features。TypeScript 只转译、不做完整类型检查；原生开发能力。  
`https://vite.dev/guide/features.html`

[S09] Three.js — Cleanup。纹理、几何体、材质等 GPU 资源的显式清理。  
`https://threejs.org/manual/en/cleanup.html`

[S10] Vite — Static Asset Handling。模块导入资源、URL 处理与 public 目录。  
`https://vite.dev/guide/assets`

[S11] Vite — Env Variables and Modes。开发/生产条件与客户端环境变量暴露。  
`https://vite.dev/guide/env-and-mode`

[S12] Vite — Plugin API。开发插件与客户端/服务器自定义通信。  
`https://vite.dev/guide/api-plugin`

[S13] MDN — Page Visibility API。页面可见性与后台回调限制。  
`https://developer.mozilla.org/en-US/docs/Web/API/Page_Visibility_API`

[S14] MDN — HTTP caching。no-cache、no-store、版本 URL 与长期缓存语义。  
`https://developer.mozilla.org/en-US/docs/Web/HTTP/Guides/Caching`

[S15] MDN — Using Service Workers。请求拦截、缓存与生命周期。  
`https://developer.mozilla.org/en-US/docs/Web/API/Service_Worker_API/Using_Service_Workers`

[S16] Playwright — Web server。通过统一入口启动待测服务、就绪与服务复用。  
`https://playwright.dev/docs/test-webserver`

[S17] Playwright — Visual comparisons。截图对比与渲染环境差异。  
`https://playwright.dev/docs/test-snapshots`

---

## 最终完成定义

**保存源码后，开发页面能可靠展示新结果；需要保留状态的视觉修改不破坏会话；需要重启或刷新时行为明确；同一套源码仍能生成可验收、可追踪、可回滚的 dist-tms273。**

“实时 Web 大前端”的关键不是消灭静态产物，而是让源码、运行时、权威世界与发布版本之间的关系清楚、可观察、可验证。
