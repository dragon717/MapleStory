# 冒险岛复刻项目：超大文件治理与渐进式架构重构计划

> 日期：2026-09-11  
> 适用方向：Rust 权威服务器 + TypeScript / JavaScript / Three.js Web 客户端  
> 文档性质：结合既有项目约束与公开工程资料制定的执行计划，不是已完成的源码审计或重构报告。  
> 核心决策：保留单进程模块化架构，按业务职责组织模块，在模块内部适度分层；通过可验证、可回滚的小步迁移，逐渐收回状态所有权。

---

## 0. 结论：治理的不是行数，而是修改的牵连范围

本次不以“把每个文件压到 500 行”为目标，也不把重构等同于重写。

建议采用这条路线：

```text
确认源码与生成物 → 识别高频修改热点 → 建立行为基线
    → 提取一个完整职责 → 缩小依赖与写权限 → 验证并切换
    → 删除旧入口 → 用自动检查防止重新长回去
```

目标架构是**模块化单体 + 按功能聚合 + 轻量分层 + 明确状态所有者**。

服务器继续负责权威规则与事实；客户端负责交互、预测、状态投影和表现。既不为了拆文件拆微服务，也不为了套设计模式给每个函数增加接口、Manager、Service、Repository 和事件总线。

成功标准是：以后修改“任务完成条件”，不需要同时理解网络连接、背包渲染、地图加载和整个 `World`；修改“背包面板”，不会顺手改动服务器奖励规则。

物理拆分、职责拆分、所有权拆分是三个不同的完成层次。第一层可以作为低风险过渡，但不能冒充第三层已经完成。

## 1. 项目依据与适用边界

### 1.1 已有上下文与尚未核实的事实

| 项目项 | 本计划采用的依据 | 不能由此推出的结论 |
| --- | --- | --- |
| 客户端 | 已讨论 TS / JS / Three.js Web 大前端 | 不代表已使用 React、特定状态库或已完成 Vite 改造 |
| 服务端 | 已讨论 Rust 权威后端 | 不代表已确认 Tokio、Axum、数据库、网络库和线程模型 |
| `world.rs` | 之前提供的编译报错出现过该文件 | 当前路径、行数、职责和依赖均需重新审计 |
| `dist-tms273` | 已明确是测试过的构建产物 | 不能把修改产物当作修改源码，也不能据此判断源码模块大小 |
| 启动方式 | 统一使用项目根目录 `启动3010.command` | 不能假定脚本已有 `dev/check/preview/stop` 等参数 |
| 架构方向 | 单机起步、权威世界，业务规则与表现分离 | 不代表现在已具备进程迁移、完整命令日志或事件溯源 |
| 超大文件 | 用户反馈存在数千至数万行文件 | 尚无经过实测的文件清单、复杂度、修改频次或测试覆盖数据 |

本轮检索没有得到可核实的项目源码：已连接 GitHub 中的 MapleStory 仓库检索没有返回匹配仓库，特征文件代码检索失败，工作容器也没有挂载源码。文件库检索未取得可引用的相关架构文档全文。因此，下文目录、类型、任务名均是**建议设计**，不得写成“现有项目已经如此实现”。

执行者进入真实仓库后，先完成 P0，将建议路径映射到真实路径；存在合理的现有模块时优先复用，不为符合本文目录名而搬家。

### 1.2 本次硬约束

- 集成启动始终通过 `启动3010.command`。可以让它委托内部脚本，但不能绕过它另建一套启动流程。
- 不手工修改 `dist-tms273`、依赖目录、编译缓存和其他生成物来修复源代码问题。
- 纯结构重构不改变玩法规则、协议字段、存档结构、任务 ID、奖励顺序、Tick 次序和已确认的时序语义。
- 业务修复、性能优化、工具链升级与结构迁移分开提交；确实不可分时，明确解释行为差异并补对应测试。

### 1.3 暂不做的事情

本轮不引入微服务、Kafka、全量事件溯源、通用工作流引擎，不全量迁移 ECS，不替换 Web 渲染框架，不同时更换数据库或异步运行时。

也不提前为“天命、世界节点、商队、世界供给”等规划建立空目录或抽象平台。现有功能真实出现可复用规则时，再从用例中提取能力。

## 2. 联网调研：常见方法与本项目的取舍

以下是工程资料支持的方法，不是可以不经判断套用的“行业唯一最佳架构”。

| 方法 | 来源要点 | 对本项目的用法 | 主要边界 |
| --- | --- | --- | --- |
| 按模块移动代码 | Rust 官方支持把模块放到不同文件，同时保持模块树与调用关系。[S4] | 巨型 `world.rs` 可先低风险拆出职责块 | 移文件不会自动消除共享状态和循环依赖 |
| 单体内部建立边界 | Monolith First 强调先识别边界，避免过早支付服务拆分成本。[S1] | 先同进程函数调用、封装状态，再考虑进程边界 | 不能承诺未来拆服务器完全不改代码 |
| 限定模型与职责 | Bounded Context 强调明确模型边界及其关系。[S3] | 区分任务规则、资产规则、地图模拟和协议表示 | 一个文件夹不必等于一个限界上下文或服务 |
| 薄应用层、独立规则层 | Microsoft 的 DDD 分层资料区分用例协调、领域规则和基础设施。[S6] | 网络与存储细节不进入纯规则；跨业务流程由用例协调 | 只借鉴依赖关系，不照搬 .NET 微服务结构 |
| 渐进式替换 | Branch by Abstraction 通过过渡边界逐步替换实现。[S2] | 旧入口委托新实现；逐个迁移调用者 | 简单提取不必强制增加接口或功能开关 |
| 小而自洽的提交 | Google 建议小型自洽变更，并将重构和行为修改分开。[S7] | 测试、机械移动、收窄依赖分别提交 | 小是概念范围小，不是机械限制 diff 行数 |
| 明确生命周期与自动约束 | Vite 提供 HMR 清理 API，Three.js 要求显式资源释放，依赖工具可检查架构违规。[S8][S9][S10] | 拆前端时同步明确创建、更新、销毁职责；CI 防回潮 | HMR 不是热替换现有对象实例的魔法，目录也不是边界本身 |

**本项目的取舍：先拆最值得维护的责任边界，再让类型、接口和测试把边界固定下来。**

## 3. P0：先审计，避免拆错对象

### 3.1 区分六类大文件

| 分类 | 典型内容 | 治理方式 |
| --- | --- | --- |
| 手写业务代码 | 世界调度、战斗、任务、网络处理混合文件 | 重点检查职责、依赖、写状态范围及修改频率 |
| 手写内容配置 | NPC 对话、地图例外、任务链、技能参数 | 考虑数据与解释逻辑分离，并加引用校验 |
| 自动生成代码 | 协议类型、资源索引、数据转换输出 | 修改生成器和输入；保留来源及可复现生成流程 |
| 构建产物 | `dist-tms273` 内 bundle、压缩文件、source map | 不按手写源码规则拆分；需要优化时研究构建和加载策略 |
| 第三方代码 | vendor、导入库、工具附带实现 | 遵循依赖管理，不直接作为本轮业务重构对象 |
| 测试与样本 | 场景回放、快照、测试用例集合 | 按场景组织；大样本不等于大业务模块 |

不能仅凭文件超过一万行就认定它是最严重的问题；也不能因为代码被搬到 `data.ts` 或 `helpers.rs` 就认定问题已解决。

### 3.2 审计每个候选文件的内容

记录物理行数、非空行数、最大函数跨度、导出 API、依赖和反向依赖、近期修改频次、共同修改文件、直接读取/修改的状态、I/O 与副作用、相关测试、运行热点。

首次历史窗口建议选择最近 90 天或最近一个主要开发阶段。这是项目起始口径，不是行业标准。生成提交、全库格式化、批量重命名要单列，避免把机械变更误判为业务热点；浅克隆和缺失历史必须标注。

依赖图既看文件级循环，也看功能模块之间的循环。Rust 同一 crate 中模块互相引用可能通过编译，不能把“能编译”当成“架构无环”。

建议输出：

```text
artifacts/refactor/large-files.json        # 真实统计，标明分类与统计口径
artifacts/refactor/frontend-deps.json      # 含未解析路径与工具限制
artifacts/refactor/baseline-metrics.json   # 构建、测试与运行基线

docs/architecture/large-file-audit.md      # 热点、事实、风险与选点依据
docs/architecture/state-ownership.md      # 可变状态的唯一负责人
docs/architecture/module-boundaries.md    # 允许和禁止的依赖方向
```

这些路径是建议交付物，本文并未生成实际仓库的审计结果。

### 3.3 重构排序

优先级按“高频修改、经常出错、跨职责牵连大、当前功能继续发展、存在可测试切口”综合判断。单纯文件最长只作为辅助信号。

默认先选一条容易隔离、回归成本低的路径，例如任务条件计算、装备显示数据转换或聊天格式转换；不默认从战斗 Tick、跨图迁移或核心存档写入动刀。如果它们已经阻塞开发，则先增加保护测试，再优先处理。

审计记录模板：

| 真实路径 | 类别/行数 | 修改热点证据 | 混合职责 | 写状态范围 | 首个提取点 | 验证方式 | 优先级 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P0 实测填写 | 不填估算数字 | 提交/缺陷/需求依据 | 具体职责 | 所属对象与字段 | 一个完整职责 | 具体测试 | P1/P2/P3 |

## 4. 设计思想：功能聚合优先，分层为依赖服务

### 4.1 按变化原因划分，而不是按代码执行顺序划分

“任务条件、任务进度、任务内容和任务面板”有明确关联；“初始化阶段的所有代码”只有执行时机相同，不代表应该放在一个永久模块。

以三件事判断边界：这部分代码因什么需求变化？它维护什么事实和不变量？哪些外部细节不应被调用者知道？

例如 `inventory` 应隐藏格子分配、堆叠和容量规则，而不是只把一个 `HashMap` 暴露出去。调用者表达“尝试放入这组物品”，不应负责复刻它的内部算法。

### 4.2 一个功能不必拥有一套完整分层目录

小功能可以只有一个模块；复杂后再拆 `state`、`rules`、`use_cases`、`view`。不创建只有转发函数的空层，也不为一个实现默认创建一个 trait。

本计划借鉴领域分层，但不要求所有游戏系统采用完整 DDD 建模。战斗热路径可以保留紧凑数据和直接函数调用，内容读取可以保持简单。

### 4.3 状态所有者与模块不是一一对应

`QuestState`、`InventoryState` 和 `CharacterState` 可以由不同业务模块定义，但仍由同一玩家/地图的执行上下文持有和统一修改。**拆业务职责，不意味着拆开本来必须一起提交的状态。**

地图世界状态由原有权威执行上下文管理；网络连接状态由会话管理；客户端快照由状态同步入口更新；渲染资源由明确生命周期的对象管理。

首阶段保留实际已有线程模型。查清当前是否单写者之前，不能宣称它已经具备单写者保证；若存在多处并发写入，将修复作为独立工作包。

### 4.4 明确命令、查询与事实，但不引入完整 CQRS 平台

命令表示请求发生什么，例如“提交任务”；查询返回已有状态或视图；事实/效果表示已经发生的变化。三者在类型和职责上区分即可，不要求消息中间件、独立读数据库或全量事件溯源。

即时需要结果的业务调用优先使用普通函数。确实需要跨时间、跨执行上下文的任务才考虑队列；队列不是所有解耦问题的默认答案。[S14]

## 5. 目标架构：Rust 权威服务器

### 5.1 建议职责与静态依赖

```text
bootstrap / main              装配实现、配置、启动与关闭
    ├── adapters              网络、持久化、内容读取
    └── runtime               调度、权威状态持有、命令分派、复制
            └── application   跨业务用例协调、提交边界、必要端口
                    └── features/<业务>
                            state + rules + 本域操作

底层共享：少量稳定 ID、值类型与错误约定
协议模型：留在协议/适配边界，不作为所有领域状态的通用基类
```

这是依赖关系示意，不是要求一个请求逐层经过所有文件。网络适配器可依赖内侧定义的命令入口；存储适配器实现 application 中确有需要的端口；启动装配点选择具体实现。

规则层不得依赖 `runtime::World`、具体网络连接、SQL 客户端或文件 I/O。跨领域用例可以读取多个模块的公开能力，但不由 `quest` 偷偷进入 `inventory` 的私有实现。

### 5.2 建议目录：按实际存在的业务逐步展开

```text
server/src/                           # 需由 P0 核实真实服务端源码根
├── main.rs                           # 装配与启动；已有入口合理时保留
├── lib.rs                            # 仅确有集成测试/库边界需要时设置
├── world.rs                          # 过渡期兼容入口，最终只保留运行时职责
├── runtime/
│   ├── command_dispatch.rs
│   ├── tick.rs
│   └── replication.rs
├── application/
│   ├── claim_quest_reward.rs          # 跨任务、背包与角色状态的协调
│   └── ports.rs                      # 仅真实 I/O 或替换边界需要的端口
├── features/
│   ├── quest/
│   │   ├── mod.rs                    # 小型公开 API，不重导出全部内部项
│   │   ├── state.rs
│   │   ├── conditions.rs
│   │   └── progression.rs
│   ├── inventory/                    # 初始较小时可保持为单一 inventory.rs
│   ├── combat/
│   └── movement/
├── adapters/
│   ├── transport/
│   ├── persistence/
│   └── content/
└── shared/                           # 稳定值类型；不是通用杂物间
```

不一次建立全部目录。已有 `mage`、任务或物品模块时先评估复用；不为了形式对称而重新命名全库。

### 5.3 `World` 最终保留什么

保留权威状态的持有与定位、原有 Tick 调度、调用用例和系统、收集已应用变化并交给复制/存储边界的职责。

逐步移走技能数值公式、任务判定、奖励生成、文本格式化、内容解析、协议编码和数据库细节。Tick 文件可以保留一个明确的调度清单，但不继续容纳这些系统的实现。

如果把业务方法拆到十个 `impl World` 文件后，所有方法仍可读取和修改整个世界，完成的只是导航改善；下一步必须减少参数与写权限。

### 5.4 模块边界的禁止项

| 位置 | 可以依赖 | 不应依赖或执行 |
| --- | --- | --- |
| 纯规则 | 领域值类型、显式配置、显式时间/随机输入 | `World`、网络、数据库、系统时钟读取、隐藏随机源 |
| 业务状态模块 | 自身规则、必要稳定公共类型 | 其他业务的私有字段、具体复制协议 |
| application 用例 | 多个业务公开 API、内侧端口 | 隐藏在全局服务定位器里的所有依赖 |
| runtime | 用例、业务公开能力、调度与复制接口 | 数千行内联玩法分支 |
| adapters | 内侧端口、DTO、具体 I/O 库 | 绕过用例直接改权威资产与任务状态 |
| bootstrap | 具体实现与装配关系 | 核心玩法规则 |

## 6. Rust 大文件迁移：先可编译，再收窄权力

### 6.1 第一步：按职责移动，保持外部入口

假设 P0 确认 `world.rs` 是热点，可以先采用 Rust 原生模块机制：[S4][S5]

```text
迁移前：world.rs
迁移后：world.rs + world/quest.rs + world/combat.rs
```

首先搬走一块完整逻辑，保留旧方法签名、执行顺序和状态布局。移动、重命名与业务修改分开提交。不同时保留 `world.rs` 与 `world/mod.rs` 作为同一个模块的定义文件。[S4]

Rust 子模块可以访问祖先模块的私有项，这有利于过渡，但并不构成业务隔离。不要为解决编译错误把全库字段改成 `pub`。[S5]

### 6.2 第二步：提取可独立验证的计算

例如将“伤害计算”变为只依赖攻击属性、目标属性、技能规则和本次随机输入的计算；将“任务可完成性”变为只依赖任务定义与玩家任务事实的判定。

设计签名应表达这样的差异，以下是接口草图，不是可直接粘贴编译的现有 API：

```text
不佳：quest::can_complete(world: &World, player_id, quest_id)
较佳：quest::check_completion(definition, progress, player_facts)

不佳：combat::attack(context: &mut EverythingContext)
较佳：combat::resolve_hit(attacker_stats, target_stats, skill, roll)
```

纯计算返回结果、失败原因或效果计划，不自己广播网络、不写数据库、不在内部取当前时间。无需为了测试把所有简单参数包装成 trait。

### 6.3 第三步：封装修改规则

在本域操作中封装容量校验、装备合法性、任务状态转换等规则。调用者通过公开业务操作修改状态，不获得内部容器的无限制写权限。

可优先使用默认私有、`pub(super)` 或限定祖先作用域的 `pub(in ...)`；`pub(crate)` 意味着整个 crate 都可访问，不是“只向一个业务模块公开”。`pub(in ...)` 也不能用来指定任意兄弟模块。[S5]

### 6.4 Rust 特有风险

借用冲突先检查职责和数据访问范围，而不是首先增加 `Arc<Mutex<World>>`、`RefCell`、全量 `clone()`、`unsafe` 或静态可变状态。

对互不重叠的状态字段可显式拆借用；对同一个集合中多个实体的操作，要验证 ID 冲突、删除再插入的失败路径、索引失效和实体生命周期。不能为了通过编译改变实体更新次序。

将“读取并计算”和“有序应用”分开时，不复制整个世界做每帧快照。热路径允许复用缓冲区和窄数据视图；额外分配与遍历用实测决定。

模块抽取也不保证 Rust 编译自动提速。仅当边界稳定、存在独立编译或依赖隔离收益时，再考虑少量 crate；不要一个玩法系统一个 crate。

## 7. 必须保住的服务器语义：时序、资产与权威

### 7.1 Tick 的顺序就是行为的一部分

P0 先记录现有 Tick 中命令、移动、碰撞、技能、怪物、掉落、任务、复制与存储的真实先后关系。本文不指定一个新顺序作为“最佳顺序”。

重点记录同一 Tick 新产生的实体何时可见、删除何时生效、事件在本轮还是下一轮消费、Buff 到期与攻击判定的先后、时钟单位和暂停/恢复语义。

移动函数时保持这些顺序。将多个循环合并、改成并行系统、调整集合遍历方式、改变随机数消耗顺序，都应视为需要独立验证的行为改动。

### 7.2 示例：领取任务奖励，不拆散提交边界

建议用这一条用例验证分层是否真的有效：

```text
网络命令
  → 会话身份与请求归属校验
  → 权威执行上下文定位玩家
  → 任务模块检查任务状态与完成事实
  → 物品/角色模块检查容量、奖励合法性与经验变化
  → 应用层协调一个完整提交边界
  → 状态应用成功后形成可观察结果
  → 复制给客户端；按既有持久化保证处理保存
```

任务模块拥有任务判定，背包模块拥有容量与放入规则，用例拥有“这些改变要一起成立”的协调责任。不是把两者放进不同异步任务，再期望结果自然一致。

失败时不应出现“任务已领取但道具没进包”，也不应出现“已经发奖却因为后续失败仍可重领”。返回 `Result` 不会自动回滚之前已经完成的内存修改；单写者也不等于自动具有事务和崩溃恢复能力。

可以使用先验证后应用的窄效果计划，但应用阶段仍要明确原子性：要么保证验证后相关状态没有变化且提交步骤不会业务性失败，要么使用现有事务/预留/回滚机制。跨 `await` 或跨执行上下文后，需要重新校验版本与前置条件，不能沿用过期计划。

重构首先保留当前协议与持久化保证。发现重复发奖或半提交缺陷时，建立独立修复任务与测试，不在“纯移动代码”提交中隐式改规则。

### 7.3 “内存已应用”与“持久化已提交”分开命名

尚未持久化的变化，不应笼统称为 durable committed event。文档和代码应说明当前结果只是 `AppliedEffects`，还是已经满足存储提交承诺。

奖励、交易等经济操作沿用或明确加强原有持久化边界；低价值位置同步可以有不同策略。不能为了统一事件模型把所有状态升级成重型事务，也不能让资产降级为无保证的广播消息。

### 7.4 面向未来拆进程，但不提前分布式化

当前先做到命令入口清楚、共享可变引用不泄露、状态所有者可识别、外部 I/O 有边界。未来跨进程仍需处理请求标识、超时、重试、去重、顺序、跨分片事务与恢复；不能承诺只换一个适配器便自动完成。

如真实项目已经使用 Tokio，跨任务队列应有容量和过载策略，避免无界积压。[S13] 高频移动输入的合并策略与奖励命令的可靠处理策略不能混为一谈；必须分别定义是否可合并、拒绝、重试及如何反馈。

## 8. 目标架构：Web 前端

### 8.1 四种状态必须区分

| 状态 | 写入负责人 | 例子 | 约束 |
| --- | --- | --- | --- |
| 服务器权威状态的客户端投影 | 网络状态应用入口 | HP、背包、任务进度、其他玩家状态 | UI 与渲染器不能任意改写 |
| 本地预测与待确认输入 | 预测/输入子模块 | 本地位移预测、未确认输入序列 | 与服务器确认状态分离，支持纠正 |
| 纯 UI 状态 | 对应功能面板 | 面板打开、选择项、搜索与滚动位置 | 不伪装成服务器事实 |
| 表现与资源状态 | 视图/渲染生命周期负责人 | Three.js 对象、粒子、镜头、纹理引用 | 不作为判断伤害和发奖的权威依据 |

不强制更换当前状态管理库。先将谁能写、谁只能读说明白，再考虑是否需要新工具。

### 8.2 建议目录与依赖方向

下面的 `client/src` 是占位源码根，执行时必须映射真实项目，不应为了目录形式迁移整个前端。

```text
client/src/
├── app/
│   ├── bootstrap.ts                 # 唯一装配点
│   └── lifecycle.ts                 # 会话级、地图级、面板级生命周期
├── runtime/
│   ├── session.ts                   # 游戏会话、连接恢复协调
│   ├── applyServerMessage.ts        # 权威投影的写入入口
│   └── prediction.ts                # 仅需要预测时保留
├── model/
│   ├── worldSnapshot.ts             # 不包含 Three.js/DOM 对象
│   └── commands.ts                  # 客户端命令语义与必要端口
├── features/
│   ├── quest/                      # 任务面板、选择器、交互控制
│   ├── inventory/                  # 背包展示与交互
│   └── map/                        # 地图与角色表现，使用 engine
├── engine/
│   ├── renderLoop.ts
│   └── resources.ts                 # 通用渲染能力，不反向依赖玩法模块
├── infra/
│   ├── network/                    # 连接和协议传输实现
│   └── assets/                     # 资源加载与缓存实现
├── protocol/                       # Wire DTO 与解析边界
└── shared/                         # 很小的通用基础能力
```

`app` 负责装配；`features` 通过显式读模型与命令端口工作，不直接导入具体连接实现；`engine` 不反向导入任务、背包和应用启动；纯 `model` 不导入 DOM、Three.js 或真实 WebSocket。

若某个功能只有两百行，保留一个内聚模块即可；大型面板再在功能内部拆 `view`、`presenter`、`selectors`，不创建全局数万行 `views.ts` 或 `types.ts`。

### 8.3 拆前端入口的默认顺序

先提取不依赖全局状态的转换和选择器，再抽出资源加载/销毁边界，接着隔离服务器消息应用与只读模型，最后把一个完整面板或地图视图搬走。

启动文件最终只描述装配和生命周期，不再包含 UI 模板、网络协议大分支、地图构建、技能效果和鼠标键盘逻辑的全部实现。

接口通过构造参数或工厂参数传入。不要把 `window.game` 改名成 `GameContext` 后继续把整个客户端传给所有模块。

### 8.4 前端移动代码的隐藏行为变化

ES 模块顶层副作用、导入执行顺序、循环导入、方法失去 `this` 绑定、闭包保存旧对象、资源 URL 相对路径、大小写路径差异，都需要在迁移时检查。

只拆源码文件不等于浏览器下载了更多或更少的包。首次加载与分包优化属于构建/运行策略，另做测量，不在本轮承诺加载速度提升。

## 9. Three.js、切图与 HMR：生命周期也是架构边界

### 9.1 三层资源寿命

会话级资源包括连接、账号会话、长期缓存和总调度入口；地图级资源包括地图场景、地图订阅、局部音效和地图对象；面板级资源包括 DOM、交互监听和短期订阅。

销毁地图不应关闭整个账号会话；关闭背包不应销毁其他视图仍在使用的共享纹理。共享资源由资源所有者或引用计数/租约机制管理，不能每个消费者自行全量 `dispose()`。

Three.js 官方明确指出，移除场景对象不等于释放其几何体、材质或纹理；纹理与材质的释放也分别处理。[S9]

### 9.2 对外提供清晰生命周期

需要持有副作用的模块应能表达创建、订阅、更新和销毁。可以使用 `createView(...): { update, dispose }` 或当前代码习惯中的对象，不强制建立庞大的基类体系。

销毁应覆盖动画循环、事件监听、状态订阅、计时器、异步加载回调、GPU 资源和地图级网络订阅。销毁尽量幂等，清理某项失败不应阻止剩余清理。

异步加载应有地图/会话代次或取消标识；旧地图请求晚返回时，不得重新挂进新场景。只检查“文件已经拆开”发现不了这类问题。

### 9.3 HMR 的取舍

若项目已采用或随后采用 Vite，可用 `import.meta.hot.dispose` 清理热更新副作用；接受模块更新还需要明确替换或重建哪些实例，旧闭包不会因为源文件变化自动更新。[S8]

优先热替换样式、纯展示和局部视图；状态结构、协议、核心会话发生不兼容变化时允许完整刷新与正常重连。不要为了追求“永不刷新”引入复杂且不可验证的状态迁移。

会话与地图生命周期稳定后，再逐步提升保状态 HMR。后台保活逻辑属于会话层，不依赖面板或 Three.js 帧回调持续运行；原有保活需求的实现与结构迁移分开验收。

## 10. 大量内容、协议与生成代码的专门治理

### 10.1 任务/NPC/地图/技能内容

如果超大文件主要是不同 ID 的重复条件、对话或参数，可以评估分成三部分：原始内容输入、规范化领域定义、少量真正需要代码的特殊处理器。

简单数据优先用数据表达，复杂特殊规则保留具名代码。不要把一万个 `switch` 分支换成一万个难调试的匿名回调，也不要为了配置化发明通用脚本语言。

必须有任务前置引用、NPC/地图存在性、奖励定义、重复 ID 和可达性检查。把数据搬进 JSON 不代表数据正确；维多利亚港后主线断点、找不到 NPC 等现有问题应成为内容验证的回归场景。

保留 TMS273 来源数据与本项目修订的可追溯关系。可采用原始内容 + 明确覆盖记录，而不是为了修一个任务无记录地重写整份导出文件。

### 10.2 协议与存档

抽取 DTO 时，固定现有序列化标签、字段名、可选/缺省差异、枚举表示、数组顺序、数字宽度和错误表示。Rust `u64` 到 JS 的处理沿用已验证约定，不能随意全部转成 `number`。

领域状态、Wire DTO 和保存模型职责可以分开，但本轮不必同步迁移持久化格式。采用边界映射与契约测试，避免“改了内部字段名，存档/协议也跟着变”。

### 10.3 生成流程

生成文件应标明输入、生成器版本或定位信息、再生方法，并能验证确定性。CI 可在隔离工作区重生成并比较，不覆盖开发者尚未提交的文件。

“大文件豁免”只适用于经过确认的生成物/样本，不允许通过加入生成标记把手写业务代码绕过门禁。

## 11. 测试策略：先证明没变，再证明更好维护

### 11.1 行为刻画测试

先给目标切口补能描述现有外部行为的测试，再移动实现。Golden master/快照用于对比，不替代明确的业务断言；已确认的错误单列缺陷及修复期望，不能永久把错误当正确规范。

对权威业务记录输入命令、初始状态、显式时间、随机输入、相关内容版本、结果状态和有序效果。只归一化无语义的内容，例如展示比较中的无序集合；不能排序真实业务事件来掩盖顺序变化。

随机抽样次数本身可能影响后续行为。比起只固定随机种子，关键链路还应能核对随机输入消费次序；容器遍历不稳定也需纳入分析。

### 11.2 新旧实现比较

优先在测试中对同一输入运行旧/新纯计算，比较返回值、拒绝原因和效果。副作用路径只能有一个权威执行者，禁止把同一真实请求发给新旧系统各发一次奖励。

需要影子比较时，使用只读/隔离数据和禁用副作用的执行环境，不连接真实写库、广播或邮件接口。简单搬移不必上生产影子执行。

### 11.3 项目回归矩阵

| 场景 | 核心断言 | 推荐测试层级 |
| --- | --- | --- |
| 登录与进入地图 | 角色状态正确，入口与资源可用 | 集成 + 浏览器 |
| 移动、跳跃和平台 | 碰撞、落地、已有阻挡语义不变 | 规则/回放 + 浏览器 |
| 技能、伤害、Buff | 判定次序、数值、到期与随机消费一致 | 规则 + Tick 回放 |
| 掉落与拾取 | 归属、容量、重复请求行为符合既有保证 | 用例 + 集成 |
| 接取/完成/领取任务 | 前置、NPC、状态、奖励没有半提交 | 内容校验 + 用例 |
| 维多利亚港后的主线 | 已修复链路继续可达，未修缺陷明确记录 | 内容图验证 + 浏览器 |
| 切图与多人可见性 | 实体出现/消失、会话和地图状态不串线 | 多客户端集成 |
| 聊天与系统消息 | 作用域、顺序、权限及显示正确 | 用例 + 浏览器 |
| 失焦与恢复 | 会话行为不依赖某个视图继续渲染 | 会话状态机 + 真浏览器手测 |
| HMR/反复切图 | 无重复帧循环、订阅、连接和持续资源增长 | 开发环境 + 运行指标 |
| 保存与重启 | 协议/存档兼容，经济状态符合保存保证 | 持久化集成 |

浏览器自动化不能完整替代真实后台节流测试；WebGL 像素截图也不能替代权威状态断言。固定视口、测试地图和输入，结合可观察状态与必要的截图。

### 11.4 性能基线

记录服务端 Tick 的 p50/p95/p99、命令队列长度、CPU/内存，客户端帧时、首屏/切图时间、活动连接/监听/动画循环数量，以及冷构建、增量构建、类型检查耗时。

相同机器、相同数据集和负载下比较。首轮由 P0 设定可接受预算并记录噪声；不虚构“拆分后快 30%”。开发构建与发布构建分别测量。

Three.js 资源在预热后应趋于稳定，不要求 `renderer.info.memory` 归零；内部缓存和共享资源可能合理保留。[S9]

## 12. 分阶段执行计划

### P0：建立真实地图

读取真实入口、构建脚本、锁文件、Cargo manifests、前端配置和项目已有架构说明；区分源码与生成物，得到热点清单、状态所有权表、实际调用路径和已知缺陷列表。

**交付/退出条件：**审计可复现；能解释前三个候选热点为何值得拆；确定一个首个试点；不存在把猜测目录当事实的记录。没有运行数据的项目明确标记“未测”，不能以零替代。

### P1：建立安全网与防回潮基线

为试点补行为刻画、协议/内容契约与最小主链路测试；记录原有 lint/编译问题；创建逐文件历史例外和新增依赖检查。

**交付/退出条件：**测试在旧实现上先通过；已知失败有明确原因；一处故意制造的依赖违规能被检查拦住；启动能确认当前源码/构建版本，而不是旧 `dist-tms273`。

### P2：最小机械拆分

仅搬出一个职责块，保留入口、数据布局、调用顺序和工具链。编译问题尽量通过正确模块路径和最小可见性解决。

**交付/退出条件：**每个提交可构建、可测试；差异可说明哪些是搬移、哪些是必要路径变化；无新增玩法行为变化。此阶段允许仍有 `impl World`，但记录为过渡状态。

### P3：完成一个真正解耦的试点

把该职责的规则和状态操作收进业务模块；将整个 `World`/全局客户端上下文依赖缩小到必要参数；旧入口改为薄委托。存在双实现时在测试中比较，逐步移除旧实现。

**交付/退出条件：**试点规则无需启动服务器或 WebGL 即可测试；修改该规则不再改巨型入口；新模块不反向依赖旧实现；对外 API 能解释，不暴露内部容器。

### P4：按热点逐个扩展服务端边界

依次迁移下一条实际业务路径。跨领域资产操作、Tick、地图迁移等高风险路径分别建立提交边界与时序测试。迁移一个能力，完成一个能力，不同时开十个半成品系统。

**交付/退出条件：**目标热点职责明确，旧 `World` 停止承接新业务；协议和保存格式保持兼容；状态所有权与调用图同步更新。

### P5：迁移前端并验证生命周期

分离权威投影、UI 状态和预测；搬迁一个面板或地图视图；明确会话、地图和资源寿命。按当前构建现状支持 HMR 或重载，不将引入 Vite 与全库拆分绑成一个提交。

**交付/退出条件：**一个视图可独立销毁/重建；服务器消息不会重复应用；热更新与切图不会重复连接或注册；开发与发布链路都通过核心回归。

### P6：收尾与门禁固化

删除无调用旧入口、临时适配层、完成使命的迁移开关和旧实现；复审导出面、遗留豁免和模块 README；比较修改牵连范围与性能。

**交付/退出条件：**不同时维护两套同等权威的实现；没有为了未来保留的无人调用抽象；CI 能阻止新增巨型手写文件、跨模块深层导入和新增依赖环。

前后端可以在边界已确定且不改同一协议/文件时并行推进；以上是依赖阶段，不是工期承诺。

## 13. 推荐的首轮提交拆法

| 提交 | 概念范围 | 必须包含 | 不应顺带做 |
| --- | --- | --- | --- |
| A：审计 | 确认一个真实热点与切口 | 真实数据、依赖、基线命令 | 迁移整个目录 |
| B：保护测试 | 固定该职责现有行为 | 正常、边界、拒绝路径 | 用新期望掩盖旧缺陷 |
| C：机械移动 | 搬走该职责 | 旧入口兼容、最小路径变更 | 改算法、协议或工具版本 |
| D：收窄依赖 | 去掉全量上下文与越界写入 | 新公开 API、调用实例、规则测试 | 全局事件总线/容器改造 |
| E：清理闭环 | 删除迁移遗留并固定边界 | 无旧反向依赖、CI 规则、文档 | 保留无人调用的旧路径 |

Google 的小变更原则强调自洽与可审查性，不是按固定行数机械拆提交。[S7] 大量纯搬移可单独提交并辅助查看 moved-code diff；人工语义修改应保持小范围，便于核对和回滚。

## 14. 自动化门禁与工具选择

### 14.1 行数是预警和讨论入口

以下是本项目建议的初始治理预算，不是语言规范或通用行业标准，P0 后可调整：

| 对象 | 起始规则 | 历史与合理例外 |
| --- | --- | --- |
| 新 TS/JS 手写业务文件 | 非空且排除纯注释后超过 600 行预警，超过 1000 行需解释职责，超过 1500 行无豁免则阻断 | 内容、样本和生成物另分类 |
| 新 Rust 手写业务文件 | 超过 800 行预警，超过 1200 行需解释职责，超过 1500 行无豁免则阻断 | 大型内聚算法或穷举定义可审查豁免 |
| 超大历史文件 | 固定基线，不继续承接新职责 | 必要委托和迁移胶水可有窄例外 |
| 函数 | 超过约 80 行触发人工检查 | 不按行数拆碎完整算法；同时看嵌套与分支 |
| 新依赖环/新跨域私有访问 | 阻断 | 历史例外逐条登记，不以大目录排除 |

同时保留物理行数，禁止通过删除注释、压缩代码、把逻辑藏进宏或大字符串降低统计。

ESLint 的 `max-lines` 支持跳过空行和纯注释行。[S11] 但 Rust/TS 的真正业务边界仍要靠类型、依赖检查和审查，不是依靠一个行数规则。

### 14.2 前端依赖约束

优先采用当前已安装的检查工具；缺失时可评估 dependency-cruiser 的循环和路径规则，以及 ESLint `no-restricted-imports` 的导入限制。[S10][S12]

至少检查 `engine` 不导入 `features/app`、纯模型不导入 UI/网络实现、跨功能只走公开 API、已迁移代码不反向引用遗留大文件。

解析配置必须覆盖真实别名、扩展名、入口、静态/动态导入与类型导入。运行期循环和仅类型循环分别报告；不能只扫描 `main.ts` 然后宣称所有孤立文件已检查。

现有循环应按具体边或问题登记；不能因为一个旧循环存在就豁免整块目录的新循环。未解析导入必须显示出来，不悄悄算作无依赖。

### 14.3 Rust 依赖约束

使用 Rust 可见性限制实现细节，并用架构测试或静态扫描检查禁止的模块依赖。扫描应覆盖 `use` 与直接限定路径，注明宏展开和动态机制等盲区；简单 grep 不能证明整个架构满足约束。

Cargo 的 crate 依赖图可验证 crate 级边界，但不自动检查同一个 crate 内业务模块的所有依赖方向。边界稳定后，确有价值才将纯领域核与适配运行时拆为少量 crate。

### 14.4 类型检查与测试命令

P0 先读取实际锁文件、工作区和脚本，沿用项目现有工具版本；不要因官方文档更新而顺带升级依赖。

以下 Rust 命令供确认工作区后接入统一检查流程，不是绕过启动脚本运行服务：[S16][S17]

```bash
cargo fmt --all -- --check
cargo clippy --workspace --all-targets --locked
cargo test --workspace --locked
```

在正确工作区执行或提供已核实的 `--manifest-path`。历史警告先登记、阻止新增；清理到基线允许时，再加入 `-- -D warnings`。不盲目使用可能互斥的 `--all-features`。

若测试改为 `--all-targets` 选择目标，需另行考虑 `--doc` 测试，不能误以为前者覆盖所有文档测试。[S16]

前端使用项目真实的包管理器及已有 `typecheck/lint/test/build` 命令；没有这些脚本时，再在独立提交中新增。TypeScript Project References 是类型检查/构建分项目的可选工具，只有边界和规模确有需要时启用，不是拆文件的前提。[S15]

### 14.5 启动与浏览器测试必须核对身份

集成测试由 `启动3010.command` 启动。需要自动化检查模式时，先读取脚本，保留双击默认行为，再通过同一脚本增加或复用能力；本文不假定任何参数已存在。

Playwright 支持通过 `webServer.command` 启动测试服务，可以接到该统一入口。[S18] 但必须先确认脚本是否前台驻留、后台派生子进程，以及退出清理方式，不能只填命令字符串就认定管理正确。

健康检查要确认项目身份、当前版本指纹、开发/发布模式和资源版本。端口 3010 可访问，不等于运行的是这次代码。CI 不应无条件复用机器上已有服务；本地复用也要验证身份。

## 15. 回滚与风险处理

纯结构迁移尽量保持协议、存档和外部接口，便于按提交回退。回滚前确认活跃进程、状态兼容和写入边界；不将运行中持有新状态的地图随意切回旧状态实现。

| 风险 | 提前防护 | 出现后处理 |
| --- | --- | --- |
| 编译通过但 Tick 行为变化 | 固定输入、时间、随机与效果顺序 | 回退该语义变更，不继续扩大迁移 |
| 奖励半提交/重复应用 | 明确提交边界和请求重试语义 | 停止新路径，保留审计证据，先处理受影响状态 |
| TS 循环与顶层副作用 | 依赖图、移除导入时隐式启动 | 恢复旧入口，改为显式装配 |
| HMR/切图泄漏 | 生命周期计数、共享资源所有权 | 禁用不安全热替换，使用完整重载 |
| GPU 资源被提前释放 | 区分拥有与借用，共享缓存统一释放 | 修复引用/租约，不盲目全场景 dispose |
| 数据拆分后引用失效 | 内容引用和路径校验 | 回退数据迁移，保留明确覆盖来源 |
| 新架构更难追踪 | 一个职责一个公开入口、直接调用优先 | 删除空层与过度通用接口 |
| 多人/AI 修改冲突 | 文件写入负责人、契约先定 | 逐任务整合，不以覆盖文件解决冲突 |

存在真实数据格式迁移时，单独制定向前/向后兼容方案、备份和恢复演练；这不属于一次简单代码回退能解决的问题。

## 16. AI 协作与长期防回潮规则

每个执行任务写清目标职责、允许修改文件、不能改变的行为、输入输出契约、相关测试和退出条件。一个巨型文件同一阶段只安排一个写入负责人，其他人可以审查或处理不重叠文件。

入口和共享协议变更由集成人统一处理。允许不同功能并行，不允许多个执行者同时把同一个 `world.rs` 各自重写，再期待自动合并保持行为。

新功能进入所属业务模块；确实尚无清晰归属时，先说明变化原因与状态所有者，不默认塞回入口、`utils`、`common` 或全局 Manager。

每个已迁移模块保留短 README，写明负责什么、不负责什么、公开 API、可变状态所有者、依赖方向和测试入口。内容应能在一屏内理解，不复制整套架构论文。

可交给编码执行者的任务说明：

```text
阅读本计划及仓库已有架构说明，先完成 P0。
所有路径、行数、框架与测试结果以真实仓库为准，不得根据计划伪造。
确认源码根、启动3010.command、dist-tms273 的生成关系和实际热点。
选一个完整且可验证的职责作为试点，先补旧实现的行为测试。
按“测试 → 机械移动 → 收窄依赖 → 删除旧路径”的顺序分提交执行。
不同时升级工具链、改变协议/存档、引入微服务或改动玩法数值。
集成启动只使用启动3010.command；检查当前版本，避免测试旧构建。
每个提交给出变更范围、实际执行命令、测试结果、未验证项和回滚点。
遇到行为差异先定位，不能通过删测试、放宽断言或公开全部状态绕过。
```

## 17. 完成定义

本轮不是要求一次消灭仓库内全部长文件。选定热点完成以下闭环，才算完成对应治理任务：

- **行为闭环：**关键回归通过；协议、存档与时序保持已声明兼容；已知缺陷和本次行为修复清楚分开。
- **边界闭环：**规则可以脱离真实网络/渲染测试；写状态权限明确；迁移模块不反向依赖旧巨型入口；没有把巨型依赖搬进新 Context。
- **维护闭环：**新增依赖违规能被 CI 拦截；临时旧实现和开关已移除或有明确退出条件；开发与发布链路验证当前版本。

建议用一个后续小需求做验收：例如修改任务条件或背包显示。记录需要阅读和修改的业务模块，确认“不相关模块无需修改”确实成立。该指标比单纯比较文件总数更接近本轮目标。

---

## 附录 A：可直接执行的只读初筛脚本

在真实 Git 仓库根执行以下脚本。它只列出已跟踪及未忽略的未跟踪文本文件，不改业务文件；自动分类仅作为候选，不能直接成为豁免依据。非空行不等同于剔除注释后的源码行数，精确语言指标仍需后续工具。

脚本不做复杂度、依赖或业务风险推断，也不把未跟踪内容上传到任何外部服务。

文档交付前，附录脚本已在临时 Git 测试仓库验证中文/空格路径、子目录执行、已跟踪/未跟踪文件、忽略规则、生成物候选、缺失文件、符号链接与只读行为。该验证不代表已经扫描真实 MapleStory 仓库；本轮未执行项目构建、玩法回归或性能测试。

```python
#!/usr/bin/env python3
"""Read-only first-pass size inventory; run from anywhere inside a Git repo."""
from __future__ import annotations

import json
import subprocess
import sys
from pathlib import Path

EXTENSIONS = {
    ".rs", ".ts", ".tsx", ".js", ".jsx", ".mjs", ".cjs",
    ".json", ".css", ".scss", ".glsl", ".wgsl", ".html",
}
EXCLUDED = {
    ".git", "node_modules", "target", "dist", "dist-tms273",
    "coverage", ".vite", ".cache", "vendor", "third_party",
}
GENERATED_MARKERS = (
    "@generated", "code generated", "automatically generated", "do not edit",
)


def git(*args: str) -> bytes:
    return subprocess.check_output(["git", *args], stderr=subprocess.PIPE)


def main() -> int:
    try:
        root = Path(git("rev-parse", "--show-toplevel").decode().strip())
        raw = subprocess.check_output(
            ["git", "-C", str(root), "ls-files", "-z", "--cached",
             "--others", "--exclude-standard"], stderr=subprocess.PIPE,
        )
    except (OSError, subprocess.CalledProcessError, UnicodeError) as exc:
        print(f"Cannot read Git working tree: {exc}", file=sys.stderr)
        return 1

    rows: list[dict[str, object]] = []
    warnings: list[str] = []
    for encoded in sorted(set(raw.split(b"\0"))):
        if not encoded:
            continue
        name = encoded.decode("utf-8", errors="surrogateescape")
        rel = Path(name)
        if any(part in EXCLUDED for part in rel.parts):
            continue
        if rel.suffix.lower() not in EXTENSIONS:
            continue
        path = root / rel
        # Skip symlinks, including symlinked parent directories.
        if any((root / Path(*rel.parts[:i])).is_symlink()
               for i in range(1, len(rel.parts) + 1)):
            warnings.append(f"Skipped symlink path: {name}")
            continue
        if not path.is_file():
            warnings.append(f"Missing/non-file path: {name}")
            continue
        try:
            text = path.read_text(encoding="utf-8")
        except (OSError, UnicodeError) as exc:
            warnings.append(f"Cannot read {name}: {type(exc).__name__}")
            continue
        if "\0" in text:
            warnings.append(f"Skipped binary-like file: {name}")
            continue
        lines = text.splitlines()
        header = "\n".join(lines[:20]).lower()
        if any(marker in header for marker in GENERATED_MARKERS):
            category = "generated-candidate"
        elif (any(p in {"test", "tests", "fixtures", "snapshots"}
                  for p in rel.parts)
              or ".test." in rel.name or ".spec." in rel.name):
            category = "test-or-fixture-candidate"
        elif rel.suffix == ".json":
            category = "data-or-config-candidate"
        else:
            category = "handwritten-or-unclassified"
        rows.append({
            "path": name,
            "physical_lines": len(lines),
            "nonblank_lines": sum(bool(line.strip()) for line in lines),
            "category": category,
        })
    rows.sort(key=lambda row: (-int(row["physical_lines"]), str(row["path"])))
    print(json.dumps({
        "scope": "tracked-and-unignored-untracked-working-tree",
        "excluded_directory_names": sorted(EXCLUDED),
        "limitations": "Not SLOC, complexity, churn, or dependency analysis; "
                        "submodule repositories require a separate scan.",
        "files": rows,
        "warnings": warnings,
    }, ensure_ascii=True, indent=2))
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

如真实项目把手写源码放在上述排除目录，先调整排除规则再扫描。大文件过多时，也应检查尚未纳入扩展名的脚本、模板、原始内容和子模块。不能将“被排除”解释为“已审计无问题”。

## 附录 B：来源与使用范围

检索日期：2026-09-11。以下优先使用作者原文、官方语言/框架文档和工具维护者文档。文中行数预算、迁移排序、目录结构和项目验收标准属于本计划建议，不是来源给出的通用标准。实现时以仓库锁定版本对应文档为准。

| 编号 | 来源与地址 | 本文借鉴点 |
| --- | --- | --- |
| S1 | Martin Fowler，Monolith First；`https://martinfowler.com/bliki/MonolithFirst.html` | 先验证模块边界，谨慎引入服务拆分成本；不视为无条件定理 |
| S2 | Martin Fowler，Branch By Abstraction；`https://www.martinfowler.com/bliki/BranchByAbstraction.html` | 通过过渡边界逐步替换，保持系统可运行 |
| S3 | Martin Fowler，Bounded Context；`https://martinfowler.com/bliki/BoundedContext.html` | 明确模型边界及关系，不将所有模型强制统一 |
| S4 | The Rust Programming Language，Separating Modules into Different Files；`https://doc.rust-lang.org/stable/book/ch07-05-separating-modules-into-different-files.html` | 原生模块拆文件，区分模块树与物理文件 |
| S5 | The Rust Reference，Visibility and privacy；`https://doc.rust-lang.org/reference/visibility-and-privacy.html` | 私有性与限定可见性，祖先/子模块访问规则 |
| S6 | Microsoft Learn，Designing a DDD-oriented microservice；`https://learn.microsoft.com/en-us/dotnet/architecture/microservices/microservice-ddd-cqrs-patterns/ddd-oriented-microservice` | 区分应用协调、领域规则与基础设施；只借鉴分层，不照搬微服务 |
| S7 | Google Engineering Practices，Small CLs；`https://google.github.io/eng-practices/review/developer/small-cls.html` | 自洽小变更、相关测试、重构与行为变更分开 |
| S8 | Vite，HMR API；`https://vite.dev/guide/api-hmr` | 热更新边界、副作用清理、接受更新的责任 |
| S9 | Three.js，How to dispose of Objects；`https://threejs.org/manual/en/how-to-dispose-of-objects.html` | GPU 资源显式释放、共享资源、内存统计的合理解释 |
| S10 | dependency-cruiser，Rules reference；`https://github.com/sverweij/dependency-cruiser/blob/main/doc/rules-reference.md` | 循环依赖、禁止依赖、未解析路径和范围限制 |
| S11 | ESLint，max-lines；`https://eslint.org/docs/latest/rules/max-lines` | 可配置行数预警、空行与注释口径 |
| S12 | ESLint，no-restricted-imports；`https://eslint.org/docs/latest/rules/no-restricted-imports` | 显式限制导入路径 |
| S13 | Tokio，Channels；`https://tokio.rs/tokio/tutorial/channels` | 有界队列、背压与显式并发 |
| S14 | Robert Nystrom，Game Programming Patterns: Event Queue；`https://gameprogrammingpatterns.com/event-queue.html` | 只在确有时间解耦需求时使用队列 |
| S15 | TypeScript，Project References；`https://www.typescriptlang.org/docs/handbook/project-references.html` | 大项目可选的分项目检查与构建，不是重构前提 |
| S16 | The Cargo Book，cargo test；`https://doc.rust-lang.org/cargo/commands/cargo-test.html` | 工作区测试、目标选择、锁文件与文档测试 |
| S17 | Clippy Documentation，Usage；`https://doc.rust-lang.org/clippy/usage.html` | 与现有 Rust 检查流程整合 |
| S18 | Playwright，Web server；`https://playwright.dev/docs/test-webserver` | 浏览器测试与统一启动入口集成 |

---

**最终方向：让每一块代码负责一件能够说清楚的事，让每一份状态有明确的写入负责人，让每次改变都能单独验证。文件变小是这件事的结果，不是替代品。**
