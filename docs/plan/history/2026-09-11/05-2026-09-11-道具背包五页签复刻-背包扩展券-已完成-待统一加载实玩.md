# 道具背包五页签复刻 + 背包扩展券：已完成，待统一加载实玩（2026-09-11）
> 状态：未完成（含已落地部分）

> 归档自迁移前 `PLAN.md`（超大文件治理：md 与代码同一口径）。原文逐字保留，未做改写。
> 归档位置：`docs/plan/history/2026-09-11/05-2026-09-11-道具背包五页签复刻-背包扩展券-已完成-待统一加载实玩.md`　　归档顺序：05/66（PLAN.md 原倒序）


- 选题：物品栏此前只按"页签序号 = inventoryType − 1"直线映射，而 TMS273 源
  `UI/UIInventory.img/Inventory` 的 tab 帧序是 **裝備/消耗/其他/裝飾/現金，第 4 号帧画在第 3 号帧之前**
  ⇒ 其他(4xxxx) 落到現金页、裝飾(3xxxx) 落到其他页，背包除裝備/消耗外"看起来没有页签内容"。
- 来源 T（本地 TMS273.7，非猜）：`tab:category/<normal|selected>/<0..4>` 帧序即视觉序，
  tab 2 帧绘"其他"、tab 3 帧绘"裝飾"（`names.ts` 注释即反证记录）；manifest 已带两套窗口尺寸
  （AutoBuild 32 格 / FullAutoBuild 128 格）的全部 20 张页签帧。
- 客户端修复（commit 621ca33）：`names.ts::itemCategoryTab` 改为显式 `CATEGORY_TAB{1:0,2:1,4:2,3:3,5:4}`；
  `view.ts` 的 `TAB_LABEL_KEYS` 与 `TAB_INVENTORY_TYPE{0:1,1:2,2:4,3:3,4:5}` 同步改序，
  所有 `inventoryMove/dropItem/gather/sort/useItem` 出站消息与 `slotLimit()` 均经该映射换算，
  拖拽 payload 同步修正。未知 id 仍兜底到其他页。
- 背包扩展券（背包类物品，作用=增加该页格数，复用 TMS273 真实道具）：
  - `Item/Consume/0243` 家族四张券已入库并可购：**2430768 裝備欄 / 2430769 消耗欄 / 2430770 裝飾欄 /
    2430771 其他欄 8格擴充券**，均 `info.slotExpand=<目标页>`，源商店 `Shop/1001100`（104000002 鯊魚海岸
    场景 NPC）有售（50000 枫币，源价）。
  - 服务端 `inventory.rs::slot_expand_target` 判型（slotExpand 1..=4，其余消耗品返回 None），
    `auth.rs`（Store 事务内）与 `world.rs`（内存路径）双路实现：容量 +8（`SLOT_EXPAND_STEP`），
    封顶 128（`MAX_SLOT_LIMIT`，String 目录"最多可擴增到128格欄位"），同事务消耗券；拒绝时不消耗，
    重放不重复扩容；容量走 `player_stats.inventory_slots_json` 持久化并随快照 `inventorySlots` 下发，
    客户端按容量启用格子并纳入渲染签名（扩容立即刷新网格）。超 24 格的行读写/仓库迁移同步修通
    （此前 `slot BETWEEN 1 AND 24` 会吞掉扩容后的物品）。
  - 初始赠券：新账号首次初始化在消耗页末位赠 2430768 ×1（`starter_backpack_seeded` 幂等标记），
    让"用券扩容"从首次登录就可体验。
  - P 边界：源里券是 `notConsume` + NPC 脚本消耗（`consume_243xxxx`），运行时实现为双击直用即耗
    （脚本载体未复刻，明确标 P）；現金页暂无 5xxxx 物品来源（原版经商城，未复刻），页签可切、恒空。
- 验证：`cargo build --offline` 通过；全量 `cargo test --offline` **285 过 / 6 失败**，失败集与 PLAN
  既有基线逐条一致（auth third_store、mage bundled_catalog、world config_drop / portal_command /
  reactor_area、third_sphere），**零回归**（含新增 `slot_expand_acceptance.rs`）；客户端 `tsc --noEmit`、
  `check:inventory`、`i18n.check` 通过。本轮加固：`check_inventory.mjs` 新增**帧序断言**（解析 view.ts 的
  TAB_LABEL_KEYS / TAB_INVENTORY_TYPE，钉死"4 在 3 前"，页签重排会在此失败）+ manifest 五页签帧、
  四张券图标存在性断言；`i18n.ts` 的第三页签中文由"设置"改"装饰"对齐源绘文本。
- 待验（用户）：`启动3010.command` 统一构建加载后实玩——消耗/其他/裝飾/現金页签切换与道具归页；
  双击扩展券 +8 格（4 张券各对应一页）、扩到 128 拒绝且不消耗；商店（104000002）购买四券；
  扩容后第 25 格起可放物、重登后格数与物品均保留；新账号首登消耗页末位有赠券。
