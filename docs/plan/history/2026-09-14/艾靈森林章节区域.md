# 艾靈森林章节区域装配（审计 T06）

- 日期：2026-09-14
- 范围：审计 §11 **T06｜只扩当前旅程真正需要的地图、NPC 与交通**（P1）
- 协议：**23 不变**；内容版本：**`tms273-24` → `tms273-25`**（新增地图/任务/NPC/怪物为内容变更）

## 1. 这次交付的是什么

冒險家重製第二章「**艾靈森林編年史**」（TMS273.7 `Quest.wz` 36341–36367，lvmin 95，源 `QuestData` 里唯一不依赖脚本入口、可整章解锁的后续章节）的完整区域：

- **21 张图全部按源装配**（87 → 108）：现代侧 `222020000 赫爾奧斯塔圖書館`（懷玆 2040052）、`222020400 時間監控室`（可玲 2041029）；过去侧 19 图——`300000000 亞泰爾營地` 枢纽、`300000002 帳篷`、`300000010 營地會議場`（过去赫麗娜 2131000）、`300000100 小森林`（时空桥）、`300010000 苔蘚森林`、`300010100/200/300/400/410/420`、`300020000/200/210`、`300030000/010/200/300/310`（封印的森林与两间首領房）。
- **可执行任务 18 → 34**：36341/36342/36343/36345/36346/36348/36349/36354/36355/36356/36357/36358/36359/36360/36361/36366 共 16 条解锁，其中 36354/36357/36360 是**类型化击杀任务**（T03 的击杀进度直接生效），碴烏（5250004×1）与艾畢奈亞（5250007×1）在两间首領房里真的刷得出来。
- 新增可刷怪 10 种（4250000/4250001、5250000–5250007），新 NPC 13 个模板入图（287 个 NPC 位），随新怪 MobReward 源掉落表带入 149 件掉落物定义。

## 2. 非内容修复（同属本模块）

1. **装配器空 job 分类缺陷**（`scripts/tms273_remaster.cjs`）：源 `Check/0` **不写 `job` 节点 = 原版不限职业**，此前被当成 `job-route-unmapped` 阻塞——这正是艾靈森林 27 条全部被挡的死因。现在只有源写了职业列表且映射不到项目法师路线才阻塞；运行时 `conditions.job` 空列表本就是"不限"（`quest_rules.rs::jobs_match` 有既有测试）。服务端语义零改动。
2. **戒指无纸娃娃层豁免**（`export_tms273_avatar_parts.cjs` + `assemble_tms273.cjs`）：新怪掉落把戒指（islot `Ri`，源在 `Character/Ring`，纸娃娃合成器不读）带进目录，导出/装配按 Po/Tm 先例豁免——两处共用同一份豁免清单。
3. **两条 HEAD 既有红项顺手修绿**：`check_tms273_remaster.cjs` 的可执行 job 断言语义随 §2.1 更新；其 1402 `ruleVersion` 允许列表补上用户 b93b430 已提交的 `tms273-mage8-p1`（法师 8 级一转，P 级）。

## 3. 来源与保护

- **T（台服同版 WZ 实测，2026-09-14）**：21 图的全部几何/life/portal/returnMap 来自客户端 `Map.wz` dump（`dump_tms273_wz_json.cjs`）；NPC/怪物归属、时间门配对、脚本门名（`in_chowBoss`/`out_elinCave`/`in_300010300`/`in_fairyBoss`/`out_fairyBoss`）全部来自全量 life/portal 扫描（15347 图，11113 张有效）。
- **P（登记为有限适配）**：6 条脚本门的服务端路由（`server/src/ellinel.rs`，模式与 `ship.rs` 的 `inERShip` 一致）。时间门的前向穿越原版由 `q36342s` 脚本把关，本路由不做任务状态校验；配对依据是源内真实回程门 `300000100.out00 → 222020400/in01`（`tn` 显式配对）。落点用目标图内的门位（原版 `sp` 是演出悬浮位，离地 38~122px）。
- **不做**：营地的 `tp`×6、赫麗娜家 `cellar`/`enterWarehouse`、圖書館 `enterHRpt`/`downtown2015`、封印的森林 `elin_elluel`、`exit_party6` 等脚本门保持「走近提示一次」旧行为——对应任务仍登记 `script-scene`/`script-counter`；36317 的 `dummy` 计数器依旧阻塞，未被顺手放行。
- **未触碰**：GM 政策、calamity 既有放置、既有 15 条任务规格（验收确认零 diff）、其他职业支线（1401/1403/1404/1405/2570/2684 仍带 `other-job-route`）。

## 4. 数据变更

- `shared/gameplay.json`（任务 34 条可执行 + 新怪/NPC/掉落）、`shared/maps.json`（108 图）、`shared/items.json`（+149 掉落物定义，全部源自 MobReward/商店源数据）、`shared/npc-names.json`、`references/tms273-data/{maps,manifest}.json`；内容版本四处同步（`assemble_tms273.cjs`、`shared/protocol.ts`、`server/src/protocol.rs`、`check_tms273_runtime.cjs` 默认值）。协议 23 未变，客户端**必须与服务端一起换**（`启动3010.command` 统一加载）。
- 无数据库迁移；旧角色存档、宠物、装备不受影响（纯新增目录数据）。

## 5. 实际执行的检查

| 命令 | 结果 |
| --- | --- |
| `python3 scripts/import_tms273.py` | 108/108 图，0 missing |
| `node scripts/build_tms273.cjs`（全链，含 21 图资源渲染） | 装配 108 maps / 56538 assets / 287 npcs / 38 monsters |
| `node scripts/check_tms273_runtime.cjs` | 108 maps / 87719 引用一致 |
| `node scripts/check_tms273_remaster.cjs` | 55 条逐字段对齐，19 条可执行清单吻合 |
| `cd client && npx tsc --noEmit` | 0 错误 |
| `cd client && npm run check` | **34/35**（唯一失败 `layer-animation.check.ts` 的 windbell 无扩展名导入 = 已登记 HEAD 既有） |
| `cd server && cargo test --offline` | **410 过 / 0 失败**（基线 404 + 新增 5 条 ellinel 验收 + 钉数更新） |
| 3011 + 临时库真内容启动 | health ok：`contentVersion tms273-25` / `protocolVersion 23`；验证实例已停，未动 3010 |
| 子代理独立验收 | **ACCEPT**（A–E 五项全过：数据忠实、修复无外溢、路由配对成立、四项检查亲测复现、边界干净） |

## 6. 未验证

- **未实玩**：在线 3010 仍是旧二进制，本轮未重启；新区域的实际步行观感、门位提示、任务对话与 boss 战手感需用户统一加载后核看。
- **未核看画面**：21 张新图的渲染均来自源 WZ 导出并通过引用一致性检查，但没有同版截图比对；高塔中继层（222020100/200/300）与玩具城方向不装配，圖書館/時間監控室的 `out00` 出塔门会提示目标未收录——这是登记的边界，不是缺陷。

## 7. 用户实玩入口

经 `启动3010.command` 统一加载（内容 `tms273-25` 前后端必须一起换）后：

- 大地图打开**路德斯湖/玩具城页**：点击赫爾奧斯塔圖書館或時間監控室落位（自然步行链路——玩具城→路德斯湖→赫爾奧斯塔塔——尚未装配，属后续区域项目，当前由既有大地图跳转承担接入；HOLD-02 边界已在 PLAN 登记）。
- 按任务链走：36341（懷玆·圖書館）→ 36342（可玲·時間監控室）→ 36343 起进入过去侧；時間監控室的時間門（`in01`）走进即落小森林，全区域步行可达，兩間首領房随 36357/36360 可打；`300000100` 的 `out00` 原路走回現代。
- 重点核看：过去侧死亡复活应落在亞泰爾營地（returnMap=300000000）；重登后任务进度保留。

## 8. 回退边界

代码与内容可整体回退（协议 23 两端未动，无需降协议）；`shared/*.json` 回退到上一提交即可，无数据库迁移。已装配 `client/public-tms273` 资源由装配链再生。

## 9. 后续阻塞（同步 PLAN，不另立台账）

- **玩具城方向自然步行链路**（路德斯湖→玩具城→赫爾奧斯塔塔中继层/电梯）是下一片 T06 区域；当前章节接入依赖大地图跳转（HOLD-02 既有机制）。
- 36317 的 `infoex dummy` 语义、36362–36364/36367 脚本场景仍按原边界阻塞；36325/36330 需职业任务链（1410–1462）先立项。
