# 飞行船航线系统二期：耶雷弗线与埃德爾斯坦线

状态：数据链、服务端、检查与验收全部落地，待统一加载 3010 后实玩。日期：2026-09-14。

## 1. 范围

按一期方案列出的二期清单，落地两条新航线闭环（候船—检票—开船—航行—到站），协议 20→**21**，内容 `tms273-22`→**`tms273-23`**，装配目录 77→**87 图**：

- **耶雷弗线**：大厅 `104020100` 的 `in01`（`inERShip` 脚本门，P 级放行）→ 树顶 `104020120` 前往耶雷弗的站台（`1100007` 检票）⇄ 船 `130090000` → 天空渡口 `130000210`（奇里盧 `1100003` 检票、奇盧 `1100004` 播报）。渡口 `out00`（`pt_00_130000210` 脚本门，P 级路由）→ 前庭 `130000200` → 耶雷弗城 `130000000`（城图随目录已在库）。
- **埃德爾斯坦线**：树顶 `104020130` 前往埃德爾斯坦站台（`2150010` 检票）⇄ 船 `200090600/601`（甲板/船舱）→ 埃德爾斯坦码头 `310000010`（`2150008` 检票）→ `out00` 进埃德爾斯坦城 `310000000`；返程船 `200090610/611`。
- **不在本期**：航行中蝙蝠魔（Balrog）事件、候船室 `station_in`/阿霖机制、票务道具（仍免费登船，P）。

## 2. 源事实与 P 级决策

- WZ 实测（`dump_tms273_wz_json.cjs`）：10 张新图全在 TMS273.7（`130000200`/`130000210`/`130090000`、`200000170` + 船图四张 `200090600/601/610/611`、`310000000`/`310000010`）；7 张簇内图（耶雷弗城/130000101/130030006/310040100/200/210/300）此前已随装配目录在库。
- 耶雷弗船源里只有一张 `130090000`（无船舱，`in00` 为任务脚本门不开放），**双向共用**；快照 `ship.route` 按乘客名单归属，未登记旁观者看去程。
- 埃德爾斯坦船按源 returnMap 分侧（`200090600/601→200000170`、`200090610/611→310000010`）；甲板↔船舱经 `move_OrbEde`/`move_EdeOrb` 脚本门（pt:9），脚本体缺失，P 级固定互切。
- 舱门 `out00..09`（pt:3，源 tm 直指对端码头/天空之城码头）按源「靠港开门」建模：**航行中服务端拒绝**（关闭提示旧行为），检票相位由服务端接管 warp **落回本端检票站台** `sp`（源 `st00` 悬空 170px，落点交给 `ground_below` 解析；源 tm 反映原版船物理停靠位，本实现登船在树顶站台/埃德爾斯坦码头，舱门即「靠港下船」）。
- 联网复核经典乘船规则与一期一致（15 分钟班次、开船前 1 分钟停止登船、10 分钟航程）；时刻常量沿用 `ship.rs` 一组，未新增。

## 3. 改动

1. **数据链**（一次跑完）：dump 10 图入 `WZ_JSON_TW` → `import_tms273.py` 新增 `ADDITIONAL_SHIP2_MAP_IDS`（77→87）→ `build_tms273.cjs` 全链（import → mob unpack → maps/entities/ui/windows/portals/effects → gameplay → backfill → items → cashshop → avatar 全套 → reactor → minimap → worldmap → assemble）。assemble 产物：**87 maps / 52207 assets / 265 npcs / 27 monsters**。
2. **导出器源缺口修复**（新商店带入新品类）：`export_tms273_avatar.cjs` 补窄别名 `weaponBelowHead→weaponOverArmBelowHead`（01582001 用 post-BB 拼写，本客户端 zmap 只有旧名）；`export_tms273_avatar_parts.cjs` 与 `assemble_tms273.cjs` 把 Tm（图腾，源在 `Character/Mechanic`，无纸娃娃层）与 Po 同例跳过。
3. **服务端 `ship.rs`**：`SHIP_ROUTES` 2→6 条（新增 `victoria-erev`/`erev-victoria`/`victoria-edelstein`/`edelstein-victoria`）；`cabin` 允许空串（耶雷弗船无船舱）；`ship_snapshot_route_index`（共用甲板按名单归属）；`ship_portal_gate`（`in01` 放行、渡口 `out00` 回前庭、舷侧门关闭、move 门互切、舱门相位闸）；`ship_passengers` 数组 2→6。`portals.rs::handle_portal` 在查表前调用钩子；`world.rs` 快照改走 `ship_snapshot_route_index`。
4. **协议 21**：`shared/protocol.ts` `ShipSnapshotState.route` 扩为 6 值联合；`PROTOCOL_VERSION` 20→21、`CONTENT_VERSION` 四处同步（protocol.ts / protocol.rs / assemble / check 默认值）。客户端与 bot 不消费 route 字符串，无其他改动。
5. **检查**：`check_tms273_runtime.cjs` 77→87、新增二期十图装配/returnMap/检票员落位断言、unshipped returnMap 白名单改为 `200000100/200000170→200000000`（310 簇五图因 310000000 入库而出列）、`WORLD_MAP_ABSENT` 增 6 图（130090000/200000170/2000906xx 源无 spot）、贴地审计豁免服务端自有舱门；`boss_acceptance.rs` 目录 77→87 + 十图加载断言；`scroll_acceptance.rs` 孤儿城更新；`world_tests.rs` 贴地审计同步豁免。
6. **验收 `ship_acceptance.rs`**（新增 6 项）：耶雷弗线双向检票/到站与名单清空、共用甲板快照归属（乘客定向、旁观者看去程）、埃德爾斯坦线含船舱乘客到站、舱门相位闸 + 检票相位落回登船站台 + move 门互切 + 舷侧门关闭、在船大地图/卷軸拒绝面、`in01`/渡口 `out00` 脚本门路由；一期 `ship_snapshot_field_rides_only_route_maps` 扩展钉死六航线 id 与四名新检票员。

## 3.1 子代理验收与缺陷修复

独立子代理验收结论 PASS，同时发现 3 个中等级缺陷，均已在本轮修复并补验收断言：

- **D1 大地图下船绕过**：`handle_world_map_move` 原来不拦船图，航行中可点大地图逃离 → 新增 `ship_is_on_board_map` 拒绝面。
- **D2 回家卷軸逃离**：`plan_map_move` 未拒船图，可于航行中用 2030000 落对端码头 → 同一拒绝面（`scroll_blocked`）。
- **D3 去程舱门落点错配**：`ship_hatch_exit` 原把去程舱门放到天空之城码头 `200000170`，与到站台 310000010 矛盾 → 统一改为「舱门 = 靠港下船，落回本端检票站台」（返程线源语义本就一致）。

遗留疑点（低、记录在案）：相位闸按门名白名单而非穷尽枚举（当前船图无其它可达门名）；共用甲板双航线同一时刻表为 P 级简化（仅展示层差异）。

## 4. 验证

- `cargo test --offline`：**378 过 / 7 失败**，失败集与既有基线逐名一致（auth third_store、mage bundled_catalog、world config_drop/portal_command/reactor_area/third_sphere、world shop_sell auto price），未顺手修。
- `node scripts/check_tms273_runtime.cjs`：**87 maps / 82891 source references** 全过。
- `tsc --noEmit` 0；`npm run check` **29/31**（两项 HEAD 既有失败：layer-animation 导入、build-release 进程表）。

## 5. 部署（待用户执行）

**协议 20→21，客户端与服务端必须一起换**：

```
zsh 启动3010.command
```

重启作废会话 token，页面刷新重登即可，不是回归。新装 10 图素材已进 manifest（52207 assets），启动 prepare 会走全量同步。

## 6. 待实玩确认

- 大厅 `in01` 光门能进 `104020120`；与 `1100007` 对话检票登船落 `130090000` 甲板。
- 天空渡口 `130000210`：与奇里盧检票返程；与奇盧对话得班次播报；`out00` 走到前庭 `130000200`，经 `130000101`/`130030006` 进耶雷弗城 `130000000`。
- 树顶 `104020130` 与 `2150010` 检票去埃德爾斯坦；甲板 `move` 门进出船舱 `200090601`；到站 `310000010` 后 `out00` 进城；红狗 `2150008` 检票返程。
- 航行中尝试走舱门/舷侧门应得关闭提示；检票相位走舱门落回本端站台；航行中大地图跳转与回家卷軸被拒绝。
- 死亡复活按源 returnMap（船图 130090000 回自身、2000906xx 回 200000170/310000010、200000170 回 200000000 未装配目录边界）。

## 7. 三期（另行排期）

航行中蝙蝠魔 Balrog 事件、候船室 `station_in` 脚本门与阿霖机制、票务道具核实接入、耶雷弗城/埃德爾斯坦城内 NPC 职能与脚本门。
