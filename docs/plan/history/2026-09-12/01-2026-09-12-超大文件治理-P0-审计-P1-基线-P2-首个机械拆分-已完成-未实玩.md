# 超大文件治理：P0 审计 + P1 基线 + P2 首个机械拆分（通讯职责）已完成，未实玩（2026-09-12）
> 状态：未完成（含已落地部分）

> 归档自迁移前 `PLAN.md`（超大文件治理：md 与代码同一口径）。原文逐字保留，未做改写。
> 归档位置：`docs/plan/history/2026-09-12/01-2026-09-12-超大文件治理-P0-审计-P1-基线-P2-首个机械拆分-已完成-未实玩.md`　　归档顺序：01/66（PLAN.md 原倒序）


- 依据：`MapleStory_Large_File_Refactoring_Plan.md`（主）+ `MapleStory_TMS273_Web_Realtime_HMR_Development_Plan.md`（P0 取证）。
  用户要求：Rust / JS / TS / Three.js / md 都按同一规则渐进式重构。
- **P0 审计（真实扫描，非估算）**：新增只读脚本 `scripts/refactor_audit.cjs`（node .cjs，匹配仓库 scripts 约定），
  产物 `artifacts/refactor/large-files.json`（326 文件，0 读取警告）、`frontend-deps.json`、`baseline-metrics.json`。
  热点实况：`server/src/world.rs` **23,992 行 / churn 36**（全仓第 1）、`server/src/auth.rs` 8,410 行 / churn 17、
  `client/src/features/inventory/view.ts` 1,640 行 / churn 13；`protocol.rs` churn 21、`client/src/app/main.ts` churn 24、
  `client/src/scenes/world.ts` churn 22（后三者是契约面/装配点，**决定不先拆**，理由写进审计文档）。
  前端依赖图：81 节点 / 154 边 / **0 未解析** / **1 处循环**（`assets/manifest.ts ⇄ features/entry/appearance.ts`）
  / **1 处越界**（`features/entry/view.ts → network/session.ts`）。
  发现 3 个**已被 git 跟踪的 iCloud 冲突副本**（`client/src/features/loading/` 下 `style 2.css` / `view 2.ts` /
  `view.check 2.mjs`，原文件时间戳更新 ⇒ 副本是陈旧重复物）。**本轮未删除**（删除已跟踪文件待你确认）。
- **P1 基线（旧实现先跑绿）**：服务端 `cargo test --offline` **292 过 / 6 失败**，失败集与既有基线逐条一致
  （auth third_store、mage bundled_catalog 56≠43、world config_drop / portal_command / reactor_area、third_sphere）；
  编译警告基线 **43 条**；前端 `tsc --noEmit` exit 0，`npm run check` **16 过 / 1 失败**
  （唯一失败是 `features/npc/dialogue.check.mjs` 的 data: URL harness 限制，既有失败）。全部记入 `baseline-metrics.json`。
- **P2 首个机械拆分（试点：通讯职责）**：新模块 `server/src/messaging.rs`，挂载沿用仓库既有先例
  `#[path = "messaging.rs"] mod messaging;`（同 `boss.rs`）。搬走 `handle_chat` / `handle_whisper` / `whisper_echo` /
  `whisper_reject` / `chat_reject` / `chat_consume_token` / `handle_emoticon` / `emoticon_consume_budget`
  与 5 个通讯策略常量，共 **542 行**。
  - **纯搬移已证明**：与移除区段逐行 diff，542 行完全一致（唯一差异是截取范围末尾一个空行）。
  - 必要的路径变化只有可见性：3 个入口改 `pub(super) fn`（`world.rs` 的 `command()` 要调用，与 boss.rs 先例一致）、
    5 个常量改 `pub(super) const`；`world.rs` 一处引用改 `messaging::CHAT_TOKEN_BURST`，`mod tests` 加
    `use super::messaging::*;` 让既有 `*_acceptance.rs` 的裸名引用继续成立。
  - **有意不搬**：`EmoticonCatalogue` / `EmoticonLimit` 留在 `world.rs`——它们是 `Gameplay.emoticons`（公开字段）
    的类型，搬进私有子模块会让公开接口引用更窄可见类型（E0446），绕开只能是净损失。
  - **状态布局零改动**：`Player` 的 `chat_*` / `whisper_recent` / `emoticon_*` 与 `World` 的 `*_sequence` /
    `emoticon_ids` 全部原地不动；零协议变体、零存档字段变化、零客户端改动。
- 验证：定向 chat / whisper / emoticon 全通过；全量 `cargo test --offline` **292 过 / 6 失败**（与基线逐条一致，
  **零回归**）；编译警告 **43 → 43**（零新增）；`world.rs` **23,992 → 23,436 行**（仍在 1500 阻断线之上，
  `impl World` 仍是单块 ⇒ **记为过渡状态，不是治理完成**）；前端 `tsc --noEmit` 仍 exit 0。
- 文档产出：`docs/architecture/large-file-audit.md`（含试点选点判据与被否候选的理由）、
  `state-ownership.md`（状态所有者表 + 「内存已应用 vs 持久化已提交」分名）、`module-boundaries.md`（真实目录的
  允许/禁止依赖方向 + 实测违规）、`docs/dev/current-entry-audit.md`（HMR 计划 P0：入口/构建/资源/端口链路）。
  md 文件也按同一口径评估，结论与建议见 `large-file-audit.md` §6（`PLAN.md` 1,149 行 / churn 29 建议拆分，**本轮未拆**）。
- 未做 / 待你决定：
  1. **HMR 计划 P1**（把统一脚本改造成 dev/build/preview/status/stop 模式）会**改变双击启动的默认行为**
     （从「构建+服务」变成「Vite 源码开发 + 内部 Rust」），且验收必须真跑一次（即重启 3010）⇒ 按既有约定不擅自重启，
     待你确认时机。取证结论：dev 链路**技术上已通**（`npm run dev` + 已配置的 `/api`、`/ws` 代理），
     缺的是统一入口、`strictPort`、模式/实例标识与 HMR 边界。
  2. iCloud 冲突副本 3 个：建议删除（有同类先例提交），待你确认。
  3. 防回潮门禁（CI 拦截新增巨型文件 / 跨域深层导入 / 依赖环）尚未接线，本轮只提供可运行的检查脚本。
- 未实玩：本轮**未重启 3010、未改数据库、未跑浏览器**，聊天/密语/表情的实际表现待统一加载后验证。
