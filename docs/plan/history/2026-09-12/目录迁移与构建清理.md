# 目录迁移与构建清理

> 状态：已完成（文件迁移与离线/构建验证）；本轮未启动服务。

- 按 README 落实根目录白名单、技术/策划/计划分类、165 份原历史记录按任务归档、研究包搬迁和证据索引；入口同步 AGENTS 与 MEMORY。
- 清理 9 份旧构建及可再生 Cargo 缓存，保留数据库、恢复快照和打开文件。运行控制归入 runtime，历史证据逐任务归入 evidence。
- 构建统一候选准备、配对切换、健康后提交与失败恢复；当前版和上一版最多两套。打包独立保留当前包和上一包，临时文件不再散落根目录。
- 验证：npm check 29/29；前端类型检查、真实 Rust/Vite 候选编译通过；两种 ZIP 实际打包完整性校验通过；根目录/入口/索引检查通过，迁移映射无缺失目标，文档本地链接无断链。
- 本轮未执行启停；Windows 原生启停/PowerShell 实机行为仍按既有计划待用户验证。新候选仅用于编译验证，验证后清理，保留迁移前成功构建供正常入口下一次轮替。
- 根因、清理数量与映射见 [迁移证据](../../../../evidence/2026-09-12/directory-migration/README.md)。原始记录不因本次整理变成当前版本的验收结论。
