# 危險地帶與UFO街區域（审计 T06 下一片）

- 日期：2026-09-15
- 范围：审计 §11 **T06｜只扩当前旅程真正需要的地图、NPC 与交通**（P1）——PLAN 登记的「再下一片 T06/交通」：地球防衛總部街西段（`221030000 危險地帶` → 洛斯威爾草原Ⅰ~Ⅳ → UFO 內部）
- 协议：**23 不变**；内容版本：**`tms273-28` → `tms273-29`**
- 提交：`455628a feat: 危險地帶與UFO街區域装配——23图与地球防衛總部西向草原/UFO步行链路，新增呼叫器与三扇脚本门P路由（审计T06）`

## 1. 这次交付的是什么

让上一片交付的地球防衛本部（`221000000 本部`）**西侧出口第一次不再是死门**，并把原版「呼叫器送进 UFO」这条唯一的进场路径做成可走通的闭环：

- **23 图按源装配**（165 → 188）：
  - 地面草原链 5 图：`221030000 危險地帶`（本部 `west00` 直连）→ `221030100/200/300/400 洛斯威爾草原Ⅰ~Ⅳ`，全部是**源静态门**（`east00 ⇄ west00` 双向配对，runtime 与 cargo 双重钉住）。
  - UFO 內部 18 图：`221030500 德秋的房間`、`221030510/520/530/540 走廊101~104`、`221030550`+`221030551`（源把**走廊105 切成两段**）、`221030600 走道201`、`221030610~630 走廊202~204`、`221030640~660 走廊H01~H03`、`221030700~730 通風口D-1~D-4`。
- **4 条 P 级路由**（`server/src/ufo.rs`，模式与 `ellinel.rs`/`helios.rs` 一致，源授权逐条来自 `Map/Map/Graph.json`）：
  1. **NPC `2052026 UFO呼叫器` 对话即进场**（`221030400 草原Ⅳ` → `221030520 走廊102` 的 `pt00`）。源把这一跳写成 NPC 脚本（`portalNum: 9999`／`scriptPortal: 2052026`／`targetMap: 221030520`），`9999` 是「不是门」的哨兵值；该图 `pt00`（`pt:1`，`tm: 999999999`）**没有被授权**——NPC 站位 x=-1938 与它重合，但进场只走对话。
  2. `221030540.pt00`（`pt:7`，脚本 `pt_221030540`）→ `221030550.west00`。
  3. `221030550.pt00`（脚本 `pt_221030550`）→ `221030551.pt00`（同一张图的另一段，门坐标与 550 逐字相同）。
  4. `221030600.up00`（脚本 `pt_221030600`）→ `221030700.west00`。
     2/3/4 的回程都是**源静态门**（`221030550.west00 → 221030540.pt00`、`221030551.west00 → 221030540.pt00`、`221030700.west00 → 221030600.up00`），所以落点取目标图里那个同名门。
- **客户端目录同步暴露三扇脚本门**（`scripts/tms273_remaster.cjs::exposeScriptedGateRoutes`）：`world.ts::tryPortal` 只受理带 `targetMapId` 的门，不暴露则玩家发不出请求、服务端路由是死代码（上一片同因修复过一次）。赋值前仍断言源门 `targetMapId === null`。呼叫器走 NPC 对话分发，**不需要**目录暴露。
- 落地数据增量：**+10 场地怪**模板（53→63）、**+4 NPC**（250→254：`2052009 小豆`、`2052026 UFO呼叫器`、`2052034`/`2052035 換氣孔`）与 +4 出生位、**+315 刷怪行**（1313→1628）、**+79 物品定义**（3507→3586）；**无新商店、任务目录不变（70 条）**。

## 2. 来源与保护

- **T（台服同版 WZ 实测，2026-09-15）**：23 图的几何/life/portal/returnMap 全部来自客户端 `Map.wz`（`dump_tms273_wz_json.cjs` 落解包树后经 `import_tms273.py` 入库）；4 条 P 级路由的目标来自同版 `Map/Map/Graph.json` 的逐图 `portalNum` 授权；NPC 名称、怪物模板与动作来自同版 `String.wz` / `Mob.wz`。
- **P（登记为有限适配）**：呼叫器「交谈即传送」与三扇 `pt:7` 门「即到即走」。原版这几条由 `pt_22103xxx` / NPC 脚本体把关，本仓库不做等级、任务或时刻校验——与 `ellinel.rs` 时间门、`helios.rs` 电梯、`inERShip` 同一口径。失败路径不静默：死亡角色拒 `dead`，目标图未装配拒 `portal_unavailable`。
- **刻意不接（保持「走近提示一次／此路线尚未开放」）**：
  - `221030550.col00`（`pt:9`，脚本 `col_221030550`）——Graph 里的授权目标是 `999999999`（未授权）；`221030551.pt00` 的授权目标是**它自己**（原地无条件移动），两者都不构成可落地的门，cargo 有反向断言钉住「没被本模块接管」。
  - **操縱杆翼** `221030800/810/820/821/850` 与 `221030801~805/840`、室内图 `221030541/602/621/701/731/890`——Graph 里没有任何图指向它们（入口是原版腳本）。
  - `221030900 主控室` / `221030910 通風口 D-77`——`BossCaoong` 首領房（`info.lvLimit` 180、`fieldScript BossCaoong`），本仓库没有该首領。
  - `22104xxx` 在 TMS273 WZ 中**不存在**（PLAN 旧文里的 `22104xxx` 草原/UFO 街是同一街区的另一种记法）；`221030301/401/501/601` 在 `String/Map.json` 有名字但**没有 `.img`**（源边界）。
- **未触碰**：GM 政策、calamity 既有放置、既有 34 条可执行任务规格（remaster check 55 条逐字段对齐、19 条可执行清单与前一轮**完全一致**）、LOCK-01~08、HOLD-01/02/05。在线 3010 服务、数据库与陪测机器人本轮未动。

## 3. 一处环境限制（不是回归，后续会话请勿误判）

`client && npm run check` 本轮为 **33/35**，两项失败**都与本次改动无关**：

- `scripts/build-release.check.cjs` —— 该脚本在 `assertNoRunningReferences` 里 `spawnSync('ps', ...)` 读 macOS 进程表，本机沙箱直接拒绝该调用（实测 `ps -axo pid=,command=` → `operation not permitted`），脚本按设计抛「无法检查 macOS 进程表，拒绝切换」。**加 `dangerouslyDisableSandbox` 仍失败**（拒绝发生在系统层而非工具沙箱）。该检查只在 `os.tmpdir()` 里用 r1~r4 假发布做轮替，**不读也不写项目内容**，因此与内容无关；上一轮记的 34/35 是本机此前允许 `ps` 时的结果。
- `src/scenes/layer-animation.check.ts` —— `features/windbell/maps` 无扩展名导入（`ERR_MODULE_NOT_FOUND`），HEAD 既有。

## 4. 数据变更

- `shared/maps.json`（165 → 188，+23 图及门/回程表）、`shared/gameplay.json`（+315 刷怪行、+10 怪模板、+4 NPC 与出生位）、`shared/items.json`（+79 定义）、`shared/npc-names.json`、`references/tms273-data/{maps,manifest}.json`（import 产物，manifest 增 `ufoMapIds`）、解包树 +23 图 JSON。
- 内容版本四处同步（`scripts/assemble_tms273.cjs`、`shared/protocol.ts`、`server/src/protocol.rs`、`scripts/check_tms273_runtime.cjs` 默认值）：`tms273-28` → `tms273-29`。**协议 23 未变**，客户端必须与服务端一起换（`启动3010.command` 统一加载）。
- 无数据库迁移；旧角色存档、宠物、装备不受影响（纯新增目录数据）。
- 两处钉数断言随装配面同步：`server/src/boss_acceptance.rs` 地图数 165→188、`server/src/mob_move_acceptance.rs` 刷怪面 52→62；`scripts/check_tms273_runtime.cjs` 的 `WORLD_MAP_ABSENT` 新增 `221030550`/`221030551`（下一步解释）。

## 5. 一处需要读者相信的「源事实」（WorldMap 缺 spot）

`check_tms273_runtime.cjs` 会断言「每张装配图都能在世界地图归档里找到 spot」，而 `221030550`/`221030551` 找不到。已核：源 `Map.wz WorldMap035` 给 UFO 内部**逐图列 spot 时把这两段跳过了**——同一 spot 的 `mapIds` 从 `221030540` 直接跳到 `221030600`，中间没有 550/551（其余 21 张新图都在源 spot 里）。因此按 `221020701 隱藏之塔`、`222020400 時間監控室` 同源先例把两者加入 `WORLD_MAP_ABSENT` 白名单，并在注释里写明归档事实。**这不是掩盖装配缺陷**：23 图全部按源装配，只是源没给这两段世界地图落点。

## 6. 实际执行的检查

| 命令 | 结果 |
| --- | --- |
| `python3 scripts/import_tms273.py` | 188/188 图，0 missing |
| `node scripts/build_tms273.cjs`（全链，约 6 分 43 秒） | `tms273-29` / 188 maps / 69666 assets / 358 npcs / 63 monsters |
| `node scripts/check_tms273_runtime.cjs` | 188 maps / **107999** 源引用一致；`WORLD_MAP_ABSENT` +2 |
| `node scripts/check_tms273_remaster.cjs` | 55 条逐字段对齐、**19 条可执行清单与上一轮零 diff** |
| `cd client && npx tsc --noEmit` | 0 错误 |
| `cd client && npm run check` | **33/35**（2 项＝§3 说明的环境限制与 HEAD 既有） |
| `cargo test --manifest-path server/Cargo.toml` | **439 过 / 0 失败**（= 基线 430 + 新增 9 条 ufo 验收） |
| 子代理独立验收 | **ACCEPT**（见 §8） |

## 7. 未验证

- **未实玩**：在线 3010 仍是旧二进制，本轮未重启；UFO 街步行观感、呼叫器进场、三扇脚本门与走廊105 两段的手感需用户统一加载后核看。
- **未核看画面**：23 张新图渲染均来自源 WZ 导出并通过引用一致性检查，但没有同版截图比对。
- **未验证**异常分支的实机表现：死亡角色点呼叫器、目标图未装配时的拒回文案（cargo 有断言，无实机观感）。

## 8. 子代理独立验收

**ACCEPT**（只读验收；2026-09-15）。子代理独立复核后给出：

- **A 数据忠实**：23 图 80 扇门与解包树源 JSON 逐字段比对（name/type/x/y/`tm`/`tn`）——**0 门缺失、0 门多余、0 个 x/y 差异**；`221030550.col00` 与 `221030551.pt00` 在目录里 `targetMapId = null`（未开放）。
- **B 路由正确性**：`Graph.json` 逐条核对与 `ufo.rs` 完全一致；`handle_ufo_pager` 两条失败路径都真拒回；`dialogue.rs` 分发在仓库管理员**之前** `return`、`portals.rs` 钩子在查表**之前** `return`；`exposeScriptedGateRoutes` **只**暴露服务端确实处置的三扇门（赋值前断言源门无目标），未误暴露 `col00`/`221030551.pt00`。
- **C 装配面与计数**：`spawns` 去重模板 = 62、新增正是 `4230127~4230134`/`4230141`/`4230142` 十个；`shared/maps.json` = 188；`worldmap.json` 仅 550/551 无 spot、源 `WorldMap035` 确从 540 跳到 600（§5 结论成立）；刻意不接的图均不在目录。
- **D 亲测复现**：runtime 188/107999、remaster 55/19、tsc 0、cargo 全绿、`npm run check` 两项失败判定为环境/HEAD 既有（与本模块无关）。
- **E 边界干净**：内容版本四处一致 `tms273-29`；`gm.rs`、`calamity`、历史文档、`AGENTS.md`/`MEMORY.md` 均未触碰。
- **观察（非阻断）**：提交时工作树上有**另一并行会话**的「冒险笔记（图鉴）」在建改动（`server/src/notebook.rs`、`protocol.rs`/`protocol.ts` 的图鉴协议与 `PROTOCOL_VERSION` 24、`scripts/build_tms273.cjs`、`client/src/app/i18n.ts` 等）。本模块提交**只含自己的文件**，那批在建改动一律留在工作树未提交（详见 §9）。

## 9. 提交方式说明（并行会话）

本轮验收期间发现工作树被另一会话并发写入（「冒險筆記圖鑑／怪物收集」在建）。为避免把未完成的模块commit 进去（其 `server/src/notebook.rs` 等仍是未跟踪文件，若随本模块提交会让 `HEAD` 无法编译），本模块**只暂存自己的文件**：

- 完全属于本模块的 16 个文件正常 `git add`；
- 4 个与并行会话共享的文件（`scripts/assemble_tms273.cjs`、`server/src/world.rs`、`server/src/protocol.rs`、`shared/protocol.ts`）用 git 底层 `hash-object` + `update-index --cacheinfo` **只暂存本模块那几行**（内容版本 28→29、`mod ufo;`），并行会话的图鉴协议与 `PROTOCOL_VERSION` 24 留在工作树；
- 提交后校验：`git diff --cached` 内**不含任何 notebook 痕迹**，`HEAD` 上协议 23 / 内容 `tms273-29` 两端一致。

`artifacts/refactor/frontend-deps.json`（`npm run check` 再生的依赖审计生成物）与 `scripts/__pycache__/*.pyc` 的删除同样**未纳入**本次提交。

## 10. 用户实玩入口

经 `启动3010.command` 统一加载（内容 `tms273-29` 前后端必须一起换）后：

- 从**地球防衛本部**（大地图直达，或上一片链路走进来）走 `west00` 出城 → `221030000 危險地帶` → 一路 `west00` 进草原Ⅰ→Ⅱ→Ⅲ→**Ⅳ**。
- 在草原Ⅳ 找 **`UFO呼叫器`** 说话 → 应直接进 **`221030520 走廊102`**（落点在源舱门 `pt00`）。
- 走廊内按原路走：`走廊102 east00` → `走廊103` → `up00` 上 `走廊204` → 一路回到 `走廊104` → `pt00` 进 `走廊105`（第一段）→ `pt00` 进 `走廊105`（第二段）；`走道201 up00` → `通風口D-1` 并沿 `east00` 走到 `D-4`。
- 反向走一遍（每段都有源静态门回程），确认不会卡死；`走廊105` 的 `col00` 走近应仍是「此路线尚未开放」。
- 重点核看：三扇 `pt:7` 门的落点是否贴地、走廊105 两段的切换是否顺、草原Ⅳ 呼叫器交互是否即走即传；以及在本部/草原/UFO 内部任意位置死亡后回到本部 `sp`。

## 11. 后续阻塞（同步 PLAN，不另立台账）

- `221030550.col00`（源授权 999999999）与操縱杆翼、`BossCaoong` 首領房仍是源边界，需先核定对应脚本体／首領实现才能接。
- 原版 UFO 内部还有 `221030541/602/621/701/731` 等「门内过渡房」（原版腳本驱动），未装配，走近保持既有提示。
- 再下一片 T06/交通候选：飞行船三期（航行中 Balrog 事件、候補室 `station_in` 脚本门、票务道具接入）与其余按审计 §11 顺序取用的区域；T04 剩余交互项与 T05 的 36317（`infoex` 语义）仍是各自的真机/核定前置。
