# 射手村三维场景与主世界实装

2026-09-24，隔离工作树 `b989`。用户接受当前场景进入实装，要求登录默认进入该场景，活动列表可以回到原版 2D；复用巨石之约的 Three.js/GLB 技术和现有 TMS273 系统。Astra 完成美术及主世界接线，Luna / max 完成源资源核查、登录落点实现及生命周期只读检查。

## 交付

- 完整可编辑 Blender 场景：`resources/scenes/henesys/models/henesys.blend`；GLB：同目录 `henesys.glb`（32,681,280 字节、8 个区块、内嵌贴图）。真实渲染：`resources/scenes/henesys/previews/overview-refined.png`、`east-draft.png`。源地图坐标与参考图位于 `models/source-layout.json`、`references/`。
- 制作入口：`scripts/creative/blender/prepare_henesys.py` → `build_henesys.py` → `refine_henesys.py` → `export_henesys.py`。后三个在 Blender Python 控制台运行；准备脚本的 `--source` 指向有 TMS273 导出资源的项目。原生文件与导出资源按仓库约定留在忽略目录。
- `World` 默认给射手村 `100000000` 加载三维环境，原版角色、NPC、任务标记、怪物、掉落、技能、宠物与交互继续由同一个 Phaser World 和现有服务端管理。UI 沿用当前 TMS273 DOM 窗口。Three.js 读取透明游戏前景，不复制战斗/任务状态机。
- 活动列表新增射手村卡片，提供“返回原版 2D”“启用三维射手村”和恢复镜头。切换不发地图迁移或活动退出指令。其他地图及巨石之约期间禁用此显示开关；正常地图和活动仍走各自原有链路。
- 新鲜登录优先射手村，合法射手村坐标保留，其他地图存档角色使用射手村出生点。已驻留角色的连接接管保留实时位置。无射手村的旧测试目录保留原回图逻辑。角色等级、物品、任务和死亡记录不重置。
- 内容版本 `tms273-44 → tms273-45`，协议 34 不变；五处手写版本及装配产物同步。装配器复制 GLB 与活动封面，清理对应压缩旧副本。现有地图、NPC/怪物摆位和任务数据未另造一份。
- 复查修复：断线时重建静态地图；清理上一地图的待展示快照/表情；重启快照经 Phaser `init` 安装，避免 shutdown 清掉新图快照；NPC/任务/反应器/墓碑复用统一窗口守卫；投影点击抑制重复兼容鼠标事件。

## 已执行验证

- `npm run build --prefix client`：通过（现有大块体积警告仍在）。
- `node scripts/assemble_tms273.cjs`：通过，211 图、120754 个 TMS273 资源，1754 个现金外观索引；本轮派生 JSON 仅版本字段改变。
- `node scripts/check_henesys_assets.cjs`：通过，8 个区块、内嵌纹理、服务资源逐字节一致；426 条源 foothold、梯子和门户与服务端一致。
- `cargo test --manifest-path server/Cargo.toml fresh_join_defaults_to_henesys_without_migrating_resident -- --nocapture`：1 过 / 0 失败，覆盖新鲜落点、背包/任务/成长/死亡记录保留、射手村坐标、resident 接管、旧目录回退。
- 原有 NPC 视图、战斗技能、任务窗口三个定向检查通过。
- `scripts/check_henesys_browser.cjs`：实际 Chromium + World 通过。覆盖原版纸娃娃、NPC 点击、怪物显示、3D/2D/3D 切换、镜头拖动、1024×768 尺寸、断线重建背景/门户、待处理表情与快照清空、单个快照驱动离图/返图、非射手村及巨石期间禁用开关，无 JS 异常。另实测投影 NPC 点击只触发一次且鼠标正常松开。
- 浏览器证据：`artifacts/henesys/login-3d.png`、`npc-monster-3d.png`、`original-2d.png`、`center-3d.png`。
- 隔离预览：启动 `npm run dev --prefix client -- --port 5187` 后访问 `/henesys-preview.html`。明确标记模拟快照，无后端连接、无存档写入。测试可用 `PLAYWRIGHT_MODULE` / `PLAYWRIGHT_EXECUTABLE` 指定已有本地依赖，无需新增生产依赖。

## 边界

首版为三维环境 + 原版二维前景，运动/跳跃/梯子仍使用原版 XY 与服务端碰撞，镜头旋转受限；不是自由深度移动。仅射手村有新三维场景，其余地图显示原版 2D。射手村原生无普通野怪刷新，本轮没有往城镇编造刷怪表；浏览器中的怪物是明确的显示测试快照，实际刷怪/掉落/任务进度仍由服务端原有规则决定。未新增未复刻的 TMS273 系统。

浏览器使用 SwiftShader 软件渲染，不据此宣称真实 GPU 帧率验收通过。完整登录后的在线任务接取/交付、战斗实玩及最终画面反馈仍待发布窗口验收。没有部署、合并、重启在线服务或修改主工作区/用户数据库/陪测机器人。
