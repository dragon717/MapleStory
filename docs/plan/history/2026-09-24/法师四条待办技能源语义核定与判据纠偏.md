# 法师四条待办技能 · 源语义核定与判据纠偏（2026-09-24）

> 用户指令：「继续」。
> **选取依据**：`PLAN.md` §1①（统一加载实玩验收）只有用户本人在本机能做；
> §1③ 的剩余项都需用户侧凭据或真实域名；§1② 缺口 C 剩余的**四条**经本轮取证确认
> **都要先立新机制**（不是口径内能直接接的一条）。
> 故本轮取**「把理由从散文变成判据」**这一段：它是缺口 A／C 的尾巴，
> **不新增机制、不动运行期行为**，却直接针对本项目最有价值的教训——
> **理由与源不符会把人锁在错误的方向上**（`1221009` 那一类）。

## 0. 版本与产物

- **零 `shared/**` 字节** ⇒ **不升内容版本**（仍 `tms273-45`／协议 `34`）、
  **不重跑装配器**、不重启在线服务、不重新发布。
- 改动仅 2 个源码文件（1 个注释、0 行为）+ 2 个门禁脚本 + 3 份计划文档。
- **未提交**（工作区 4 个业务文件待提交）。

## 1. 本轮真正做的事：把「四条待办」从散文变成可从源独立重算的判据

### 1.1 先取证：四条技能的**权威源文案**（这是本轮全部判断的依据）

源＝`references/tms273-data/fire-fourth-job-source.json`（212 火毒）与
`holy-fourth-job-source.json`（232 主教）的 `description` / `perLevelDescription`。

| 技能 | 源 `description` / `perLevelDescription` 关键句 | 真实机制缺口 |
|---|---|---|
| `2121054 火靈結界` | 「…使用技能時啟動效果，再次使用時則關閉效果的**開關技能**」／「**每秒消耗MP** #mpCon，每 #x 秒（＝`subTime` 3000 ms）向最多 #mobCount 名敵人以 #damage% 的傷害，攻擊 #attackCount 次」＋`dotTime` 秒 DoT | **開關态**（持久开关位 + 每秒扣 MP 结算点）。DoT 与范围攻击**已有** |
| `2321054 復仇天使` | 「取得天使的純粹的憤怒…**#c慈愛#技能轉變成#c復仇#技能**」／「群體治癒、淨化、神聖之泉、神聖之水各別轉換成天使之觸、勝利之羽、天使之泉、神聖之血…**#c[被動效果]#** 魔法攻擊力增加 #madX、最終傷害增加 #mdR%、無視怪物防禦率增加 #ignoreMobpdpR%、攻擊屬性耐性減少 #u%」 | **技能轉換**（被转换的四本复仇副本本包也没有施法分支） |
| `2321006 復甦之光` | 「用神聖的光芒**讓隊員復活**」，`action` 是 `resurrectionNew`／「復活在範圍內死亡的所有隊員後，獲得 #time 秒**無敵**狀態…」 | **队伍复活**（`revive.rs` 只处理玩家自己的死亡与回城） |
| `2321015 神聖之水` | 「將在周圍召喚盛滿聖水的**聖杯**。隊員對聖杯按下「上」**方向鍵**時，可吸收聖水並恢復HP」 | **可累积场景实体 + 方向键交互** |

### 1.2 纠偏：两处与源不符的理由（都在门禁与源码注释里）

| 位置 | 旧理由（错） | 源里的事实 |
|---|---|---|
| `server/src/world.rs`（進階祝福决策块） | 「開關技能那批——**復仇天使 `2321054`**／火靈結界 `2121054`——的源 `common` 里没有 `time`」 | 源文案里写「**開關技能**」的只有 `2121054`；`2321054` 写的是「**慈愛**技能轉變成**復仇**技能」＋`[被動效果]`，**不是**開關 |
| `scripts/check_tms273_attributes.cjs`（`madX` 的 `exceptReasons`） | 「復仇天使…它的 `madX`=50 是「**施放窗口内的临时魔攻**」，语义上属于 ActiveBuff 层…**无法构成增益窗**」 | 源把 `madX` 与 `mdR`／`ignoreMobpdpR`／攻擊屬性耐性一起写在 **`#c[被動效果]#`** 一组里——**不是**窗口值 |
| `scripts/check_tms273_damage_pipeline.cjs`（`NOT_CONSUMED.mdR`） | 分组注释写「`indieDamR` / `mdR` 是**窗口内**的值」；该条自身理由只有四个字「復仇天使」 | 同上 |

三处**都改成源派生理由**，并显式写明**未定项不推断**：源里既没给 `[被動效果]` 的期限，
也没写清它与那次「慈愛→復仇」轉換（`mpCon`＋`cooltimeMS`）的前后关系 ⇒
**既不按窗口、也不按学得即生效的被动处理，登记为不消费**。
这是本项目的老纪律：**不替源编语义**（`2111003 致命毒霧` 的 `time` 判据同理）。

### 1.3 立判据：`MAGE_BRANCH_PENDING`（`check_tms273_damage_pipeline.cjs`）

```
① 集合从源独立重算（不拿本表当输入）：
   212／232 书里「带 mpCon（＝可施放）、非 hidden、world.rs 里没有常量」
   ⇒ 恰好 = {2121054, 2321006, 2321015, 2321054}
② 理由钉在源文案上：每条登记 keywords[]，必须真的出现在权威源文件
   的 description／perLevelDescription 里（改理由≠改文案）
③ 每条 constFor(id) === null（起了常量＝接了路径 ⇒ 当场红）
④ 附带：212／232 里「带 mpCon 且 hidden」⇒ 恰好 ['2321016']，
   它就是復仇天使那次轉換的产物（神聖之水→神聖之血），玩家点不到
```

**为什么用 `mpCon` 做判据**：不带 `mpCon` 的那一批（20 本 Hyper 强化被动 +
`神秘狙擊`／`元素強化`／`祝福旋律`）**不是**「可施放但没有分支」，而是被动/强化，
由 `NOT_CONSUMED.damR` 与 `attribute.rs` 的被动槽位各自记账。
实测这 26 条未起常量的 212/232 技能里，**只有这 4 条带 `mpCon` 且非 `hidden`**。

### 1.4 顺带核实的两条源事实

- `subTime` **又一次是「一字段多语义」**：`2121054` 的 `subTime=3000` 是**毫秒**
  （源文案「每 #x 秒」且 `x=3`）；`2321006` 的 `subTime=30` 是**秒**（源文案「#subTime秒內」）。
  门禁 `PHYSICAL_MECHANISM_PAIRED.subTime` 早已登记「孤立的 `subTime`（`3111013 箭座` 的召唤周期、
  `4221052 暗影霧殺` 的独立子窗口）是另一类语义」，本轮把 `2121054` 这条也归入**场/开关的周期**，
  单位靠量级认（与 `summon_lifetime_ms` 的 `>= 1000 ⇒ 毫秒` 同一条口径）。
  **未改该判据**（改它要动物理线范围表，不在本轮口径内）。
- `2321016 神聖之血` 是 `2321054 復仇天使` 轉換出来的 `2321015 神聖之水` 的复仇副本，
  源里 `hidden`、带 `mpCon` ⇒ 由职业规则自动启用，不进上表。

## 2. 验证（含扰动）

| 项 | 结果 |
|---|---|
| `check_tms273_damage_pipeline.cjs` | **PASS**，新增汇总行：`法师 212／232 未接执行链（带 mpCon、非 hidden，理由钉在源文案上）=2121054/2321006/2321015/2321054；其 hidden 可施放副本 =2321016` |
| `check_tms273_attributes.cjs` | PASS（只改理由文案） |
| `cargo test --bins` | **668 过 / 4 失败**，与基线**逐项相同**（`boss_practice_*` ×3 + `tms273_chapter_first_six_acceptance`，均由登录落点 `100000000` 引起，见台账 §3；本轮只改了一条 doc 注释） |
| `run-checks.mjs` | **65 项 / 57 过 / 8 红**，8 红与既有基线**逐项相同**（`layer-animation.check.ts`、`gauge.check.ts`、`refactor_audit --deps --check`、`check_repository_layout.cjs`、`check_inventory_surface.cjs`、`check_tms273_player_status.cjs`、`check_tms273_desktop_package.cjs`、`build-release.check.cjs`）；**我改的 `check_tms273_damage_pipeline.cjs` 与 `check_tms273_attributes.cjs` 均 PASS** |

**扰动验证（4 组，全部如实变红，恢复后复绿）**：

| 扰动 | 触发 | 断言原文（截断） |
|---|---|---|
| ① 把 `2121054` 的锚点词 `開關技能` 改成源里不存在的简体写法 | exit 1 | `2121054 的理由引用了权威源文案里不存在的词 \`开关技能\` —— 理由与源文案分叉了` |
| ② 给 `2121054` 起一个常量（假装已接线） | exit 1 | 集合判据 + `constFor` 反向断言同时触发 |
| ③ 删掉 `2321015` 的登记条目 | exit 1 | `…技能集合变了：少一条＝…多一条＝…`，`+ '2321015'` |
| ④ 删掉 `NOT_CONSUMED.mdR` 的 `2321054` | exit 1 | 消费集合判据触发 |

## 3. 边界（本轮**没有**做的事，别误读）

- **一条技能都没有接线**：四条仍「尚未开放施放」，行为与改前**逐字节相同**。
  本轮只改「理由」与「判据」，不改执行链、不动范围表、不动属性聚合。
- **没有替源编语义**：`2321054` 的 `[被動效果]` 是否随那次轉換生效，源里读不出来 ⇒
  登记为「不消费 + 不推断」，而不是硬塞进某一层。
- **没有改 `subTime` / `prop` 的物理线判据**（那要动 `PHYSICAL_MECHANISM_PAIRED`，属另一轮）。
- **没有动 `history/2026-09-21` 与 `2026-09-23` 的旧记录**：它们是当时的交付快照，
  按惯例不改写历史；更正写在本文与 PLAN／台账里。

## 4. 待办与下一步

四条仍须**逐条立新机制**，按「新机制的可复用度」排序，供你点选：

1. **`2121054 火靈結界` → 開關态**：机制最通用（開關技能是一整族），且
   DoT／范围攻击／`subTime` 周期**都已有**，新增面最小（持久开关位 + 每秒扣 MP）。
2. **`2321054 復仇天使` → 技能轉換**：需要「同一格技能的复仇副本」这套映射，
   而被转换的四本在本包同样没接线 ⇒ 单接它只能落地「轉換表」，玩家侧看不出变化。
3. **`2321006 復甦之光` → 队伍复活**：要新开「复活他人」入口（`revive.rs` 目前只管自己）。
4. **`2321015 神聖之水` → 计数器 + 可交互实体 + 上键拾取**：三件都要新做。
