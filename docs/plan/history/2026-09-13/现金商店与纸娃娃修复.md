# 现金商店与纸娃娃修复（2026-09-13）

用户要求按 TMS273 现金商店复刻，商品是扩展券、宠物、时装等现金道具；修复缺图和实际装备/时装纸娃娃绑定。主代理统筹，三个 GPT-5.6 Luna / max 子代理分别负责商品目录、商城界面和纸娃娃；保留在线服务、账号与存档。

## 来源与修改

- 本地 TMS273.7 `UI/CashShop.img/Base/backgrnd`（1024×768）与 `CSTab/Tab/0`（126×370）已查看。使用深色商品区、左侧十行分类、右侧角色预览；较小窗口重新排列以保留操作空间。背景直接使用完整源底图，不再叠加另一套顶部条。
- 联网核对 [V273 改版报道](https://www.4gamers.com.tw/saged/detail/056640944v7wkv)。研究包 PDF 为图源索引；A07/A11 远图无法打开，未将其当作已查看的界面，未宣称原版全部功能或 1:1 完成。
- 1491 条源商品核为 1100 条可售、391 条明确排除，1002 个道具图标和定义。排除 363 条当前不支持的宠物/骑乘装备、26 条源缺图、2 条未实现仓库服务。12 条宠物商品恢复，8 条扩展服务 SKU 映射到源扩展券 2430768–2430771（交付映射为 P 规则，保留源价格、SN 与 sourceItemId）。
- 修正物品族分段、Eqp 名称节点、Pet 图片路径、7/8 位 ID 别名；商品图标及定义进入总装配和总导出链。宠物使用去前导零的目录键，真实现金武器 `01702087` 使用源 `Wp` 槽位。
- 商城商品详情、搜索、分类和购物车保留；失败回执保留购物车，成功后才移除。图片失败移除损坏的 img，避免显示浏览器缺图和错误文字。
- 纸娃娃导出按装备 ID 查描述，替换易错数组下标；同 ID 的 7/8 位拼写统一。466 个现金外观按需加载独立 JSON 和 Phaser PNG 纹理，复用源动作与锚点；武器按实际武器族选对应分支。商城修正 WZ 图层顺序，避免身体遮住头发和服装。

## 验证与待验

开发与定向验证已完成，统一加载后的账号实玩待用户。

- 源目录与装配检查通过；装配内容 `tms273-18`，包含并发地图任务的 71 张地图。
- Rust `cargo test --manifest-path server/Cargo.toml cash_ -- --nocapture`：8 通过，含真实武器槽位、现金宠物与重开数据库恢复。
- 开发者离线浏览器使用真实静态资源与模拟回执，不访问在线账号。截图、输出及后端日志保存在 `evidence/2026-09-13/现金商店与纸娃娃修复/`。
- `appearance.check.mjs`：88 组性别/发色组合、卸装、7/8 位现金 ID、武器分支、移动/技能锚点与缺失动作不回退站姿通过；`player/view.check.mjs` 通过。
- 真实离线 Phaser 验证：先在商城缓存现金套装 JSON，再首次装备到 PlayerView，站立与 `skill2201008` 均包含源时装并无 `__MISSING` 纹理。异步完成有换装/销毁检查，纹理准备好后才参与组合。
- 四种浏览器尺寸、现金套装首次试穿、结算失败保留购物车/成功移除、损坏图像清理通过；页面脚本异常为 0。
- `check_tms273_runtime.cjs` 通过：71 张地图、73769 个源引用。集成时补齐并发世界地图消息的 TS 返回类型及源新增 `2030003` 回城卷的定向检查值；未改变其业务。
- `build-release.cjs prepare` 通过，候选 `20260913124727-96fb0d1b`，协议 **19** / 内容 **tms273-18**（合并工作区并发任务的最新版本）。Vite 有大 bundle 提示，不影响构建。小屏余额与推荐商品收尾后，客户端已再次构建并纳入同一候选。
- 未重启在线服务；统一加载及实玩由用户经 `启动3010.command` 完成。

## 边界

- 仅售当前有道具定义和可用图片的现金商品；未实现的仓库服务、宠物/骑乘装备保持排除，不能伪造玩家装备槽位来放行。
- 两个仅有弩分支的现金武器没有当前可用武器分支，外观索引记录跳过；没有源动作的武器分支不拿站立帧冒充技能姿势。
- 界面规则与源外观已做开发者定向检查；正常账号的购买、使用扩展券、穿脱、重登与跨图体验仍待用户实玩，不声明完整官方现金仓库/赠送/里程系统。
