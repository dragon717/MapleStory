# 风铃二维素材还原与目录整理

日期：2026-09-13。当前用户反馈覆盖此前分面 3D 方向。主代理负责概念核看、2D 场景接入与定向检查，Luna / max 分别完成 TMS273 Canvas 调查导出与目录迁移。未启动独立 QA、未切换在线服务、未修改数据库或地图物理几何。

## 交付

- 风铃场景使用 Phaser 原生 2D 分层；取消本场景 GLB 加载、Three 相机与每帧演员画布上传。远景复用已有独立浮岛/峡谷背景；巨树与浮岛地形依据原概念通过内置 image_gen 生成透明模块，保留原概念及生成回执。左右巨树、上方树桥、驿站与下方根道按原构图方向组装。
- 现成 `prop-leafwing.png`、驿站、巡风龙、货车、材料和风铃直接复用。桥用 TMS273 木板和绳索沿服务端 foothold 拼接；保留落桥、逐段修桥、推车、燃烧、滑翔及状态变化音效。
- 素材唯一分类：`resources/scenes/windbell`、`characters/windbell`、`ui/windbell`、`music/windbell`、`sfx/windbell`；3D 文件与其贴图/建模证据留在 `resources/blender/windbell`。旧 `resources/creative/windbell` 与兼容软链已移除；制作脚本、素材清单和文档同步迁移。
- 用户的“上述素材先 TMS273、找不到再联网；确需 3D 时 Astra / medium、白模后贴图”已写入项目 `MEMORY.md`、长期规范与全局记忆。

## 同版来源

`resources/tms273-export/windbell-2d.json` 和运行目录 `assets/windbell/tms273.json` 保存帧来源、resolvedSource、尺寸、origin、delay。独立导出脚本为 `scripts/creative/prepare_windbell_tms.cjs`。

| 类别 | 采用来源 |
| --- | --- |
| 明火 | `Map/Obj/acc1.img/mapleIsland/burningHouse4/2/0..5`，6 帧，每帧 100 ms |
| 树枝 / 木柴 | 已导出物品 4000003 / 4000018，`Item/Etc/0400.img/.../info/icon` |
| 云 | `Map/Obj/2025DimensionTower.img/2023ForestBlast/back/cloud1/0` 为运行首选；其他 Canvas 变体保留 |
| 桥板 | `Map/Tile/woodBridge.img/enH0/0` |
| 草土 | `Map/Tile/grassySoil.img/enH0/0` |
| 岩石 | `Map/Obj/acc1.img/dryRock/nature/18/0` |
| 绳索 | `Map/Obj/connect.img/rope/0/3/0` |

实际目视排除了暗棕烟雾候选 `PL_Abrup_Camp/ani/1` 和误名为枝条的草丛 `darkWood/nature/16`；它们不被最终 catalog 引用。PL_Abrup_Fire 的 Spine atlas 未冒充原版逐帧动画。明火保留原始每帧 origin，整体使用 (-50,+50) 的场景摆位偏移，将原房屋火层对准火槽。全部必要类别已在 TMS273 找到，本轮未使用网上跨版素材。

## 验证与边界

- `client/src/features/windbell/runtime.check.mjs`：实际 preload/构造/update/destroy 路径自检通过；检查文件、原帧 delay/origin、无重复初始音效、放桥终点、修桥/推车、展翼着地隐藏及离图销毁。
- `scripts/refactor_audit.cjs --deps --check`、`scripts/check_repository_layout.cjs`、导出脚本语法检查通过。
- 目录迁移代理确认资源引用完整，runtime 2D/音频复制成功；新增巨树与浮岛地形均为真实 RGBA，alpha 0..255。
- 前后端构建通过，候选 `20260912190159-835d260e`；候选 27 张必要图像与素材源逐字节相同（`artifacts/windbell/2d-candidate-assets.json`）。构建结果见 `artifacts/windbell/2d-build.log`，候选内容版本 `tms273-12`。首次命令因 shell 未含 cargo 路径退出，随后使用现有 `/Users/muniao/.cargo/bin/cargo` 重试；未安装工具或改变宿主配置。
- 不宣称 1:1 完成：现有根道物理几何仍为斜坡，原概念为石阶。实际画面比例、遮挡、流畅性及宽屏/横屏/窄屏实玩交用户核看；待验入口统一见当前计划。
