# 主线后续章节复刻（36314 之后）

状态：数据与判定链已落地，待统一加载 3010 后实玩。日期：2026-09-13。

## 1. 本次范围

以 **TMS273.7 `Quest.wz/QuestData`** 为唯一基准，把此前只登记了显示文本、没有运行时规格的 **55 条任务** 全部复刻进运行时数据契约，并逐条给出可执行性结论。

| 集合 | 数量 | 内容 |
| --- | --- | --- |
| 冒险家后续章节 | 20 | 36315–36334（lvmin 20→200 的章节链） |
| 艾靈森林章节 | 27 | 36341–36367（area 39，lvmin 95） |
| 其他职业一转支线 | 6 | 1401 / 1403 / 1404 / 1405 / 2570 / 2684 |
| Remaster 内部任务 | 2 | 36335 / 36336（NPC 楓之谷GM 9010000） |

**本次不做**：不新增地图与 NPC 美术，不实现脚本 VM，不发明脚本执行结果，不把 QuestInfo 显示文本当成完成条件，不给未证实的奖励数值。已装配的 15 条（开场 6 + 续章 9）一条不改。

## 2. 输入 / 输出

输入：

- `references/tms273-data/quests.json` —— 70 条任务的 `Quest.wz/QuestData` 原始导出，含 `Check` / `Act` / `Say` / `QuestInfo`。
- `shared/maps.json`（44 图装配目录）与 `shared/gameplay.json` 的 `npcs` / `npcSpawns` —— 判断 NPC 是否在场。
- 规则依据：`server/src/quest_rules.rs`、`server/src/world.rs::QuestSpec/QuestPhase/QuestConditions`。

输出：

| 文件 | 变化 |
| --- | --- |
| `scripts/tms273_remaster.cjs` | 新增适配器 `applyRemaster`：按源生成规格 + 分类 |
| `scripts/assemble_tms273.cjs` | 在 `applyChapter` 之后接线，并断言 55 条 |
| `scripts/tms273_chapter.cjs` | 导出 `display` / `decode`，供新适配器复用（避免清洗规则分叉） |
| `shared/gameplay.json` | 55 条任务补上完整规格；`compatibility.adventurerRemaster` 新增 |
| `shared/quest-text.json` | 55 条任务日志文本 |
| `scripts/check_tms273_remaster.cjs` | 新增不变式检查（逐字段对齐源） |
| `scripts/check_tms273_runtime.cjs` | 可执行任务数 15 → 17，并钉住后续章节分类 |
| `client/scripts/run-checks.mjs` | 新检查挂进 `npm run check` |

## 3. T 字段映射（源 → 运行时）

| TMS273 源 | 运行时字段 | 性质 |
| --- | --- | --- |
| `Check.0.lvmin` | `start/complete.conditions.levelAtLeast` | T |
| `Check.0.job[]` | `sourceJob`（原样保留）+ `conditions.job`（映射） | T / P 映射 |
| `Check.0.subJobFlags` | `sourceSubJobFlags` | T |
| `Check.0.quest[]`（id/state） | `conditions.quests`（state 1→active，2→completed） | T |
| `Check.0.QuestOrOption == 1` | `conditions.questOrOption`（仅在该阶段真有前置时带） | T |
| `Check.0.npc` / `Check.1.npc` | `start.npcId` / `complete.npcId` | T |
| `Check.1.item[]` | `complete.conditions.items` + `consumeItems` + `objectives` | T |
| `Check.1.mob[]` | `sourceMobRequirements`（只登记，不判定） | T（判定未实现） |
| `Check.1.infoNumber` / `infoex` | `sourceInfoNumber` / `sourceInfoex` | T（脚本计数器，未实现） |
| `Check.0.fieldEnter` | `sourceFieldEnter`（目标图不在装配目录时记 `missing-map`） | T |
| `startscript` / `endscript` | `sourceScripts` | T（脚本体缺失） |
| `QuestInfo.0/1/2` | `summaries.available/active/completed` | T |
| `Act` | —— | **70 条全部为空**，奖励非原作事实 |

补充规则：

- **职业**：源 job 是 TMS 职业码（100/200/300/400/500 一转大类，x1x/x2x 二转三转）。运行时只认识 0/200/220/221/222，因此只把法師系映射过去；其他职业的转职支线保留源数据但标 `other-job-route`，不借机开放其他职业玩法。
- **奖励**：源 `Act` 为空，沿用项目 P 分档（等级 ×30），转职支线与内部任务为 0，不发金币不发物品；逐条标注 `rewardSource`。

## 4. 可执行性分类结果

可执行 **2 条**：`36316`（赫麗娜 1012100，lv20，前置 36315）、`36332`（德斯 1541009，lv100，前置 36331）。两者都是源中「起止 NPC 明确 + 完成阶段只有 `order`」的任务。

其余 53 条按源机制登记阻塞原因（一条可命中多项）：

| 原因 | 条数 | 说明 |
| --- | --- | --- |
| `missing-start-npc` | 48 | 源未写 `Check.0.npc`，靠 `startscript` 自动/场景开场 |
| `missing-complete-npc` | 46 | 同上，完成也没有 NPC |
| `missing-region` | 35 | 起止 NPC 模板都不在装配世界（艾靈森林 27 条 + 部分后续章节） |
| `job-route-unmapped` | 30 | 源职业码无法映射到项目法师路线 |
| `script-counter` | 13 | `Check.1` 带 `infoNumber`/`infoex`，完成由脚本计数器判定 |
| `other-job-route` | 6 | 其他职业转职支线 |
| `script-item-source` | 6 | 完成需要脚本发放的道具 |
| `mob-progress` | 4 | `Check.1.mob` 击杀目标，运行时尚无击杀进度 |
| `missing-map` | 3 | `fieldEnter` 目标图不在 44 图装配目录 |
| `dev-quest` | 2 | Remaster 内部物件控制任务 |

## 5. 验证

- `cargo test --offline`：**317 过 / 6 失败**。用 `git stash` 复跑 HEAD 基线同为 **317 过 / 6 失败**，失败集逐名一致（auth third_store、mage bundled_catalog、world config_drop / portal_command / reactor_area / third_sphere），无新增失败。
- `node scripts/check_tms273_remaster.cjs`：55 条逐字段对齐源全过。
- `node scripts/check_tms273_runtime.cjs`：44 maps / 50662 引用一致，可执行数 17。
- `npm run typecheck`：0 错误。
- `npm run check`：**29/31**，新增项 PASS；两项失败与本次改动无关且在工作树 HEAD 上同样存在——`layer-animation.check.ts` 报 `manifest.ts` 的 `features/windbell/maps` 无扩展名导入无法解析（HEAD 既有），`build-release.check.cjs` 因沙箱内无法检查 macOS 进程表而拒绝（环境限制，与 3010 在线实例相关）。

## 6. 未验证与下一步

- 未重启、未重新构建客户端：本次只改内容与脚本，加载需经 `启动3010.command` 统一入口。
- 36316 / 36332 虽然可执行，但前置（36315 / 36331）仍被脚本计数器与缺失 NPC 阻塞，正常玩家路径暂不可达；本次只保证数据自洽，不伪造入口。
- 解锁后续章节需要：① 恢复 q36315/q36331 等脚本入口或给出显式的 P 承接 NPC；② 补齐艾靈森林（area 39）地图与 2131xxx/2133xxx NPC；③ 实现任务击杀进度后 `mob-progress` 的 4 条才能判定。
