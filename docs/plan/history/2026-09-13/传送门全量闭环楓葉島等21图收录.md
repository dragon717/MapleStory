# 传送门全量闭环：楓葉島等 21 图收录装配（50 → 71 图）

## 用户反馈

- `此路线尚未开放：目标地图 100000001 尚未收录`（楓葉島方向）。
- 需求：**复刻所有地图链接**（凡 TMS273 源里存在的目标图全部装配），以及**世界地图的地图跳转**。

## 根因与现状

- 上一轮（见同日「六条岔道树顶传送阵」文档）后，目录停在 50 图。portal 闭环的 21 张目标图（楓葉島 100000001/2/3、100000100/200，弓箭手村内景、墮落城市西线、蘑菇村东路、幸福村车站、魔法森林Library 等）已由 `import_tms273.py::ADDITIONAL_PORTAL_CLOSURE_MAP_IDS`（2026-09-13 新增）导入到 `references/tms273-data/maps.json`（**71 图**，missing 为空），版本号四处已同步 `tms273-18`，`check_tms273_runtime.cjs` 与 `boss_acceptance.rs` 已预设 71。
- **但装配链没有跑完**：`build/current/client` 的 manifest 还是 50 图（tms273-16）⇒ 客户端 `catalogHasMap` 判关闭门，就是用户看到的现象。服务端 `shared/maps.json` 也未更新（须重启才生效）。
- 世界地图跳转链路（`worldmap-view.ts::onJump` → `worldMapMove` → `portals.rs::handle_world_map_move`，落在目标图 `sp`）此前已建成，无需改动——它依赖目录收录，目录扩到 71 后可跳的点自动变多。

## 本次执行的装配链

`import_tms273.py`（前一轮已跑，19:51）→ `export_tms273.cjs maps|entities|portals|effects`（19:51-52 已跑）→ `export_tms273_reactor|minimap|worldmap.cjs`（19:52 已跑）→ **`generate_tms273_gameplay.py`（本次重跑）** → **`backfill_tms273_slot_expand.py`** → **`export_tms273.cjs items`** → **`export_tms273_cashshop.cjs`** → **`export_tms273_avatar.cjs --mage-actions`** → **`assemble_tms273.cjs`** → `check_tms273_runtime.cjs`。

途中处理：

1. **陈旧中间态 gameplay/items（本次新发现）**。前一轮 20:22 曾跑过一次 generate，但产物与 71 图目录不一致：16 家店的行里引用了 items 表没有的道具（弓 1452002、弩 1462001、药水 2260000、返回卷 2030003 等，共 132+3+6+1 条）。重跑 `generate_tms273_gameplay.py` 后 items 865 条、全部商店行闭合；`export_tms273.cjs items` 导出 850+ 帧图标。教训：**导出链必须在同一次目录状态下跑完，中断后重跑要整段重来**。
2. **manifest.pets 别名断言**。装配把与真宠物 id 相撞的 cash 图标补成 8 位填充别名（`05000000` 等 12 条，帧与本体逐字节一致）。`check_tms273_runtime.cjs` 原「数量相等」断言改为「pets.json 990 条逐条在场 + 额外条目必须是等价填充别名」。
3. **WORLD_MAP_ABSENT 增两条**。`100030400`、`310040210` 源 `WorldMap010` 没给 spot（与 `002010000`/`104020130` 同类源侧缺失），已核实后加入豁免集。
4. **walker 测试口径升级**（`realmaps.rs`）。新图 `100020000 芽孢山丘` 的源 foothold 止于 x=1440 而图右缘 1511——持续右走会走出台缘坠落，属源侧真实地形。服务端 `recover_at_fall_boundary` 会把越界坠落夹回最后一个 foothold（实玩安全），原「第 300 tick 必在地面上」改为「停止输入后 600 tick 内必须回到地面」，不可回收的破图会超时失败，审计意图不变。
5. **怪物面更新**（`mob_move_acceptance.rs`）。部署面 17 → **26**（新图带来 100000 蜗牛族、1110100 蓝水灵、2130100 等 9 个模板）；`can_move()` 全量断言照旧通过。

## 装配后的目录

- `assemble_tms273.cjs` → `{"version":"tms273-18","maps":71,"assets":30422,"npcs":222,"monsters":27}`。
- 传送闭环抽查：楓葉島 `100000000/in00..05|east00`、`100000100/200` 内景、魔法森林两家店、树木站台三登机口，目标全部在目录内。
- **剩余 18 个门的 18 个目标确认源边界**（TMS273 WZ 无 JSON，非漏装配）：`100000101..105`（楓葉島商店内景）、`100000203`、`100020100`、`100030320`、`100051010`、`101020100`、`102030100`、`102040100`、`103010000`、`120010000`（前往天空之城）、`130000200`、`310040000/110/400`。这些门维持「此路线尚未开放」提示，是正确行为；将来解包源补齐后重跑 import+装配链即自动接通。

## 验证

- `check_tms273_runtime.cjs` → `71 maps; 73769 source references; client/server geometry and quest generation agree.`
- `cargo test --offline` → **354 passed / 7 failed**，失败集与基线完全一致（auth third_store、mage bundled_catalog、world config_drop/portal_command/reactor_area/third_sphere、shop_sell auto price）。
- `tsc --noEmit` 0；`npm run check` 29/31（仅 `layer-animation.check.ts`、`build-release.check.cjs` 两项基线失败）。
- 世界地图跳转：链路无改动，`handle_world_map_move` 依旧以「目标图在目录内 + 落 `sp`」为闸门；71 图后可跳 spot 覆盖楓葉島、墮落城市、蘑菇村、幸福村等新区域。

## 部署（待用户执行）

服务端只在启动时读 `shared/maps.json`，客户端 manifest 是静态资源，**必须一起换**。请在终端执行：

```
zsh 启动3010.command
```

（脚本会自动：check → cargo/vite 候选构建 → 停旧 → activate → 健康检查 → 拉起陪测 bot。）

重启后会话表作废，页面需刷新并重新登录——这不是回归。

## 待实玩确认

- 楓葉島方向：`100000000`（楓葉島東侧路）的 `in00..in05/east00` 走进楓葉島本岛，光束与落地正常。
- 大地图（M 键 / WORLD 按钮 / MENU）点击楓葉島、蘑菇村、墮落城市等 spot 可直接跳转。
- 芽孢山丘右缘走出台面会坠落并被拉回台缘（回收行为，非卡死）。
