# 巨石之约：立体港口与手绘场景重制

2026-09-21。Astra 主代理完成模型、权威接线、渲染与实际操作检查；Luna / max 仅核查素材、构图、遮挡来源及生成独立树冠贴图。沿用当前用户授权，未切换 3010、修改原库、提交或推送。

## 本轮结果

- Blender MCP 制作可编辑 `resources/scenes/colossus/redesign/models/handpainted_harbor.blend`：错层石岸、红瓦屋、蓝旗灯塔、木码头、晒网架、货箱、街灯、花箱；六区地形使用不规则石崖和成组植被，去除旧重复草格。保留 `colossus_terrain_refined.blend`，草层与土层同步收至膝部内侧。
- 港口增加三条实际可走的路线：`harbor-street` 居民坡街、`harbor-roofs` 后侧屋顶回廊、`harbor-skywalk` 海侧悬台花园。12 个双向路口复用既有服务端道路和移动规则；首个坡街入口在主路 s=17，位于断阶之前。后方高台 z 约 -39 m、海侧高台 z 约 +27 m，不在同一竖直平面。
- 房屋、桥面和平台直接从模型进入游戏；石阶按同一权威路线和两个缺口分段。路口优先响应 ↑，T／点击仍可交谈；同点往返路牌合并。遮挡角色的攀爬石壁淡出，主路仍有独立的遮挡处理。
- 保留原版纸娃娃、船及大小石头人；补蓝旗、暖石材、树冠笔触与海面层次。Kenney CC0 植被保留原网格，树冠图片的来源和提示记录于 `resources/scenes/colossus/redesign/textures/generation.md`。
- 近港青灰墙的最终射线来源是侧卧前臂的完整尺寸护腕。改初始上臂、肘部姿态与展开角，把前臂放到港外；保留护腕比例、48 骨骼／30 指骨和既有起身动作。当前实际掌面最高顶点初始世界 y=40 m，海面 y=-5 m。既有 100 km 仍是工程尺度候选，最终身高未定。
- 远景镜头完整容纳头、肩、躯干及双手。M 键第三／四阶段使用 v4 图：沿固定体表蜿蜒至头顶，再绕背指向神庙；可动手臂不承担固定桥。本轮仍只开放外侧起始段，不宣称头顶到神庙全程已完成。

## 必要验证与可见证据

- 最终相关 Rust 9 项通过，覆盖新路口端点重合与连续输入、跳落与旧断阶、能力和持久归属、原 NPC 对话、骨骼攀爬、初始掌面露出高度及抬手。
- 客户端类型检查及候选构建通过；世界地图 11 项通过；模型／地图／素材包检查通过：25 文件、344 原版帧、4 地图阶段、48 骨骼／30 指骨。
- 主代理真实浏览器从码头进入坡街，走完整个屋顶回廊并回到居民区，再经主路进入海侧悬台，跳起／落回平台并回到主路 s=35。使用真实按键和权威快照，没有改客户端坐标。
- 实看正、侧两种角度、石壁遮挡和最终登岸：`resources/scenes/colossus/redesign/previews/town-sea-platform.png`、`town-depth-side.png`、`town-cliff-occlusion.png`、`town-final-arrival.png`。`giant-reveal-v5.png` 是本轮完整巨像镜头证据，拍摄于最终初始手臂姿态调整之前；站立终态未改变。
- 本机 1440×900 标准画质 600 帧样本：P50 8.3 ms、P95 9.3 ms、P99 9.4 ms，渲染 P95 1.3 ms；这是当前浏览器样本，不替代实体低端 GPU／省电模式和真实触控板手感验收。检查页无运行错误。

最终候选 `20260920171704-12586335`；协议 34、原版内容 `tms273-33`、巨像内容 `colossus-runtime-0.5`。隔离试玩地址 `http://127.0.0.1:3017/`，数据在 `runtime/preview-3017`。保留用户可能正在体验的 3012，关闭本任务旧自检 3011／3013／3014／3015／3016；3010 和用户库未动。

## 待用户体验

本轮把港口从单坡扩成有前后距离的三组回路，并更换实际场景美术；不以此声称手绘世界地图 1:1 终稿或完整世界已完成。外围旧环港空地的密度、更多自然植被层次及不同设备的观感仍以实际反馈继续迭代，行动入口保留在 `docs/plan/PLAN.md`。
