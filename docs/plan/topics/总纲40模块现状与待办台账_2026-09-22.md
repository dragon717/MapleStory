# 总纲 40 模块 · 现状与待办台账（2026-09-22）

> **本文件是「完整台账」，不是行动入口。** 现行行动入口是 [`../PLAN.md`](../PLAN.md)
> ——那里只保留**未完成、且最重要的三件事**。本文件承接原先写在 PLAN.md 里的**全部明细**：
> §1 四十模块进度速览、§2 §01–§40 逐模块现状与**全部待办**、§3 待确认或操作、§4 原创扩展、§5 跨模块台账。
> 逐日期交付记录见 [`../INDEX.md`](../INDEX.md)。
>
> **本文件的来历**：2026-09-22 PLAN.md 按总纲重构后、再精简为「三件事」之前，其内容**逐字节复制**至此
> （sha256 `fa1d0458335b221b0d2f99287bf014b7c36a294fd96862a5b516f680c0f99d98`，681 行 / 73,065 字节，
> `cmp` 与当时的 PLAN.md 一致；同内容另留档
> [`../history/2026-09-22/PLAN_按总纲40模块重构后_精简前完整快照.md`](../history/2026-09-22/PLAN_按总纲40模块重构后_精简前完整快照.md)）。
> 除本段说明与**相对链接改基**（本文件在 `topics/` 下，`history/`→`../history/`、`topics/`→本目录、
> `INDEX.md`→`../INDEX.md`、`../technical|design/`→`../../technical|design/`）外，**正文一字未改**。
>
> **总纲**：[`MapleStory_Replica_Audit_Code_Map_2026-09-18_c50b564.md`](MapleStory_Replica_Audit_Code_Map_2026-09-18_c50b564.md)
> ——固定提交 `c50b5647a9a57fb00cd4947559d41eb5b694885f` 的审计快照，给出 **40 个模块**的功能分类与
> 「主干已实现／基础已实现／部分实现／未接入」四档判定。
>
> **口径优先级（以总纲为准）**：
> 1. 模块**编号、名称、归属**一律沿用总纲，本文件不另立第二套分类；
> 2. 总纲的**进度档位**与其判定依据（总纲 §2 重点判定、§5 接入边界）**不在此改写**；
> 3. 本文件只做两件事：登记**本仓当前状态**（总纲快照之后已推进的部分）与**待办**；
> 4. 两者冲突时以总纲为准；总纲未覆盖的内容（原创世界、工程台账）单列在 §4、§5，**不计入**原版 40 模块。
>
> **可追溯性**：按总纲重构前的完整 PLAN（2026-09-22 版，1478 行 / 210,283 字节，git 末次提交 `b64976f`）已
> **逐字节留档**——
> [`history/2026-09-22/PLAN_按总纲40模块重构前的完整快照.md`](../history/2026-09-22/PLAN_按总纲40模块重构前的完整快照.md)
> （sha256 `29e05079a103257bedc9cb092257c8b5debb7e48b36b3f1aa324b47105981e7a`；`cp -p` 复制后经 `cmp` 与原文
> 逐字节一致）。**总纲本身一字未改**（`topics/MapleStory_Replica_Audit_Code_Map_2026-09-18_c50b564.md`
> 保持 35,412 字节原样）。
>
> **两条留档契约**：
> 1. **待办项**（`- [ ]`，共 **101** 条）**全部**落到 §2–§5，并**保留原文措辞与交付记录链接** ⇒ 执行细节不依赖快照即可读取。
> 2. **已完成项**（`- [x]`，共 **80** 条）在 §2–§5 中按模块**凝练承接**（不再逐条罗列）；其完整叙述与
>    交付记录已由 [`INDEX.md`](../INDEX.md) 按日期承接，需要背景时回快照或对应 `history/YYYY-MM-DD/` 记录。
>
> **零丢失已做成可复现判据**（2026-09-22 核对）：① 101 条未完成项归一化后逐条子串比对，**0 未命中**；
> ② 80 条已完成项经「凝练承接／交付记录存在性／INDEX 行」三路复核，旧 PLAN 引用的 **65** 条交付记录
> **0 断链**；③ 补录 **18** 条原先只能从旧 PLAN 抵达的历史记录，使 `history/` 下 **241** 份记录
> **全部**可从 `INDEX.md` 抵达（**0 孤记录**）。

---

## 0. 版本与验收基线

| 项 | 当前值 | 说明 |
|---|---|---|
| 内容版本 | `tms273-46` | 五处手写落点：`shared/protocol.ts`、`server/src/protocol.rs`、`scripts/assemble_tms273.cjs`、`scripts/check_tms273_runtime.cjs`、`scripts/check_colossus_live.cjs`。**只有内容资源／数据契约真的变才升**（纯代码搬移不升）；升完必须重跑装配器，否则派生 `shared/*.json` 与 `client/public-tms273/assets/manifest.json` 落后。（本表初稿记的 `tms273-38` 已落后：`39` = S1 施放窗、`40` = 椅子恢复间隔固定节拍、`41` = 法师 212／232 七条同格副本接执行链、`42` = 再补两条傳說冒險、`43` = 召喚聖龍 `2321003` 接入通用召唤队列、`44` = 進階祝福 `2321005` 接增益窗与属性层、`45→46` = 開關技能收口 ＋ 火靈結界 `2121054` 接执行链（2026-09-24 第七轮，新增 1 条 `skillEffects` 与 29 张 PNG）。**S5 召唤通用化、召唤存活时长收口、桌面 D0 机械迁移（2026-09-23）、burn 窗收口与 `1121015` 接执行链、技能轉換 `2321054` 接执行链（末两条均为 2026-09-24）全部零 `shared/**` 字节 ⇒ 未升版本、未重跑装配器。** ⚠️ **本表只记历史沿革；当前值以 `PLAN.md` §0 为准。**） |
| 协议版本 | `35` | 与内容版本独立；改 wire 才动。`34→35` = 快照 `derivedStats` 新增 `fireWardActive`（開關态**逐本**上报）。**技能轉換那轮（2026-09-24 第八轮）未改 wire。** |
| 总纲快照口径 | 协议 `24` / 内容 `tms273-31` | 总纲是 2026-09-18 的固定快照，**其版本号不作为当前值**；当前值以本表为准。 |
| 装机规模 | 211 图 / 120,783 资源 / 395 NPC / 79 怪 | 装配器 `tms273-46` 自报 `maps:211`、`assets:120783`、`cashAppearanceLayers:1754`、`npcs:395`、`monsters:79`、`missing:[]`。客户端清单同版：`skillBooks` 35、`skillCatalog` 504、`skillEffects` **49**。 |
| 技能目录 | 35 本 / 504 条（战士 139 + 弓箭手 105 + 飞侠 107 + 法师 153） | 书准入＝四张职业表按布局切片派生。 |
| 转职任务 | `shared/job-advance.json` 30 条 | 法师三分支 9 + 战士／弓／侠各 7。 |
| `cargo test` | **674 过 / 4 失败** | ⚠️ **2026-09-24 更正：原记「666 过 / 0 失败」与实测不符。** HEAD `a14883f` 基线 **667 过 / 4 失败**，4 个失败是**有意为之的产品行为与旧验收契约的冲突**（根因与处置见 §5.1 与 §3），非回归。含 S5 1 条 + 法师副本 6 条 + 召唤存活时长 1 条 + 召喚聖龍 2 条 + 進階祝福 4 条 + burn 窗三义 1 条 + 開關技能 1 条 + **技能轉換 5 条**（第八轮）。 |
| `run-checks.mjs` | **63 项 / 55 过 / 8 红** | 8 红＝既有基线集合，逐项见 §5.1。⚠️ 本表曾记「63 项 / 56 过 / 7 红」、§5.1 曾记「65 项 / 57 过 / 8 红」，**总数与过数都记错了**（红集合一致）⇒ 判据是**失败集合逐项相同**，不是总数。 |

---

## 1. 总纲 40 模块 · 进度速览

> 「总纲判定」列＝总纲原文档位（2026-09-18）。「本仓现状」列＝其后至 2026-09-22 已推进的部分；
> **若现状已超出总纲档位，本表不改写总纲档位，只标注「已超出，待总纲下一版固化」。**

| 编号 | 模块 | 总纲判定 | 本仓现状 | 待办数 |
|---|---|---|---|---|
| 01 | 账号与角色 | 主干已实现 | 总纲口径；角色选择纸娃娃与游戏内一致已修 | 1 |
| 02 | 界面与纸娃娃 | 主干已实现 | 总纲口径 + 物品栏滚动／残影／装备对比、自定义键位、地图纸娃娃普通装备缺显、菜单可见改名 | 5 |
| 03 | 移动与碰撞 | 主干已实现 | 总纲口径；花蘑菇不移动已修待验 | 1 |
| 04 | 战斗与怪物 | 主干已实现 | **已超出**：四支柱机制纵深 + 物理线 37 条／法师分支 14 条攻击接执行链 + 跳字与击退 | 9 |
| 05 | 掉落与拾取 | 主干已实现 | 总纲口径；`PickupSink` 拾取去处建模 | 0 |
| 06 | 死亡与复活 | 基础已实现 | 总纲口径；原创墓碑／虚影见 §4.2 | 0 |
| 07 | 职业与技能成长 | 部分实现 | **已超出**：四条职业线 35 本 504 条、转职链 30 条、一转四职业岔道菜单、书准入单一权威 | 8 |
| 08 | 属性、Buff、异常状态 | 部分实现 | **已超出**：属性聚合／伤害管线／限时状态三权威已收口 | 2 |
| 09 | NPC 与对话 | 部分实现 | **已超出**：点击对话阶段一／二／传送类完成（233/265 模板 553 行源台词） | 0 |
| 10 | 任务与剧情 | 部分实现 | 任务进度、`infoex` 语义核定；主线后续章节 55 条中 2 条可执行 | 4 |
| 11 | 交通与旅行 | 部分实现 | **已超出**：飞行船三期全部收口、計程車传送、勇士部落 `102030100` 放行 | 7 |
| 12 | 背包与道具 | 主干已实现 | 道具权威世界模型增量 ①–④ 落地 | 3 |
| 13 | 装备与卷轴强化 | 部分实现 | 总纲口径（星力／潜能未接入） | 0 |
| 14 | NPC 商店与仓库 | 基础已实现 | 維多利亞港三家店、魔法森林两家店已装配 | 2 |
| 15 | 现金商城 | 部分实现 | 展示试穿／购买／限购／租赁／持久去重 + 购买事务化 + 窗口规范 | 7 |
| 16 | 玩家交易 | 未接入 | 未立项 | 0 |
| 17 | 拍卖／寄售 | 未接入 | 未立项（与 §12 增量 5 同一决策阻塞） | 0 |
| 18 | 邮件 | 未接入 | 未立项（同上） | 0 |
| 19 | 组队与好友 | 部分实现 | 总纲口径；好友／黑名单待实玩 | 1 |
| 20 | 公会与联盟 | 未接入 | 未立项 | 0 |
| 21 | 聊天 | 部分实现 | 总纲口径；表情／悄悄話待实玩 | 1 |
| 22 | Boss 与副本 | 部分实现 | 总纲口径（单人练习） | 0 |
| 23 | 宠物 | 部分实现 | **已超出**：三宠伙伴重构、持久化、跨图常驻、变速／寻物 | 2 |
| 24 | 冒险笔记／图鉴 | 部分实现 | **已超出**：NB-00~05 + NB-07（单入口＋四页窗口＋私有查询）已落地 | 3 |
| 25 | 五／六转与高阶成长 | 未接入 | 未立项 | 0 |
| 26 | 账号横向成长 | 未接入 | 未立项 | 0 |
| 27 | 普通采集物／Reactor ★ | 基础已实现 | 总纲口径 | 0 |
| 28 | 专业采集 ★ | 未接入 | 未立项 | 0 |
| 29 | 制作／炼金／分解 ★ | 未接入 | 未立项 | 0 |
| 30 | 坐骑 ★ | 仅装备层基础 | **已超出**：`KeyR` 骑宠键、装备窗骑宠格、鞍具子页、骑宠页签 935 件 | 4 |
| 31 | 椅子 ★ | 未接入 | **已超出**：椅子页签 2799 件、缺显修复 | 2 |
| 32 | 狩猎符文 ★ | 未接入 | 未立项 | 0 |
| 33 | 狩猎连击／多杀 ★ | 未接入 | 未立项 | 0 |
| 34 | 精英怪／精英 Boss 事件 ★ | 未接入 | 未立项（飞船巴洛古袭击按总纲不计入本模块，归 §11） | 0 |
| 35 | 成就 ★ | 未接入 | 未立项 | 0 |
| 36 | 称号 ★ | 未接入 | 未立项 | 0 |
| 37 | 勋章／徽章装备 ★ | 仅装备层基础 | 总纲口径；图鉴奖励／勋章仍被 NB-08 阻塞 | 0 |
| 38 | 联机与会话 | 主干已实现 | **已超出**：网络运动插值已实现（本地预测仍按总纲不做） | 1 |
| 39 | Web 与桌面交付 | 部分实现 | **已超出**：强缓存对象库（刷新 61MB→3.8KB）、页面陈旧自愈、强制更新契约修正、源码开发入口 | 11 |
| 40 | GM 与运营基础 | 部分实现 | 总纲口径 + `/exp` | 3 |

**未立项模块统一说明**（16／17／18／20／25／26／28／29／32／33／34／35／36）：
总纲判定「未接入」，本仓**未立项**，总纲已给出建议落点（见总纲 §1 表与 §4 代码目录）。
**这些不是遗漏缺口，是尚未开工的模块**；按既定政策不预先补全，待用户实际需要时
**以总纲的建议落点立项**，不另造分类与目录。

---

## 2. 模块现状与待办（按总纲编号）

### 01 · 账号与角色　〔总纲：主干已实现〕

总纲口径：注册登录、多角色创建／选择、外观与当前装备预览；仅单频道，删角／改名未接入。
总纲建议落点：删角／改名优先扩 `lobby.rs` 与 `F/entry/`，**不另造角色服务**。

本仓推进：角色选择纸娃娃与地图内、商城预览三处一致性已修（根因是 lobby 角色摘要只带创角冻结外观、
不带当前装备；已在 `CharacterSummary` 增加 `equipped`）。

- [ ] 经 `启动3010.command` 统一加载后实玩：换装／穿商城时装的角色重登，快速开始与角色选择纸娃娃应与地图内、商城预览一致。

### 02 · 界面与纸娃娃　〔总纲：主干已实现〕

总纲口径：HUD、常用窗口、换装、键位与快捷栏已接入；未路由的原版菜单仍显示未实装。
**不要把菜单美术算成系统完成。** 建议落点：按实际功能接菜单。

本仓推进：物品栏滚动／残影／装备对比根因修复（2026-09-18）、自定义键盘快捷栏（2026-09-14）、
地图纸娃娃普通装备缺显补 693 个按需外观（2026-09-13）、菜单「冒險筆記（圖鑑）」可见改名（NB-07 前置）。

- [ ] 菜单「快捷鍵變更」或默认 O：拖入初心者技能、修改键盘绑定和 32 格显示键，刷新/重登验证保留。配置按角色保存在当前浏览器；四尺寸离线无横向溢出/生命状态遮挡，实玩手感待用户验收。
- [ ] 通过 `启动3010.command` 统一加载候选 `20260913142719-7e27c737`（协议19 / 内容`tms273-20`）后刷新，核看现有角色的帽子1002017、上衣1040017、武器1212000与翅膀在商店/地图的站立、移动、施法、穿脱与跨图显示。在线服务和存档未动，无独立QA。
- [ ] 大地图（World Map）：地图 ID 补零缺陷修复 + 打开落在所在区域页。
- [ ] 修复小地图 MaxMap 切片几何（源像素实测重排）：确认小地图拼接无错位/空白。
- [ ] **世界地图「已探索点 + 当前位置任务悬停提示」**：待用户确认 ② 的两条口径后，实现「世界地图已探索点 + 当前位置任务悬停提示」。**阻塞于用户口径**——①「已探索点」要不要**持久化**（跨重登/跨图保留）；②悬停提示的「该做什么任务」取哪一层（当前图可做 vs 进行中 vs 全部）。**需要用户给口径**，否则做出来会是猜的。（2026-09-21）

### 03 · 移动与碰撞　〔总纲：主干已实现〕

总纲口径：行走、跳跃、下跳、爬绳、游泳、墙体碰撞与坠落恢复；完整原版手感未实玩核验。
建议落点：复用现有移动链；缺陷定向修复。原创场景（巨石之约）的轨道／瞬移问题见 §4.3。

- [ ] 花蘑菇（菇菇寶貝 1210102）无法移动：确认已会走动。

### 04 · 战斗与怪物　〔总纲：主干已实现〕

总纲口径：普攻／技能命中、受击、仇恨、刷新、部分怪物技能；**不是全怪物 AI 完整复刻**。
建议落点：新规则按职责扩现有模块；特殊事件见总纲 30–33。

**本仓已超出总纲档位**（待总纲下一版固化）：

- **四支柱机制纵深**（2026-09-22 第十三轮）：`server/src/mechanics.rs` 是**召唤物／DoT／投射物／二段命中**的
  唯一权威；`AttackPlan::of(level)` 是**唯一入口且纯派生、不写技能名单**。结算优先级写成代码判据：
  同拍 7 层（触发骰 → 首段 → 挂 DoT → 二段 → 召唤脉冲 → 投射物到达 → DoT 滴答）；
  世界拍 `step_summons → step_projectiles → step_monster_dots`（越已确定／越被动越靠后 ⇒ DoT 永不抢
  投射物的击杀归属）。交付记录见 `INDEX.md` 2026-09-22 首条与 `history/2026-09-22/四支柱战斗机制纵深.md`。
- **攻击技能接执行链**：`PHYSICAL_AREA_ATTACKS` **39 条**（⚠️ 本条原记 37 条；2026-09-24 按源表实测为 38，
  同日接入 `1121015 烈焰翔斬` 后 39；**计数注释也是判据，须从源重算**）；法师分支 14 条走同一条
  `cast_elemental_area`。名单是**源字段规则派生**的：删 `PHYSICAL_MECHANISM_FIELDS` 一行即宣布该机制已实现
  ⇒ 门禁自动要求技能进表**且** `mechanics.rs` 出现读取点（两处任缺其一当场红）。
- **源 `time` 三义收口 + 再接一条物理线技能**（2026-09-24，零 `shared/**` 字节 ⇒ 不动版本、不重跑装配器）：
  `1121015 烈焰翔斬` 的 `time` **逐级等于 `dotTime`**（源里是同一表达式）⇒ 是 **burn 窗**（同一条 DoT 时长的
  第二份书写），而收口前那句「有 `time` 就有自增益窗」**内联**在 `AttackPlan::of` 里 ⇒ 一接线就会凭空开出
  45~60 秒窗。现立**唯一派生点** `mechanics.rs::{self_buff_window_ms, is_dot_window}`，判据是**逐级数值恒等**
  （不是「见过 `dotTime` 字段」），从源独立重算**恰好 3 条**（`1121015`/`2121006`/`2121011`）、**无灰带**；
  `NEGATIVE_STATUS_TIME_ATTACKS` **8 → 2**（只剩 `3121052 波紋衝擊` / `4121017 挑釁契約`，`time` 语义未核定）。
  门禁新增 burn 窗独立重算 + 三元素精确断言 + **互斥断言** + 未接执行链清单**双向比对** + **§3g′ 三层文本断言**
  （唯一派生点在不在／真读三字段／`AttackPlan::of` 经派生点且**自己不再读 `level.time`**）。
  验收侧**修掉一个空判据**（原用例靠「`1121015` 不可施放」**蒙对**结论 ⇒ 改真实施法路径：满级 `prop=100`
  施放须成功 + DoT 须真挂上 + 不许冒出自增益窗），并新增三义用例（含 S1 零回归断言）。
  见 `history/2026-09-24/burn窗收口与1121015接执行链.md`。
- **机制判据纠偏**（2026-09-22 第十二轮）：`1221009 騎士衝擊波` 源 `time:"0"`／`prop:"0"`（所有等级都是字面量 0）
  被旧判据永久锁在表外、报文还说错原因；现改用**机制组**判据（同组字段有一个某级非零才算参与）。
- 三块战斗权威：`damage.rs`（伤害修正管线）、`attribute.rs`（属性聚合）、`player_status.rs`（限时状态）。
- 跳字与击退（2026-09-19）：`BasicEff.img` 花色是**帧像素**非运行期染色（`NoRed`=普通伤、`NoCri`=暴击、
  `NoProduction`=绿回血、`NoBlue`=蓝回魔）；源 `pushed` 是「击退所需**单次伤害阈值**」而非距离。

- [ ] 统一加载实玩：物理线新接 5 条技能的 DoT 跳伤节奏与投射物落地判定 ；**外加 `1121015 烈焰翔斬` 的 burn 跳伤**（2026-09-24 接入，时长走 `dotTime`、**不该**出现自增益图标）。
- [ ] **大模块后续切片**（本轮做完了 S2/S3/S4 与 S1 的 DoT 那半，其余未开工）：**S1 施放窗：已交付（窄口径，2026-09-22，内容 `tms273-39`）**；**S5 召唤通用化：已交付（窄口径，2026-09-23）**——`ThunderSummon` → `Summon`、`Player::summon` 单槽并入 `Player::summons` 一条队列（`SUMMON_BUDGET` **2 → 3**）、脉冲周期收口成 `mechanics.rs::summon_pulse_ms` 唯一派生点、**存活时长同日第三轮再收口成 `mechanics.rs::summon_lifetime_ms` 唯一派生点**（收口前三处各写一份且互相不一致：冰魔／火魔当秒、球形闪电当秒但默认值另写 20／60、冰鋒刃**根本不读源**写死 `4_000`）、`step_summons` 不再含技能名单。**仍未做**：负面状态窗未统一、`3111013 箭座` 未接、门禁的 `subTime`/`prop` 判据未动；法师 `212`／`232` 攻击接线仍在待办。⚠️ 旧结论「召唤实体未实现」只对**物理线分支书**成立，对**冰雷域**不成立。
- [ ] 统一加载实玩：骑士 `1221009 騎士衝擊波` 学技能 → 加点 → 放技能（3 目标 × 10 段）。
- [ ] **大模块后续切片**（本轮只做了 S0 判据纠偏；**S1 的 DoT 那半 + S2 + S3 + S4 已在第十三轮完成**；**S1 施放窗 2026-09-22、S5 召唤通用化 2026-09-23 亦已完成**）：**法师 `212`／`232` 攻击接线已交付十条（窄口径，2026-09-23，内容 `tms273-42`→`43`）**——取「源 `common` 与已接线的冰雷那本**逐字段同形**」的副本，复用既有机制不新增机制：楓葉祝福 `2121000`／`2321000`、魔力無限 `2121004`／`2321004`、楓葉淨化 `2121008`／`2321009`、召喚火魔 `2121005`（走 S5 的通用召唤队列）、傳說冒險 `2121053`／`2321053`（走 `HYPER_ADVENTURER_SKILLS` 三本表；**它的消费点在伤害管线里**，所以同时把 `check_tms273_damage_pipeline.cjs` 的 `NOT_CONSUMED.indieDamR` 从 3 条降到 1 条），以及**召喚聖龍 `2321003`（同日第五轮，第三条召唤：接纳名单 `DEMON_SUMMON_SKILLS` → `SUMMON_SKILLS`、位移形态 `Follow`，仍是零新机制）**。**当时记的「剩 7 条」已随三轮接线收敛为「剩 3 条」**（`2121052` 藍焰斬／`2321052` 天堂之門 早已在 `BRANCH_AREA_ATTACKS`、`2321005` 進階祝福 同日第六轮接线、**`2121054 火靈結界` 2026-09-24 第七轮接线**）——**本轮（2026-09-24 第八轮）又接掉 `2321054 復仇天使`（技能轉換），现只剩** `2321006 復甦之光`（队伍复活）、`2321015 神聖之水`（计数器＋可交互实体＋上键拾取），**两条**都要先立新机制；`BRANCH_AREA_ATTACKS` 现 16 条，第八轮一条未加（`2321054` 不是范围攻击，走的是「授予另一批技能」的新路径）。**第七轮收口方式**＝把「按一个 id 写死的開關技能」收成接纳表 `world.rs::TOGGLE_FIELD_SKILLS`（`2221054`／`2121054`）＋三个派生点（形态 `ToggleFieldPulse`／每秒耗蓝 `MAGE.mpCon`／周期 `subTime`＋兜底 2400ms），`Player` 三字段并成 `toggle_fields: BTreeMap<u32, ToggleFieldRuntime>`；内容 `tms273-45→46`、协议 `34→35`，见 `history/2026-09-24/開關技能收口与2121054接执行链.md`。两条已核实的基建事实：**冰雷域已有召唤实体**（到期／锚定／周期打击／`summon_limit` 都在 `elemental.rs`），**玩家侧 DoT 也已有**（`player_status.rs::due_dots`）。
- [ ] 经 `启动3010.command` 统一加载后实玩：第九轮新增的 **25 条技能**（学技能 → 加点 → 放技能）与 **22 条转职任务**（找 NPC → 接任务 → 打怪收集 → 转职）都未在真实客户端跑过。
- [ ] 遗留（**不是本轮引入**）：战士 `1001008 戰鬥置換`、飞侠 `4001011 二段跳`／`4001003 隱身術`、弓箭手 `3001004 斷魂箭`／`3001007 連續跳躍`**未接执行链**（机制缺席，逐条登记在门禁）；那 **9 条机制标记 + 14 条 `hidden`**（合计 **23 条**；⚠️ 本条原记「17 + 14」，2026-09-24 由门禁
`PHYSICAL_UNWIRED_MECHANISM` / `PHYSICAL_UNWIRED_HIDDEN` 从源重算并双向钉住）仍未接 —— 缺 DoT／触发骰／窗口状态／投射物时序／第二命中盒／召唤实体，删掉 `PHYSICAL_MECHANISM_FIELDS` 对应一行即自动要求它们进表；同格的另两条法师分支副本**已接十二条（2026-09-23 的十条 ＋ 2026-09-24 第七轮的 `2121054 火靈結界` ＋ 第八轮的 `2321054 復仇天使`），仍剩 2 条「尚未开放施放」**（`2321006 復甦之光`／`2321015 神聖之水`）。
- [ ] 经 `启动3010.command` 统一加载后实玩：火毒／主教角色**学技能 → 加点 → 放技能**走一遍（含四转书加点、`212/232` 的冷却是否真的计时、`damR` 强化后的伤害数值）。
- [ ] 遗留：同一格的另两条分支副本里，**已接十条**（`2121000/2121004/2121005/2121008/2121053`、`2321000/2321003/2321004/2321009/2321053`，2026-09-23，内容 `tms273-43`；逐条复用 `MAPLE_WARRIOR_SKILLS`／`INFINITY_SKILLS`／`MAPLE_CURE_SKILLS`／`SUMMON_SKILLS`／`HYPER_ADVENTURER_SKILLS` 五张表，**不新增机制**），**该条「仍剩 7 条」的名单已随四轮接线作废**（`2121052`／`2321052` 早已在 `BRANCH_AREA_ATTACKS`；`2321005 進階祝福` 2026-09-23 第六轮接线；`2121054 火靈結界` 2026-09-24 第七轮接线；`2321054 復仇天使` 2026-09-24 第八轮接线）；**当前仍「尚未开放施放」的恰好 2 条**：`2321006 復甦之光`、`2321015 神聖之水`——被动已按数组消费，**主动施法窗仍是逐技能实现**；`BRANCH_AREA_ATTACKS` 现 16 条（该条原记的「14 条」亦已过时）。另：召喚火魔源里多出的 `dot`／`dotInterval`／`dotTime` **已确认在被消费**（2026-09-23 复核更正：召唤物的周期打击走的就是 `cast_elemental_area_at → settle_area_segments`，而 DoT 挂载点就在那条链里；由 `mechanics_acceptance.rs::mech_summon_pulse_mounts_the_source_dot` 钉住）。召唤实体**已收口成通用槽位**（见上 S5，2026-09-23），**存活时长也已收口**（同日第三轮，`mechanics.rs::summon_lifetime_ms`；门禁 `check_tms273_damage_pipeline.cjs` §3h 双向断言唯一派生点、三个施放入口、以及这三个函数体里不许再出现 `time`）；**接纳名单已扩到三条**（同日第五轮，`DEMON_SUMMON_SKILLS` → `SUMMON_SKILLS`，位移形态收口在 `world.rs::summon_motion`）。**2026-09-24 再补**：仍「尚未开放施放」的那几条与其逐条理由已从散文变成判据（**第七轮又接掉 `2121054 火靈結界` ⇒ `MAGE_BRANCH_PENDING` 自动从四条降到三条**，这本身就是该判据生效的证据）——`check_tms273_damage_pipeline.cjs::MAGE_BRANCH_PENDING` 以「`212`／`232` 里带 `mpCon`、非 `hidden`、`world.rs` 无常量」**从源独立重算**得恰好这三条（`2321054 復仇天使`／`2321006 復甦之光`／`2321015 神聖之水`），双向钉住；每条理由须含源文案锚点词（改理由≠改文案，漂了就红）。同时**纠偏两处与源不符的理由**：`2321054` 不是開關技能（源是「慈愛技能轉變成復仇技能」＋`[被動效果]`），它的 `madX`／`mdR` 也不是「窗口内的值」——`world.rs`、`check_tms273_attributes.cjs`、`check_tms273_damage_pipeline.cjs` 三处登记已更正；可施放但 `hidden` 的 `2321016 神聖之血` 恰好是復仇天使那次轉換的产物。**第八轮已把那次轉換实装**（`world.rs::TRANSFORM_PAIRS` 四对 ＋ `TRANSFORM_SKILLS`），被转换的四本復仇副本里**只有 `2301010 天使之觸` 已接执行链**（它早就在 `BRANCH_AREA_ATTACKS`），另三本（`2311015`／`2311014`／`2321016`）**缺机制、由门禁 §3i 的 `TRANSFORM_UNWIRED` 表逐条登记**（含「不许给它们起 `SKILL_*` 常量」的反查——起了常量＝宣布已接执行链，那条登记当场红）。
- [ ] 经 `启动3010.command` 统一加载后实玩（协议与内容版本都要一起换）：挨怪打看红字；喝红/蓝药水看绿/蓝字；开魔心防禦挨打看「红字 + 蓝字」两根；坐椅子／魔力激发看绿蓝；打怪看红字，并在**伤害达到该怪阈值**时看到怪物被打退一步。
- [ ] 验收（该轮记录，实玩回访见下条）：`cargo test` **579 passed / 0 failed**（基线 578 + 新增击退验收 1）、`tsc` 0、客户端 check **47/55**（8 项红灯与基线**逐项完全一致**）、装配器 PASS（`tms273-33`，`combat.damageNumbers` 四项齐全）。
- [ ] 未在真实客户端实玩验收；跳字观感（位置／上浮节奏／绿蓝是否与原版一致）与击退手感（50 px 是否合适）需实玩确认，**不宣称实玩通过**。另：魔心扣魔跳字的根因修复（判据改为「两根都为 0 才丢」）**需重新加载后复验红字 + 蓝字，以及绿字（喝药/椅子/魔力激发）**。
- [ ] 怪物追击 / 仇恨系统：确认怪会主动追人、脱战会回归。

### 05 · 掉落与拾取　〔总纲：主干已实现〕

总纲口径：掉落、归属保护、手动／宠物拾取、奖励认领与入包；**目录数量不计进度**。
建议落点：扩奖励来源时复用资产事务与 `A/notebook.rs` 留档。

本仓推进：道具权威世界模型增量 3 新增 `PickupSink`（`Inventory`／`MonsterBookCard`／`Mesos`），
「**只有 `Inventory` 会拒绝**」是「拒绝后必须把 `drops.active` 恢复成 1」的唯一依据。待办见 §12。

### 06 · 死亡与复活　〔总纲：基础已实现〕

总纲口径：死亡、惩罚、复活落点、状态恢复、去重；**不含**原创墓碑留存／虚影演化
（总纲明示该原创扩展另立项，不计入原版缺口）。原版语义本仓零改动：死亡判定与惩罚仍在
`monsters.rs::commit_incoming_damage`，复活落点与状态恢复仍在 `revive.rs::complete_revive`。
原创扩展见 §4.2。

### 07 · 职业与技能成长　〔总纲：部分实现〕

总纲口径：经验、AP／SP、初心者→法师→冰雷四转及部分 Hyper；其他职业未接入。
建议落点：先选一个新职业；确有独立规则再增 `B/<职业域>.rs`。

**本仓已超出总纲档位**（待总纲下一版固化）：

- 技能目录 **35 本 / 504 条**（探险家四条职业线）；`shared/mage-skills.json` **11 本 / 153 条**（法师）。
- **一转四职业岔道菜单**（2026-09-22 第十一轮）：`選擇岔道` 从单选扩为四选；装配器由一张 `FIRST_JOBS` 表
  `flatMap` 生成选项与 `advance-*`/`advanced-*` 节点，**不再四条手写分支**；门槛按线派生
  （`auth::first_job_level` 法师 8／物理线 10），发放走同一份 `auth::grant_first_job_fields`。
- **转职链**：`shared/job-advance.json` **30 条**（法师三分支 9 + 战士／弓／侠各 7）。
- **书准入单一权威**（2026-09-22 第七轮）：三处运行期闸（`world.rs::skill_job_allowed`、
  `auth/skills.rs::job_allowed`、`auth/db.rs::grant_level_sp`）收口成 `mage.rs` 派生表；
  客户端四份副本收成 `player/input.ts::BOOK_JOBS`（技能窗／HUD／键盘层共读）。
- 源技能名走**双根查找**（两份 `WZ_JSON_TW` 互补）；新书登记需**两层次分卷**。
- **主动技能白名单镜像同步**（2026-09-24）：`client/src/features/skills/view.ts::ACTIVE_SKILLS` 补入
  `'1121015'`，与服务端 `world.rs::PHYSICAL_AREA_ATTACKS` 保持镜像（门禁 `§3f` **双向断言逐项相同**）。
  技能目录**仍是 35 本 / 504 条**、转职链**仍 30 条**——本轮是执行链接线 + `time` 语义收口，**不改书准入与转职口径**。
- **技能轉換 `2321054 復仇天使` 接执行链**（2026-09-24 第八轮，零 `shared/**` 字节）：法师 212／232
  四转「尚未开放施放」由 **4 条降到 3 条**（`2321054` 移出；`MAGE_BRANCH_PENDING` 由源自动重算 ⇒ 它同时是判据生效的证据）。
  转换态**从技能表派生**（`world.rs::transform_done` ＝「復仇那几本在技能表里」）⇒ **零新状态位**、随存档持久、
  天然幂等（持久层 `commit_skill_transform` 用同一条件做 CAS）。四对配对与**顺序**从 `2321054` 的**帮助文本**
  （`String/Skill.img` 的 `h`：「…各別轉換成…」逐字写出成员与顺序）**独立重算**，
  ⚠️ **不是**「按目录键顺序按位配对」（导出树的键对整数样 id 是数字升序，按位配对会**配错两对**）；
  形态标记（`#c[慈愛]#`／`#c[復仇]#`，各恰好 4 条，**必须带方括号**）只判成员资格，两路互证。
  准入**按形态反转**（復仇侧解 `skill_hidden` 的唯一例外；慈愛侧新增拒码 `skill_transformed`），
  **两个方向都有验收断言**（只测一侧的话，把闸门整个删掉也能过一半）。
  `[被動效果]` 三项按「学得即生效」口径消费：`madX`→`MASTER_MAGIC_SKILLS`（属性层、带留痕）、
  `mdR`→`ELEMENTAL_RESET_SKILLS`（独立乘算）、`ignoreMobpdpR`→累加进无视防御那一格；
  同句里的「攻擊屬性耐性（`u`／`x`）」**没有消费点** ⇒ 不消费。
  **技能目录仍是 35 本 / 504 条、转职链仍 30 条**——本轮是执行链接线 + 一对多映射，**不改书准入与转职口径**。

- [ ] 统一加载实玩：四条线的一转、以及其二/三/四转的等级门槛（30/60/100）需实际游玩确认。
- [ ] 三条源剧情任务 `1401`/`1403`/`1404` **仍不可执行**（本轮走菜单旁路，未覆盖它们）；将来要接需先在 `import_tms273.py` 侧补齐 NPC 与脚本，再删 `PREREQ_LESS_ALLOWED` 对应登记。
- [ ] `scripts/_probe_skill_outlinks.cjs` / `.json` 保持**未跟踪**（是分卷映射的可重跑证据，同目录的 `_probe_canvas_volumes.*` 已入库）；是否入库由提交时决定。（两处同类条目，此处合并为一条。）
- [ ] 经 `启动3010.command` 统一加载后实玩：战士／飞侠号**学技能 → 加点 → 放技能**走一遍，并核看 35 本书的技能窗页签（页签按转职层级共用下标 0..6，源 `UIWindow2` 只有 7 组图）。
- [ ] 遗留（本轮如实登记，**不是本轮引入**）：① ~~战士／弓箭手／飞侠的**二转以上主动技能全部未接执行链**~~ —— **第九轮已做完**（25 条进 `PHYSICAL_AREA_ATTACKS`，见 §04）；② 五条一转机制缺席技能未接，见 §04 同名条目；③ ~~**转职任务未补**~~ —— **第九轮已做完**（`shared/job-advance.json` 8 → 30 条）；④ 上一轮的两条遗留原样保留：同格的另两条法师分支副本仍「尚未开放施放」、**召唤实体仍未实现**。
- [ ] 通过 `启动3010.command` 统一加载候选 `20260914115820-a031a887`（协议 23 / 内容 `tms273-24`）后，核验 8 级法师转职、初心者技能继续使用且不强制学习魔灵弹；在线服务、数据库和机器人本轮未动。
- [ ] 魔力波動二段简化（下+跳 → 跳）：确认起跳条件已放宽。
- [ ] 经 `启动3010.command` 统一加载后实玩：法师四转技能页签、SP 与加点（含本分支 SP 归属；改前三道闸只认冰雷 ⇒ 火毒／主教升级点全发进 `200` 那本）。
- [ ] **名号待修**：源书名是 `魔導士之路(火毒)`／`最頂級火毒魔法`，而现网 `job-211`／`job-221` 标题已叫「大魔導士」——**三转被叫成了四转的名号**。

### 08 · 属性、Buff、异常状态　〔总纲：部分实现〕

总纲口径：属性聚合、伤害修正、Buff 生命周期和五类疾病已接入；黑暗、虚弱、诱惑、尸化、禁药等未建模。
建议落点：在**现有属性／状态权威内**补规则，**不再维护第二套面板算法**。

**本仓已超出**：三块权威模块已收口 —— `attribute.rs`（唯一聚合入口 `aggregate_attributes`，
层序＝能力值→被动→增益→装备，取整只有楓葉祝福那一次）、`damage.rs`（`DamagePipeline`，
分组只按源字段名判定且只有一处，`resolve()` 用 `i128` 有理数累乘 ⇒ 与施加顺序无关）、
`player_status.rs`（唯一时钟与穷尽 `Expiry`，**「不能只有一串前端剩余秒数」**）。
四个未建模疾病（121 黑暗／122 虛弱／133 屍化／134 禁藥／137 冰冻）**刻意不实现**，理由写进门禁。

- [ ] 魔心防禦（2001002）改为“99% 转 MP × 逐级抵偿率 100→80，差额由护盾消解”：确认 1 级与 2 级起的差异符合指定规则。
- [ ] 实玩回访：Buff 与跳字表现（魔心扣魔跳字等）在原版口径下的一致性。

### 09 · NPC 与对话　〔总纲：部分实现〕

总纲口径：点击、原文分页、任务菜单、商店／仓库／转职／部分传送；完整脚本能力未覆盖。
建议落点：按真实动作扩 DSL 或专属职能，**不能只补台词**。

**本仓已超出**：阶段一（点击响应 + 预留接口）、阶段二（源台词 + 对接任务）、传送类 NPC 实装**均已完成**。
实测 **233/265** 个模板有源台词共 **553** 行（111 来自 `info/speak`、122 回落 `d*`/`n*`）；
余 **32** 个物件型条目源里确实一句话都没有 ⇒ **不进表、不补默认值凑数**。
`shared/npc-scripts.json` 計程車（`1012000` 維多利亞計程車）**1 条真转换**，128 条拒绝理由逐条登记
（121 缺实体、5 模式不支持、2 目的地未装配），**不造替代**。
边界：**en locale 下这批源台词没有译文**（源是繁体中文，客户端只在 `zh` 下做 tw→cn 转换），不补＝不编造。

### 10 · 任务与剧情　〔总纲：部分实现〕

总纲口径：接交、前置、持有／装备／击杀目标、事务奖励、日志追踪；**通用脚本计数、完整过场和后续主线仍有缺口**。
建议落点：需要场景执行时增 `B/quest_scene.rs`，**进度仍归任务域**。

本仓推进：任务进度与最小场景执行（T03：`quest_kills` 表 + `questService` + 36315 转可执行）、
`infoex` 语义核定（T05 收口）、续章路径阻塞可解释（新增 `blocked` 日志状态 + 击杀目标指路）。
主线后续章节：55 条未复刻任务已按 TMS273.7 `Quest.wz` 补齐运行时规格，
**可执行 2 条**（36316/36332），其余登记 `blockedBy`。

- [ ] **T03 任务进度与最小场景执行**（实现、定向验收与全套回归完成，**待统一加载实玩**）：见[交付记录](../history/2026-09-14/任务进度与最小场景执行.md)。待验：`Q` 打开任务日志应看到 36315 可接取并带「接取任務」；接取后打死藍色蘑菇王 → 进度 1/1 → 完成得 600 EXP → 重登保留；36316 在日志里**不应**出现接取按钮。
- [ ] 经 `启动3010.command` 统一加载后实玩：确认任务目录与赫麗娜/德斯的菜单在有前置时不出现异常项。
- [ ] 解锁路径待定（**需用户选定其一或全部**）：恢复 q36315/q36331 脚本入口或给出显式 P 承接 NPC；补齐艾靈森林 area 39 地图与 2131xxx/2133xxx NPC；实现任务击杀进度以解 `mob-progress` 的 4 条。
- [ ] 1402 法師之路「无法接取」（见[交付记录](../history/2026-09-13/法师之路无法接取修复.md)）：确认在選擇岔道点汉斯出现「前往魔法森林圖書館接取：法師之路」并能送达 `101000003`，任务在图书馆正常接取；同时确认已接取后回来点汉斯，法师训练选项照常。

### 11 · 交通与旅行　〔总纲：部分实现〕

总纲口径：传送门、回城卷、计程车、飞船与巴洛古袭击已接入；票务／航程部分为项目适配。
建议落点：复用交通与事件模块；**已有袭击不能冒充精英事件**。

**本仓已超出**：飞行船航线系统**三期全部收口**——
一期（維多利亞樹木站台 ⇄ 天空之城闭环）；二期（耶雷弗线 + 埃德爾斯坦线）；
三期（甲板巴洛古袭击事件、候船室 `station_in` 授权分流修正、耶雷弗城 + 埃德爾斯坦城内 NPC 职能与脚本门）。
勇士部落路线 `102030100`（火焰之地）已放行（内容 `tms273-34 → tms273-35`）。
候船室 `200000112`／阿霖 `2012002` **核定为原版不可达 ⇒ 不做、不补门**；票务道具核定为源里没有同版船票 ⇒ P 级免费登船作为**已知偏离**登记。

- [ ] 通过 `启动3010.command` 统一加载后实玩：从勇士部落走到 `102030100`（火焰之地）确认不再报「尚未收录」。
- [ ] 经 `启动3010.command` 统一加载（协议 19→20→21 前后端必须一起换）后实玩：一期检票登船、航行到站、对向返程、班次播报、死亡复活落点；二期大厅 `in01`→耶雷弗站台→`130090000`→天空渡口→耶雷弗前庭/城闭环，埃德爾斯坦线树顶站台⇄码头⇄城闭环、舱门相位闸与 move 门互切。
- [ ] 六條岔道树顶传送阵与未装配目标提示（见[交付记录](../history/2026-09-13/六条岔道树顶传送阵与未装配目标提示.md)）：确认沿树干长绳上爬不再反复弹 `传送失败：map_unavailable`；确认右侧中层平台 `eli00` → 森林的起始點 → 閃爍森林的通路 → 魔法森林 一路照常。
- [ ] 「傳送陣多一個」修复（同[交付记录](../history/2026-09-13/六条岔道树顶传送阵与未装配目标提示.md)）：光束改为只给客户端真会画的类型（`pt: 2` 可见、`pt: 7` 腳本门 → `pv`），碰撞门 `pt: 3` 不再发光 ⇒ 刷新页面后确认树顶两道叠光柱消失、爬绳到顶仍能进 `104020100`，同時确认楓之港 `east00`/弓箭手村 `Achter00` 等腳本门的光束照常。
- [ ] 已补装配 4 张站台图（装配目录 44→48：`104020100 維多利亞樹木站台` + `104020110`/`104020120`/`104020130`）：**需先在自己终端跑 `zsh 启动3010.command`（客户端+服务端要一起换；服务端只在启动时读 `shared/maps.json`）**，确认树顶 `top00`/`top01` 能进站台大厅、大厅 `under00..under10` 能原路回六條岔道，`in00`→天空之城站台、`in03`→埃德爾斯坦站台可进出；其余仍未装配的门（如 `310040200/side00`）保持「走近提示一次」的旧行为。上游飞行船航线（天空之城/耶雷弗/埃德爾斯坦）与 `in01` 的 `inERShip` 脚本体需另行立项。
- [ ] 传送类消耗品（回城卷軸 / 定点城镇卷軸）：确认使用后落点正确。（維多利亞港三家商店的传送验收同类，见 §14。）

### 12 · 背包与道具　〔总纲：主干已实现〕

总纲口径：分栏、堆叠、拖动、整理、丢弃、扩容、药水／卷轴；**设置栏存在但椅子使用未接入**（椅子见 §31）。
建议落点：特殊道具按行为分派；**不要在通用使用函数堆所有玩法**。
依据：[`rust_authoritative_world_model.md`](../../technical/rust_authoritative_world_model.md)、
[物品系统审查全文](../../technical/物品系统审查与权威世界模型改造.md)。

本仓推进：**道具系统 → 权威世界模型改造**增量 ①–④ 已落地（行为零变化／无玩家可见变化）：
① 事实词汇 `auth/item_world.rs`（`Owner`／`ItemLocation`／`ContainerSide`／`ItemState`）+ 仓库搬运收敛 `move_stack` + 拾取权判定改 `drop_fact`+`pickup_allowed`；
② 容器拒绝原因类型化为 `MoveRefusal`（措辞收敛到 `StorageOperation::refusal_code` 唯一翻译点）；
③ 商店购买路径失败边界测试 8 条 + **修掉重复扣款**（`handle_shop_buy` 原缺 `requestId` 幂等账本）；
④ 收窄 `pub` 面 + `write_inventory_tx` 按差集最小写入 + 拆除生产外观 `Store::write_inventory`。
另：勇士部落路线顺带回填 **157 件**道具定义（`items.json` 3710 / 商店行 1736），并新增防回潮门禁。
已论证**不纳入**的：增量 2（装备栏纳入 Move）、增量 3 原范围（`ItemLocation::Ground`）。

- [ ] 道具背包五页签复刻 + 背包扩展券：确认页签、扩展券购买与容量生效。
- [ ] **（论证存档）增量 3 的原范围说明 —— 已按上述收窄范围执行完毕，保留供后人核对**：原假设是 `ItemLocation::Ground` + `GroundSource` + `pickup` 走 `move_stack`，**已否决**（同一个 `add_inventory_tx` 内层业务码，`storage_transfer` 用 `?` 当硬错误中止事务，`pickup` 用 `match Ok(Err(code))` 当业务拒绝并恢复掉落 ⇒ 两边语义相反，共享 `Destination` 必然改写其中一边）。收窄后可落地范围＝给「拾取去处」建模（`PickupSink`），并把三个分支的 SQL 搬回 `auth/db.rs`；**不做** `world.drop_owners` 去镜像（性能决策，需单独论证）；丢弃方向的「取出后直接消失」按词汇是**销毁**而非 Move，应另立领域动词。
- [ ] **增量 5｜仅在立项时建**（**阻塞于用户决策**）：`TransferOwnership` / `Escrow` / `Lock(Auction|Trade|Contract)` / `Reward` / `MailAttachment` / `DomainEvent` —— 当前**零消费者**（项目无邮件／拍卖／玩家交易／挂机收益）。提前建会违反「不提前建空目录或通用工厂」的规范，也会把 LSP 边界重新搅乱。
- [ ] 超大文件治理第一、二步落地（`messaging.rs` 542 行 / `inventory_ops.rs` 1,444 行搬出）：重点确认聊天、密语、表情、拾取、背包整理/移动/丢弃、用道具、丢金币路径无退化。

### 13 · 装备与卷轴强化　〔总纲：部分实现〕

总纲口径：穿脱、准入校验、实例属性、卷轴概率／次数；**星力、潜能、附加潜能、方块未接入**。
建议落点（总纲）：`F/enhancement/`；`B/enhancement.rs`；`A/enhancement.rs`；**保留现有装备实例**。
本仓**未立项**；与 §12 增量 5 的 `Escrow`/`Lock` 无关，不混用。

### 14 · NPC 商店与仓库　〔总纲：基础已实现〕

总纲口径：买卖赎回、物品／金币存取与事务；**复杂实例全路径保真仍需核验**。
建议落点：优先修现有往返链；**不新建重复库存**。

本仓推进：普通商店购买／赎回事务化（NB-04：三个单事务提交入口 + 事务内双写）；
維多利亞港三家店（41→44 图）、魔法森林两家店（48→50 图）已装配。

- [ ] 魔法森林两家商店传送阵补全（装配目录 48→50：`101000001 魔法森林武器/防具商店` + `101000002 魔法森林雜貨店`）：**同样需在自己终端跑 `zsh 启动3010.command` 一起换前后端**（内容 `tms273-13`），确认魔法森林 `in00`/`in01` 能进两家店、店内 `out00`/`out01` 能原路返回，三位商人（`1031000 妖精 佛羅拉` / `1031001 妖精 舍拉比` / `1031100 妖精 蓮`）对话能开店并买卖，`2030002 魔法森林卷軸` 双击落回 `101000000`；`9062704` 按源 `Npc.info.hide` 不出现。
- [ ] 維多利亞港三家商店传送修复（装配目录 41→44 图）：确认三家店都能进。
### 15 · 现金商城　〔总纲：部分实现〕

总纲口径：展示试穿、购买、限购、租赁及购买持久去重；**赠送、愿望单、里程、兑换券、退款未接入**。
建议落点：按子功能扩现有商城；**真实支付另立项**。

本仓推进：現金商店核心链路（8 分类页签、商品网格、购物车结算、楓點余额与 GM `/cash`；导出 1452 条在售商品／1349 图标）、
购买事务化（T02：持久请求回执／持久余额／限购原子化）、UI 窗口规范接入（标题拖动／点击置顶／触屏命中层／缩窗边界）、
点击与旧角色预览根因修复、与实装纸娃娃修复。

- [ ] **T02 现金商店购买事务化**（实现与定向验收完成，**待统一加载实玩**）：见[交付记录](../history/2026-09-14/现金商店购买事务化.md)。待验：`/cash 1000` → 商店购买 → 刷新重登余额与道具仍在；**重启后**余额、已购道具、限购计数均保留；余额不足／超限购被拒且不扣款。
- [ ] 协议 17→18，客户端与服务端必须一起换：通过 `启动3010.command` 统一加载后实玩（开窗余额、加购结算、`/cash` 充值、重登保留）。
- [ ] 通过 `启动3010.command` 统一加载协议 19 / 内容 `tms273-18`，核看现金商店分类、扩展券/宠物/时装、余额与结算。
- [ ] 使用现有角色验证试穿、实际穿脱、移动/施法、重登与跨图时装绑定，以及扩展券使用/宠物召唤。在线服务与存档未重置，无独立 QA。
- [ ] 经 `启动3010.command` 统一加载候选 `20260913132429-628f3130`（协议 19 / 内容 `tms273-19`）后刷新网页，确认点击/试穿/购买。旧版 tms273-18 的待验商店条目由本条覆盖。
- [ ] 关闭商店后聊天框 `/cash 1000`，再开商店测试星星拖鞋/褐色小貓/装备栏扩展券；确认扣点、入包、使用及重登保留。定向检查和候选构建通过，在线服务未重启。
- [ ] 经 `启动3010.command` 统一加载更新后的 `tms273-19` 候选并刷新，确认标题拖动、内容滚动/按钮点击不拖窗、尺寸变化不出界及叠放行为；在线服务未重启。

### 16–18 · 玩家交易 / 拍卖·寄售 / 邮件　〔总纲：未接入〕

总纲口径：**NPC 交易已做（见 §14），但玩家双边锁定、确认、资产交换未形成流程**；
无挂牌、撤单、成交与领取流程；无收发件、附件托管与领取流程。
⚠️ **不是 `B/trade.rs` 已完成** —— 它只服务 NPC 商店与仓库。
总纲建议落点：`F/player-trade/`、`B/player_trade.rs`、`A/player_trade.rs`；
`F/auction/`、`B/auction.rs`、`A/auction.rs`；
`F/mail/`、`B/mail.rs`、`A/mail.rs`（附件领取须复用入包与图鉴留档）。

本仓**未立项**；与 §12 增量 5 是**同一决策**（无消费者则不建托管与锁定抽象）。

### 19 · 组队与好友　〔总纲：部分实现〕

总纲口径：组队生命周期、同图经验共享、好友／黑名单已接入；**队伍按现行设计不跨重启保存**。
建议落点：复用成员与经验规则；正式组队目标见总纲 22。

- [ ] 好友与黑名单：确认增删、在线状态与黑名单屏蔽。

### 20 · 公会与联盟　〔总纲：未接入〕

总纲口径：无完整创建、成员权限、公会存档及联盟流程。
⚠️ **已有社交、认证与消息基础；不是公会实现。**
建议落点：`F/guild/`、`B/guild.rs`、`A/guild.rs`；联盟等有实际需求再扩。本仓未立项。

### 21 · 聊天　〔总纲：部分实现〕

总纲口径：地图公聊、密语、表情、限流与屏蔽；**队伍／公会频道、跨频道／跨服与离线投递未接入**。
建议落点：先扩 `messaging.rs` 的频道授权；真实跨节点时再建消息路由模块。

- [ ] 聊天表情（表情貼圖）与悄悄話（私聊）：确认发送、频控提示与头顶气泡表现。

### 22 · Boss 与副本　〔总纲：部分实现〕

总纲口径：单人练习、Boss 行为、胜败／离开／重试；**正式奖励／次数和原版组队通关链未完成**。
建议落点：先在 `B/boss.rs` 扩正式模式，奖励／资格增 `A/boss.rs`；实例共性出现后再抽取。

### 23 · 宠物　〔总纲：部分实现〕

总纲口径：三宠、跟随、拾取、喂食、饱足度、亲密度、等级与寿命；**生命水复活未接入**，
装备／命令／自动补药该轮未专项核实。⚠️ **未专项核实项不要直接当作空白重写。**

**本仓已超出**：宠物持久化与三宠伙伴重构（现金宠物持久化、1～3 实例伙伴、跨图常驻、
独立变速／千码回归／双速寻物、原图宠物详情窗与菜单入口），候选 `20260913094539-37c612d9`。

- [ ] 通过 `启动3010.command` 统一加载后实玩：三宠与重连恢复、传送/卷轴/任务/活动常驻、犬类前跑、复杂地形寻物与掉落保护。
- [ ] GM /add 与宠物：旧 MVP 已由「宠物重构」覆盖，最新为协议 17 / 内容 `tms273-15`；统一加载后核验现金道具和召唤状态恢复。**饥饿/成长与 GM 权限体系仍属后续立项。**

### 24 · 冒险笔记／图鉴　〔总纲：部分实现〕

总纲口径：物品获得留档、四页窗口与查询已接入；**怪物登记、收藏奖励／勋章、探险仍被阻塞**。
建议落点：继续 NB-06／08；**复用当前事实与查询，不新造图鉴**。
方案：[`MapleStory_冒险笔记图鉴_怪物收集复刻与扩展计划_f62ef983.md`](MapleStory_冒险笔记图鉴_怪物收集复刻与扩展计划_f62ef983.md)。

**本仓已超出**：NB-00~05 全部落地（基线核定、契约冻结、资源与目录导出、核心事实与版本/历史补记、
普通商店购买/赎回事务化、全物品授予入口接入 11 处）+ **NB-07 单入口＋四页窗口＋私有查询已落地**
（含「页签点不到」与「登记未核定被当成错误」两处修复、旧菜单可见改名）。
四张定义表：`notebook-catalog`（物品）+ `mounts`（935）+ `chairs`（2799）；
分区判据**双向**核（`mounts + saddles` 键集 == `mounts.json` 键集 + 逐件槽位）。
隐私与生命周期：共享 host 只 `append`；过期响应整份丢弃；目录版本错配**拒绝排版**；
任务页的基集合就是已获得集合（未获得的条目从基集合里就不存在）。

- [ ] **NB-06 原版怪物登记链路**（`collection_rules.rs`＋死亡结算事务内；**阻塞于登记概率／资格门核定**——同版源不含登记概率与资格门，**不能编一个数值**）。
- [ ] **NB-08 奖励、勋章与探险**（**规则未核定部分显式阻塞**；窗口里对应区域不出控件）。
- [ ] **NB-09 定向检查与整合**。

### 25–26 · 五／六转与高阶成长 / 账号横向成长　〔总纲：未接入〕

总纲口径：**已有 Hyper 技能 ≠ Hyper 属性、V 矩阵、HEXA 或成长符号**；
多角色已做，但战地联盟、传授技能未形成账号成长链。
建议落点：按立项分别增 `B/hyper_stats.rs`、`v_matrix.rs`、`hexa.rs`、`symbols.rs` 与对应 `F/`、`A/`；
账号成长增 `F/account-growth/`、`B/account_growth.rs`、`A/account_growth.rs`。本仓未立项。

### 27 · 普通采集物／Reactor ★　〔总纲：基础已实现〕

总纲口径：攻击／区域交互、状态推进、耗尽、刷新、部分掉落；**不是专业采集**。
`ReactorPlacement` 明确持有交互类型、状态数、刷新时间与掉落表，`ReactorInstance` 持有当前状态与刷新截止点，
掉落复用 `roll_drop_specs`。建议落点：复用原链；**若专业采集确需共享，再将现有 Reactor 职责抽为
`B/reactors.rs`，不是从零重做**。

### 28–29 · 专业采集 / 制作·炼金·分解 ★　〔总纲：未接入〕

总纲口径：无专业学习／资格／熟练度，无配方执行、材料与产物结算、制造成长、装备分解链。
⚠️ **「能扣材料、能发道具」不能推定完成** —— 通用背包事务只是原语，当前实际使用分支主要为
装备穿脱／扩容／回复／卷轴／传送；**商店买卖和卷轴不是制作**。
建议落点：`F/profession/`、`B/profession.rs`、`harvesting.rs`、`crafting.rs`、`A/profession.rs`、
`D/professions.json`、`recipes.json`（**分类用数据，不每配方一个函数**）。本仓未立项。

### 30 · 坐骑 ★　〔总纲：仅装备层基础〕

总纲口径：已有 `Tm/Sd → -18/-19` 槽映射；**骑乘切换、骑乘状态、移动与骑乘表现未接入**；
装备层兼容**不证明骑乘**。建议落点：`F/mounts/`、`B/mounts.rs`、`D/mounts.json`；既有物品持有／穿脱不重写。

**本仓已超出**：骑宠快捷键 `KeyR`（`STORAGE_VERSION` 升 2 + 旧档迁移；**不占快捷栏格子**——源底板固定 2×16 = 32 格）、
装备窗骑宠格（`mountedItemId()` 区分「装着」与「骑乘中」，`slotsSignature` 加 `mounted`）、
**鞍具作为骑宠页子页**（源 `info.islot = Sd` 那一半，**26 件**；在服务端是独立分区 ⇒「一件只属于一页」不变式三处仍成立）、
骑宠页签 935 件、全部骑宠缺显根因修复。
调研结论：**骑乘不需要马鞍**（`is_mount_item` 只看 `Tm` 槽的 `tamingMob`，与 `Sd` 槽无关）。

- [ ] 经 `启动3010.command` 统一加载后实玩（协议与目录都要一起换）：按 `KeyR` 上下马；无骑宠时按 `KeyR` 应看到提示；装备窗骑宠格骑乘中高亮并显示「骑宠·骑乘中」；冒险笔记骑宠页下切「鞍具」列出 26 件、每格有配置 ID 与图标、详情报「佩戴等级」且**不报**「坐骑档」。
- [ ] 验收（该轮记录）：`cargo test` **578 passed**、`tsc` 0、客户端 check **47/55**、笔记本 UI 门禁 PASS（新增鞍具子页样张）、快捷键 UI 门禁 PASS。8 项红灯**全部为既有**。
- [ ] 未在真实客户端实玩验收；本轮只有离线组件检查、无头浏览器几何/对比度门禁与 `cargo test`，**不宣称实玩通过**。
- [ ] 经 `启动3010.command` 统一加载后实玩：第五个页签「骑宠」列出 935 件、物品格左上角配置 ID 不被「本版本未开放」牌子压住、骑宠详情报坐骑档、GM 发放骑宠后该格转已获得。

### 31 · 椅子／坐下 ★　〔总纲：未接入〕

总纲口径：设置栏可容纳道具；**当前通用使用拒绝该栏**（非装备、非消耗品在持久化使用函数中落入
`InvalidInventoryType`），角色动作集合没有坐姿；⚠️ **不能把纸娃娃可能存在的坐姿素材、
设置栏图标算成玩家能坐下**。建议落点：`F/chairs/`、`B/chairs.rs`、`D/chairs.json`；
持有复用背包，**坐姿先作会话状态**。

**本仓已超出**：`shared/chairs.json` **2799 件**（`Item/Install/0301*`、`0302`），
第六个页签「椅子」已落地（默认「全部」——2799 件里真进商店的是个位数。详情额外报恢复量；
**间隔缺席＝源未核定，不替源编一个 10 秒**）；顺带修掉物品树里唯一那件椅子孤挂在
客户端根本没有的 `sections.setup` 上（整族归椅子页后 `sections.setup` 变空，分区仍「无重叠全覆盖」）。
对账：椅子 2799 件 = 2683 verified + 116 missing。

- [ ] 经 `启动3010.command` 统一加载后实玩：第六个页签「椅子」列出 2799 件、每格配置 ID、详情报恢复量、GM `/add 3010000 1` 后该格转已获得。
- [ ] **116 件 `03019` 椅子仍无图标**（**如实登记，非本次引入**）：根因是本地 TMS273 包 `Item/Install/_Canvas/_Canvas_003.wz` **缺卷**；83 侧（v83）没有这些 id，补不了。需拿完整客户端包补分卷后重跑椅子图标导出，或明确接受这 116 件无图。

### 32 · 狩猎符文 ★　〔总纲：未接入〕

总纲口径：未见地图生成、交互／解谜、资格冷却、效果授予与结束链；**不指五／六转成长符号**。
建议落点：`F/runes/`、`B/runes.rs`、`D/runes.json`；需持久冷却／认领时再增 `A/runes.rs`（**不存无须持久化的动画**）。本仓未立项。

### 33 · 狩猎连击／多杀 ★　〔总纲：未接入〕

总纲口径：已有击杀事实、任务击杀计数与技能叠层；**没有狩猎连击计时、多杀归并及专属收益反馈**。
`mystic_strike_stacks` 属技能运行状态，**不是连击**。建议落点：`B/hunting.rs`、`F/hunting/`、`D/hunting.json`；
**统一接普攻与技能的已提交击杀，不可只在 `attacks.rs` 的普攻路径加计数**。本仓未立项。

### 34 · 精英怪／精英 Boss 事件 ★　〔总纲：未接入〕

总纲口径：无精英生成条件、事件状态、专属规则与结束奖励链；
**普通 Boss 标志、练习和飞船袭击不算**（巴洛古袭击已归 §11 交通事件，不计入本模块）。
建议落点：`B/elite.rs`、`F/elite/`、`D/elite.json`；发奖接 `A/loot.rs`，**不复制掉落系统**。本仓未立项。

### 35 · 成就 ★　〔总纲：未接入〕

总纲口径：**物品首次获得表不是成就完成表**；无独立条件聚合、完成状态、奖励认领与成就窗口。
事实来源是 `A/loot.rs`、`quests.rs`、`notebook.rs`，但**它们不是成就系统**。
建议落点：`F/achievements/`、`B/achievements.rs`、`A/achievements.rs`、`D/achievements.json`。本仓未立项。

### 36 · 称号 ★　〔总纲：未接入〕

总纲口径：无称号解锁、选择／卸下、到期与头顶展示链；**普通角色名字不是称号**。
建议落点：`F/titles/`、`B/titles.rs`、`A/titles.rs`、`D/titles.json`。本仓未立项。

### 37 · 勋章／徽章装备 ★　〔总纲：仅装备层基础〕

总纲口径：`Me → -49`、`Ba/Be → -50` 已有映射及通用穿脱／属性通道；
⚠️ **尚不能据此证明全部勋章可获取、可收藏或可重发**；图鉴的奖励／勋章操作仍明确拒绝。
建议落点：穿戴复用原链；专属获取／重发需要时增 `B/medals.rs`、`A/medals.rs`、`F/medals/`。
本仓未立项；受 §24 的 NB-08 阻塞。

### 38 · 联机与会话　〔总纲：主干已实现〕

总纲口径：权威同步、重连、后台保留／驻留、运动插值；**本地预测未实现，不等于已支持跨服**。
建议落点：优先验现有会话边界；跨节点另立项。

**本仓已超出**：网络运动插值（Net Motion）已实现 —— `client/src/features/net-motion/motion-interpolator.ts`
（纯逻辑、无 Phaser 依赖），每个 id 留 `prev`/`latest` 两份权威样本，用**按服务端 tick 速率前进的客户端时钟**
在两者之间取位置；**只插值 `x`/`y`**（动作／朝向／血量取最新样本）；自角色缓冲**半拍**、远端与怪物/宠物缓冲**一拍**；
时钟**只调速率不倒流**（`maxSlew < 1` ⇒ rate 恒正）、**不外推**（`clock ≤ latestTick`）；
瞬移/换图/断流**直接落点**（单拍 > 600 单位或间隔 > 6 拍）；判定不受影响（门/采集/拾取仍读权威坐标）。
**刻意不做**：本地预测、提高 Tick 频率、插值 NPC／掉落／采集物。
根因：服务端 `TICK_MS = 50`（20 Hz）推权威快照而浏览器按 60 fps 画，改前全仓 `interpolat|lerp|predict` 在 `client/src` 下**零命中**。

- [ ] **待实玩**：走动/跳跃/爬绳/游泳时角色与相机是否连续；换图/传送/复活落点是否干脆（无横穿地图的滑行）；怪物追击与宠物跟随是否平滑；原地不动时不该抖动；标签页切回后第一帧直接落点。

### 39 · Web 与桌面交付　〔总纲：部分实现〕

总纲口径：源码开发入口、缓存、按需加载、强制更新、Tauri 外壳；
**模块安全热替换、Three.js 接入和桌面完整交付未完成**。
建议落点：继续现有交付计划；**先修桌面资源来源／部署与打包验收，不另写游戏前端**。
主计划：[`MapleStory_Web_Cache_Force_Update_Tauri_Plan_v3_2026-09-17.md`](MapleStory_Web_Cache_Force_Update_Tauri_Plan_v3_2026-09-17.md)。

**本仓已超出（按 V3 阶段编号）**：

- **V3-P0/P1 源码开发入口**：`启动3010.command` 新增 `dev` / `status` / `stop dev`，
  **无参数仍是原来的发布式启动**；Vite 固定 `127.0.0.1:5173` + `strictPort`；页面身份 `__CODE_MODE__`。
- **V3-P2a 资源修订与缓存分类**：`client_delivery.rs` 的 `/api/client-release`（`no-store`）与缓存分类中间件；
  `resource-url.ts` 逻辑 key 与下载地址分离；端点收敛 `endpoints.ts`。
- **V3-P2b 首页强制更新与真实下载面板**：`update-service.ts` 状态机 + `desktop-downloads.ts` + `view.ts`。
- **V3-P2c 内容寻址对象库 ⇒ 强缓存真正生效**：`index_client_assets.cjs`（闭包 → sha256 → 对象库 → 不可变索引 →
  固定名指针，**最后写指针＝唯一发布动作**）；闭包名单改为**自动发现**（原手写 6 项清单漏掉 57% 资源 ⇒
  闭包 28,065 → 69,692、对象 27,928 → 67,582）。预压缩 `publishPrecompressed`（门槛 256KB，写 `.br`+`.gz`）+
  `ServeDir::precompressed_br().precompressed_gzip()`（**刻意不引入 `tower-http` 的 compression feature**）+
  `Vary: accept-encoding`。实测整轮：**冷 38.6MB / 2.3s → 刷新 3.8KB / 1.4s（0.0%）**
  （接入前为冷 422MB / 26.1s、刷新 417MB / 25.9s、99.2%）。
- **根因收口**：那 26 秒**从来不是缓存问题**，是「整份 manifest 全量预加载」——进入游戏实测联网
  23,518 个对象／387MB／26.1s，而同机服务端能跑 6,000 req/s ⇒ 瓶颈在**客户端逐张建纹理**。
  两处修：① `imageLoadType: 'HTMLImageElement'`（26.1s → 15.6s）；② `lazy-texture.ts::ensureTextures`
  （**返回布尔＝本帧能不能画**），首屏只入队当前地图+本角色+战斗+界面。
- **页面陈旧自愈**（2026-09-21 起三轮）：`version-heal.ts` + 四重护栏防重载环；
  「强制更新」契约修正（`apply()` 改为**只要拿到发布描述就导航**）；`entry/view.ts` 构造时静默核对一次发布。
- **V3-D0 桌面打包脚本与环境**：`client/src-tauri/` Tauri 2 薄壳、`build-desktop.cjs`（独立暂存区、
  **显式拒绝写 `build/current`**）、`make_desktop_icon.cjs`、`.github/workflows/desktop-release.yml`。
- ⚠️ **跑启动前必须先物化 iCloud 占位文件**（否则 cargo/vite/门禁会**永久挂死**——不报错、只是不动）。
  当前实况＝**全仓 0 个占位**（2026-09-22 实测）。别用「找 `*.icloud` 文件」当探针——现代 iCloud 占位
  **保留真实文件名**，只有 `blocks === 0` 才认得出。

- [ ] **待用户重跑一次 `启动3010.command`**：页面陈旧自愈的修复要到达用户必须发布新 bundle（`releaseId` 变化）。本轮未跑——沙箱读不了 macOS 进程表，`activate` 阶段会以「无法检查 macOS 进程表，拒绝切换」中止。
- [ ] 重跑后，把**已经打开的老标签页**留在那里验证自愈：登录一次应当**自动**跳到当前发布，不再需要手动 F5；二次登录则应稳定停在原文案（防重载环生效）。
- [ ] **那张旧标签页手动刷新一次即可**（拿到含自愈代码的新包）；此后每次服务端换代都会自动收敛，**不再需要手动刷新**。（自证方法**不要再按主包哈希认**——包名随每次发布变化；可靠做法就是在报错页按一次 `Cmd+Shift+R`。）
- [ ] 本轮的「强制更新」修复**尚未在线上**（写记录时线上是 `20260922013333-c38b3d6d`，本地 09:33 构建，含上一轮的「载入即自愈」但不含本轮）⇒ 需再跑一次 `启动3010.command`。
- [ ] 本轮改动**要生效仍需重跑一次 `启动3010.command`**（本轮未跑，不打扰正在运行的服务）。
- [ ] 未做（明确交待）：**V3-P3 的「按图加载」只做了「按需装载」，没做「分块/分级」**。计划要求「缩小集合后必须有可靠的动态补齐路径、换图代数与资源释放」，不能只删掉一个循环。当前 `buildPreloadPlan` 仍是全量全集（行为未变），本轮未动。另：**「刷新不再传字节」≠「刷新不再解码」**——每张图的解码与建纹理在内存里，与 HTTP 缓存无关；当前做法保证「没画的就不解码」，但没有对**同一屏内的图**做 LOD 或分帧上限。
- [ ] **未验**：`tauri build` 的完整原生打包与真安装包在干净机器（无 Vite、无本机 3010）启动——D0 验收项。
- [x] ~~**D0 真实阻塞**：桌面 `frontendDist` 只含应用代码，内容走远端 `assetBase`，而 `check_tms273_client_actions.cjs` 的 `UNCOVERED` 名单里**还有 22 个 DOM `<img>.src=frame.url` 站点**未接 `resolveAssetUrl`（HUD/背包/聊天/图鉴/小地图/世界地图/组队/好友/角色/宠物/NPC 头像/仓库/商城）。Web 下它们只是不参与强缓存，**桌面下会 404** ⇒ 需要一次机械迁移，名单已在门禁里（新增会自动失败）。~~
  **2026-09-23 已交付**（机械迁移完成）：22 个文件 **74 处**赋值全部接入 resolver，`UNCOVERED` **清空**、`RESOLVER_CONSUMERS` **11 → 30**；顺带修掉三处「拿逻辑地址比 `<img>.src`」的守卫失效（`world/minimap-view.ts` ×2、`pet/panel.ts` ×1）与一处门禁正则覆盖不到的同类站点（`pet/panel.ts::setArt` 的 `setAttribute('src', …)`）；14 个离线检查补恒等桩。零内容改动、未重启服务。详见
  [`history/2026-09-23/桌面D0_DOM图像站点接入resolver.md`](../history/2026-09-23/桌面D0_DOM图像站点接入resolver.md)。
  **D0 仍剩的那半条**＝下方「`tauri build` 完整原生打包 + 干净机器启动」未验。
- [ ] 服务端 CORS：桌面来源（`tauri://localhost` / `http://tauri.localhost`）需**精确** CORS，且 WebSocket 的 Origin 校验要单独核对（CORS 不能替代它）。属部署条件，需真实域名后落实。
- [ ] **V3-D1 分平台构建与签名**：缺目标系统 runner、代码签名/公证身份与发布凭据；**缺什么就阻塞哪一步**，不伪造下载地址与签名结果。
- [ ] **V2-HMR**：CSS 原位更新与 TS 整页刷新由 Vite 默认能力提供；「明确 UI／表现模块的安全热替换」（`session-runtime` / `presentation-host`、单个 HUD 槽位替换协议）尚未实现。
- [ ] **V2-R1 / R2 / R3（Three.js）**：依赖里已存在 `three@^0.186`，但**安装依赖不等于接入**；R1（可关闭的局部接入 + 资源/生命周期验收）未开始，**未过门槛不删除 Phaser**。

### 40 · GM 与运营基础　〔总纲：部分实现〕

总纲口径：本地 GM 命令含 `/add`、`/cash`、`/exp`；**不等于正式权限与运营审计体系**。
总纲建议落点：公开部署前另定授权；需要时增 `A/permissions.rs`、`A/audit.rs`，**保留现有本地政策**。

本仓推进：`/add` **唯一道具拒绝回执**（根因是回执把 `item_unavailable` 落进兜底文案「道具发放失败。」，
没有主语、没有位置、没有解除办法；已把 `only` 冲突查询收成单一权威 `auth::only_item_holder`）、
`/exp` 加玩家经验（直调 `World::add_exp` ⇒ 与正常玩法结构上不可能分叉）、`/cash` 充值。

- [ ] 经 `启动3010.command` 统一加载后实玩：骑着野豬再 `/add 1902000 1` 应回「野豬（1902000）是唯一道具（源 only=1）：你已装备在骑宠槽（-18）。先卸下或丢弃现有那一份再发放。」；卸下坐骑后再 `/add` 应恢复发放成功。
- [ ] 经 `启动3010.command` 统一加载后实玩：`/exp 10000` 应出 `GM：已获得 10000 经验，…` 且 HUD 经验条/等级同步；`/exp 0`、`/exp abc` 只回用法提示且状态不变；重登后等级/经验/升级所得 AP、SP 均保留。
- [ ] （如实登记，未修）`/exp` 与 `/cash` 一样**无 requestId 幂等**：GM 命令在聊天幂等窗口之前被截获，同一 requestId 重发会再次发放。当前全角色可用、单机复刻用途；**接 GM 账号表时应在 `handle_gm_command` 入口一并加身份闸门与幂等**。（另：死亡状态也可加经验。）

---

## 3. 待你确认或操作

- [ ] **确认 3010 启动提速**。`构建打包` 那一步的根因（843MB / 7 万个内容文件被当成构建产物、每次启动全量复制一遍）已修：生产构建不再复制内容数据，实测 vite 段 50.61s → 3.1s、候选 882MB → 3.3MB。请按惯例在你自己的终端跑一次 `zsh 启动3010.command` 确认 `[2/4]` 落到秒级——本机沙箱读不到进程表，`activate` 那一步我无法代跑。同一改动带来一条行为变化：**改内容不再需要重建**，直接改 `client/public-tms273/assets/**` 刷新页面即生效（旧的 `rsync` 覆盖 `build/current/client/assets` 热修做法已失效）。
- [ ] **世界地图两条口径待定**（同时阻塞 §02 的「已探索点 + 悬停任务提示」）：见 §02 末条。
- [ ] **§12 增量 5 与 §16–18 的立项决策**：无「玩家交易／拍卖／邮件」消费者就不建托管与锁定抽象。
- [ ] **第 24 条图鉴奖励块源树漂移的处置方向**：6 条奖励声明 `json-present` 但源 JSON 不在位；`shared/items.json` 另有 62 条同类陈旧 ` 2` 路径（47 条有规范副本、6 条只在 `手工服务端` 提取树、**9 条两棵树都没有**）。**需决策**：剔除那 9 条，还是从 `手工服务端/tms273/WZ_JSON_TW` 回灌。修它要改物品分母。
- [ ] **iCloud 隔离区去留**（见 §5.3）：已复核结论＝**不能整目录删除**，等你定。
- [ ] **【2026-09-24 新增】「一律落射手村」与 4 条既有验收的冲突怎么收**（见 §5.1）：
  `commands.rs::resolve_join_map_position` 在目录含射手村时把**任何 join 无条件解析到 `100000000`**、丢弃持久化落点，
  这是「登录默认进入三维射手村」那轮**有意为之**的产品行为（该轮交付记录第 11 行：「其他地图存档角色使用射手村出生点」），
  但它**打破了 4 条旧验收**（`boss_practice_*` ×3 + `tms273_chapter_first_six_acceptance`，HEAD 即红、非本轮引入）。
  三条路：**(a)** 认定旧契约作废、把 4 条验收改写到新契约；**(b)** 给产品行为留 carve-out
  （如「新鲜角色落射手村、已有存档保留落点」）⇒ 改 `commands.rs`；**(c)** 暂不动，只登记为已知红灯。
  **我没有擅自改这 4 条判据**——「判据文件被静默回退比判据写错更危险」，改判据要你点头。
  ⚠️ 副作用说明：在 (a)/(b) 落地前，`cargo test` 的**基线就是 4 红**，别再按「0 失败」判定回归。

---

## 4. 原创扩展（按总纲口径**不计入**原版 40 模块缺口）

> 总纲原文：「**管理员定制与原创世界机制不作为原版缺失项**」。本节单列，便于与 40 模块区分。

### 4.1 风铃岛 2D 素材版

最新反馈已按分层 2D 落地，协议 14、内容 `tms273-12`；前后端构建、素材状态检查和候选 27 张必要图像
逐字节核对通过。此前 Three 候选不再是当前待验版本。

- [ ] 用户从「菜单栏→活動清單」进入两图；3010 启动修复任务已通过统一入口加载新版。
- [ ] 核看左侧巨树、上方悬桥、右上驿站、浮岛岩壁与根道的整体还原，以及 TMS273 明火、干枝、云、桥、现成展翼效果。根道仍保留现有斜坡几何，**不声称原图石阶已 1:1 复刻**。
- [ ] 实玩攀绳放桥、点燃热流与叶翼落地、公共修桥/运输及返回；保留原有公共桥进度、账号和数据库。
- [ ] 用户核看宽屏、横屏、窄屏及尺寸变化下的遮挡、角色脚面和流畅性。本轮无独立 QA 或浏览器实玩结论。

### 4.2 原创扩展 · 死亡世界一期（墓碑留存与虚影演化）

已实现，协议 **28 → 29**；`server/src/death_world.rs` + `auth/death_world.rs` + `death_tombstones` 表迁移 +
`F/notice/tombstone.ts`。原版死亡/复活语义**零改动**。墓碑留存（30 分钟到期、重启不刷新、同图容量 8、
练习图与风铃实例图不落碑）、虚影演化（阶段是 `(now, mourners)` 的**纯函数**，无第二时钟）、
悼念（重复悼念重放同一状态）、虚影形态＝死者灰色形态（只靠 tint，不重画）、
D06 试点（虚影打击与玩家击杀共用 `monster_rewards` 同一把主键，不建虚影账号、不稀释分成）。
计划详见 `topics/MapleStory_Death_Implementation_Plan_v0.1.md`。

- [ ] 既有红灯（与本次无关，基线复跑确认）：`check_repository_layout.cjs` 红在 `evidence/INDEX.md` 失效链接 `2026-09-14/keybindings/`；`update-service.check.ts` 基线即红（asset-index 模块缺失）。
- [ ] 未实玩：碑体观感、虚影三阶段的可读差异、悼念回执文案需实玩确认。
- [ ] 未做（后续立项）：虚影帮助/危及后来者（P2）、经验球、独立 NPC 与天命参与（P3+）。

### 4.3 巨石之约（Three.js 第三活动：港口、移动巨像、箱庭）

> **用户最新要求优先于下文旧姿态与镜头方案。当前回合只记录计划，不修改实现、不启动构建或切换服务。**
> 旧候选的检查通过**不能覆盖**本次新报问题。

用户实测问题：往回走的环形十字路口及另一个十字路口均出现「瞬間移動被地图碰撞阻挡。」，
伴随「技能已施放。」与路牌提示。两处作为**同一类问题**追查，不逐路口打补丁；
**目前尚未复现并确认最终根因，不能记为已修**。

| 顺序与负责人 | 下一步实施内容 | 完成判据 |
| --- | --- | --- |
| 1 · Astra：瞬移与路口共同根因 | 先在回程环路及「蓝旗坡道」附近复现，记录实际 track/s、高度、落地状态、方向、相机朝向、路口及技能 requestId。沿输入映射 → `skills.rs::plan_teleport` → `colossus_motion.rs::Body::teleport` → `Config::passage` / Travel → 快照和反馈追查所有调用方。 | 两个报告位置往返均能正确移动和瞬移；合法连接处不误报碰撞，真正缺口／墙体仍受约束；↑ 路口与技能目标使用一致的权威空间事实。拒绝请求不误发成功提示、不错误扣费或结算。**不得靠屏蔽报错、扩大路口半径、取消碰撞来掩盖根因。** |
| 2 · Astra：仰躺与膝部港口 | 将巨像初始姿势改为背部朝下、面朝上的平躺，明确替代侧卧；膝盖承着港口，按港口位置安排腿部姿态；在 Blender 检查身体、膝盖、海面、港口和镜头的实际空间。保留现有模型与手指骨骼，**重新计算承载变换**，不直接沿用侧卧旋转或旧手部露出高度常量。 | 从港口及侧面能读出仰躺；港口确实位于膝部，人物、道路、碰撞和模型同位；无前臂护腕挡天、地面穿插或港口突然翻转。 |
| 3 · Astra：人类姿势缓慢坐起 | 动作分为仰躺停留 → 双手落至海底并建立支撑 → 肘肩发力、胸背逐渐抬起 → 骨盆与腿部配合、进入坐姿并缓冲停稳。双手在支撑阶段保持可信接触，**不能整个人形绕一点旋转、漂浮起身或手掌滑过海底**。 | 近景能看懂「双手撑住后，身体才抬起」；膝部港口和乘员连续承载。此次表现重点是缓慢坐起，**不沿用旧流程立即快切站立**；后续是否继续站立不在本轮自动扩写。 |
| 4 · Astra：慢镜头与身上流水 | 镜头先留在港口建立参照，再缓慢跟随撑手、胸肩和头部抬升，坐起后给停留时间；**不为看全身而急速拉远**，全貌可不完整入镜。水从出水身体表面、石缝及手臂流下，沿世界重力下落。 | 无突然远拉、甩镜头或港口丢失；能看清坐起过程与水从身上退下。流水起点跟随骨骼，落水方向不随身体倾斜成横流，手和身体的入水／出水有连续反馈。 |
| 5 · Astra：必要定向验证与交付 | 路口修复留覆盖两处共因及真实阻挡的一项最小回归；动作阶段检查支撑点、膝部承载、跳离／落地与重连时序，再查看实际游戏镜头、慢坐起和流水。只有实际改动后才运行所需检查。 | 提供可试玩候选和真实画面，明确未测设备；保留服务、用户数据库、机器人与原版 UI，**不另起独立 QA**。 |

当前只读代码线索：`Body::teleport` 把目标限制在当前轨道 `[0, len]`，**不遍历相连路口**，并把「位移为零」等情况
一起返回 `None`；`plan_teleport` 将其统一变成碰撞阻挡。`Config::passage` 只选择**当前轨道**、已落地且距离小于 4 m
的入口。**尚不足以断定两处报告都是同一端点问题。**

已形成的历史基线（保留，供对照）：

- 首个可玩候选 `20260919195219-184bd9ea`（协议 31，巨像内容 `colossus-runtime-0.1`）。
- 六箱庭 + 原版 UI 候选 `20260920042948-b80eb74e`（协议 32，`colossus-runtime-0.2`）：六区／13 轨道／36 路口、
  手绘材质与人物动作、原版 HUD／地图／活动／NPC 对话已贯通。地图总长约 4.6 km 是**地形规模**，不是十小时内容完成证据。
- 骨骼重设计候选 `20260920125451-927ff1d0`（协议 33）：48 骨骼石质人形、双手十指 30 节指骨、GLB 与共享骨架
  休止姿态接入 Rust／Three.js。**当前约 40 m 攀爬与路口连接是可玩工程闭环，并非数十公里连续攀登内容。**
  软件渲染省电样本仍约 219 ms／帧，**性能未达标**。
- 最新候选 `20260920171704-12586335`（协议 34，巨像内容 0.5）：立体港口重制——可走坡街、后侧屋顶回廊、
  海侧悬台均已实走回环；模型与落脚不一致、路口对话抢占、石壁遮挡和初始前臂挡天已修。隔离预览 3017。

最高优先级试玩反馈：实际场景与手绘世界地图差距过大，需重做港口构图、错层石岸、红瓦建筑、蓝旗灯塔、
植被生态，去除重复草皮方格；巨像揭晓须呈现可辨认的完整形体。**保留已完成的权威移动、能力、存档。**
完成判据为工作树内可编辑模型、实际场景替换、必要构建与真实画面证据，**不用概念图代替交付**。

### 4.4 可持续演化完整世界 v1（余途 · 回风原）

依据 [`自主开发委托 v2.0`](../../design/world-awareness/GPT6_Astra_可持续演化完整世界_自主开发委托_v2.0.md)，
覆盖 v1.0 的首章时长目标。工程基线已交付，候选 `20260919173723-bc7b3f81`（协议 30，风铃补充 `windbell-runtime-0.2`）；
16 项 Rust 风铃定向、1 项真实内容离线加载、客户端状态/素材与 NPC 渲染、协议文案检查与候选构建已通过。
六个闭环与来源边界见[设计契约](../../design/windbell/余途_回风原_世界基础与二次生长.md)，
工程结果见[交付记录](../history/2026-09-20/余途_回风原_持续世界与二次生长.md)。

- [ ] **统一加载**：用户按既有 `启动3010.command` 入口（候选协议 30、原版内容 `tms273-33`、风铃补充 `windbell-runtime-0.2`）；**本轮没有替用户切换在线版本**。
- [ ] **实玩与表达核定**：两图操作、真实脚点、阿苇步态、槐生透明边缘、对白与声音能支持理解；确认是否值得记住与回来，**不以工程结果代替感受**。

---

## 5. 跨模块台账（不属单一模块）

### 5.1 门禁与验收基线

本机 `run-checks.mjs` **63 项 / 55 过 / 8 红**（**2026-09-24 第八轮实测**；⚠️ 本表曾记「65 项 / 57 过 / 8 红」、
更早记「63 项 / 56 过 / 7 红」，本表初稿记的「91 项 / 6 红」是更早的运行器版本
——**总数与过数都记错过，红集合一直一致** ⇒ 判据是**失败集合逐项相同**，不是总数），
8 项红**均为既有基线**，非回归：

`layer-animation.check.ts`（卡 `assets/manifest.ts` 的无扩展名导入，仅 `scenes/world.ts` 一个文件就有 21 处）、
`gauge.check.ts`（同类：`features/hud/view.ts` 的无扩展名导入指向 `features/inventory/drag-controller`；
由提交 `2a17688`「椅子恢复间隔 + 快捷栏道具拖拽绑定」引入，**与 S5 召唤通用化无关**）、
**`refactor_audit.cjs --deps --check`**（2026-09-24 新增登记：违规项
`scene-layer-not-imported-by-features: client/src/features/henesys/preview.ts -> client/src/scenes/world.ts`，
已用 `git show HEAD:…preview.ts` 证明 **HEAD 就存在**、由射手村三维那轮引入，**与 burn 窗收口无关**）、
`check_repository_layout.cjs`、`check_inventory_surface.cjs`、`check_tms273_player_status.cjs`、
`check_tms273_desktop_package.cjs`、`build-release.check.cjs`（本机沙箱无 `ps` 权限）。

**同时更正 `cargo test` 基线**：原记「670 过 / 0 失败」，**实测 HEAD `a14883f` 为 667 过 / 4 失败**
（独立 worktree 检出 HEAD 复跑，失败项与 2026-09-24 工作区**逐字相同**）：
`boss_practice_*` ×3 + `tms273_chapter_first_six_acceptance`。根因＝
`commands.rs::resolve_join_map_position` 只要目录里有射手村就**把任何 join 无条件解析到 `100000000`**、
丢弃持久化落点（`DEFAULT_LOGIN_MAP_ID` 由 `9b489e4`「登录默认进入三维射手村」引入）——是**有意为之的产品行为**，
而那 4 条验收断言的是**改动前的契约**。处置见 §3。

**同日第三轮（召唤存活时长收口）复跑，失败集合逐项相同**（该轮零 TS 改动、只动 Rust 与门禁脚本）。
**2026-09-24（burn 窗收口）复跑：`run-checks` 8 红逐项相同**（`check_tms273_damage_pipeline.cjs` 与
`skills/view.check.mjs` 均 PASS），`cargo test --bins` **668 过 / 4 失败**（668 = 基线 667 + 本轮新增 1 条）。

**2026-09-24（開關技能收口，第七轮）复跑：失败集合逐项相同**；`cargo test --bins` **669 过 / 4 失败**。

**2026-09-24（第八轮 · 技能轉換 `2321054`）复跑：`run-checks` 8 红逐项相同**
（`check_tms273_damage_pipeline.cjs` 新增 §3i、`check_tms273_attributes.cjs` 删掉 `madX` 的
`except: ['2321054']`、`check_protocol_errors.cjs` **无需改动即绿**、`features/skills/view.check.mjs`
与 `combat/{skill,sound}.check.mjs` 全 PASS），`cargo test --bins` **674 过 / 4 失败**
（674 = 669 + 本轮新增 5 条；**失败集合＝基线逐项相同**）。

⚠️ **本轮踩到一个会制造「假红」的坑（已写进 `SKILL.md` §7）**：扰动还原用 `cp -p`（**保留 mtime**）
＋ `build/.cargo-cache` 是 **mtime 增量** ⇒ cargo 判定「源码没变、不用重编」⇒ 全量跑的是
**还带着扰动**的陈旧二进制，报出与扰动 E2 一模一样的 `xform_admission_flips_with_the_form` 失败
（报文「转换之后 天使之觸 仍被拒」）。**插桩实测却是 `form=Some(Avenge) has=true granted=true`**
——三个析取项全绿却仍报 `skill_hidden`，且**单独跑也红**（不是并行干扰），`touch` 后强制重编即 12/12 绿。
⇒ **判据是产物 mtime > 源 mtime，不是 `cmp` 说文件内容一致**；**改完源文件或还原扰动后必须 `touch` 再跑验收**。
这与「装配器不重编会 <1s 假成功」是同一类陷阱，但**发作方向相反**：那次假绿，这次**假红**，
而且报文看起来完全像真缺陷。

⚠️ 验收口径（沿用既定做法）：告警/fmt 基线要**独立 worktree 检出 HEAD** 取（不是 `git stash`），
比对**去行号后**的清单；`npm run check` 会**写** `artifacts/refactor/frontend-deps.json`，基线对照时别当人手改动。

- [ ] 未修的既有失败项（需单独确认）：`layer-animation.check.ts` 的 `features/windbell/maps` 无扩展名导入无法解析（HEAD 既有；是一次跨多文件的风格统一，另行处置）。
- [ ] 既有红灯（与本次无关，基线复跑确认）：`check_repository_layout.cjs` 红在失效索引链接。
- [ ] （未修，与本次无关）`check_tms273_notebook.cjs` 仍卡在奖励块的源树漂移（6 条奖励声明 `json-present` 但源 JSON 不在位；`shared/items.json` 另有 62 条同类陈旧 ` 2` 路径）。处置方向见 §3。
- [ ] **下一个 P0（未领取）**：T04 剩余部分（高低平台侧壁／绳顶／游泳起跳／移动中施法／死亡复活／暂离回归）需真机实玩记录，**不能静态推断**；T05 剩余**已收口**（`infoex` 语义已核定，36317/36350 核定为「原版机制不在源里 ⇒ 维持阻塞」）；T07 的**属性聚合口径已收口**（`bossdamR`/`finaldamR` 等源里不存在对应消费点的字段**未类型化**，出现消费点时按同一模式登记），与 B05/B06 的其余部分一并待取用；之后按审计 §11 顺序取用。
- [ ] **基线回归修复：7 项长期失败清零**（**待统一加载实玩**）：见[交付记录](../history/2026-09-14/基线回归修复七项.md)。

### 5.2 仓库重构（[专题方案](MapleStory_Repository_Based_Refactoring_Plan.md)）

> R0→R11 阶段表推进；已完成条目已移至 `history/2026-09-12/`，本节只保留未完成与待确认事项。

- [ ] `frameAt` 从 `assets/manifest.ts` 搬出到独立纯函数模块：搬移会破坏多个经 data URL 装载 `animation.ts` 的既有 check，本轮不做，后续再评估。
- [ ] **R10** 门禁挂 CI + 例外登记 + 架构导航更新：当前仓库无远程 CI，接入时执行 `node scripts/refactor_audit.cjs --deps --check`。
- [ ] **R11** 按新基准的业务热点继续治理；每次重新运行 `refactor_audit.cjs --sizes` 后再决定下一批，**不自动扩张**。
- [ ] **待确认语义**：`quest_prerequisites_match` 的 OR + 空前置清单返回 false 是否为缺陷。已有特征测试按原行为钉住，确认后单独修复。

### 5.3 iCloud 冲突副本清理与门禁封堵

第三次同类问题（前两轮都只删不封堵，且 `core.hooksPath` 从未启用）。本轮清点 398 项：删 355 项零信息损失残留、
隔离 34 项存疑残留到 `.trash-icloud-dup/`（可整目录回滚）、`git rm` 6 个已入库旧版源码副本；
新增 `scripts/check_icloud_conflict_copies.cjs` 接入 `.githooks/pre-commit`、撤销三处跳过例外、
在装配脚本复制前加反向断言。详见[交付记录](../history/2026-09-18/iCloud冲突副本全量清理与根因封堵.md)。
本轮不需要进游戏实玩（无玩家可见行为变化）。

- [ ] **已复核（2026-09-22 第八轮，只读）⇒ 结论：不能整目录删除，等你定。** 隔离区实为 **8673 个文件**（34 是顶层条目，多为整棵「冲突目录」）。① **4 个 Blender 工程与本体「内容不同」**（不是重复），其中 `windbell_world_asset_library_textured 2.blend` **18.1 MB > 本体 12.8 MB**，存在「旧版打包了贴图、新版改为外链」的可能 ⇒ 删掉就丢一版；② **1 个孤儿 PNG** 无本体；③ **29 个「冲突目录」** 位于两棵 `WZ_JSON_TW` 的 `Item/Consume/02xx 2` 等路径下、**路径集与本体不同**——其中 `Item/Consume/0243 2` 正是 `check_tms273_notebook.cjs` 长期红灯引用的那一份 ⇒ **隔离区与那条漂移是缠在一起的**。需要你的判断：① 两版 Blender 哪个是要的；② `0243 2` 这类冲突目录以哪边为准；③ 其余是否可整目录清掉。**此前不做任何移动或删除。**

### 5.4 外围事项

- [ ] **Codex 射手村皮肤**：皮肤包已完成，Dream Skin App 已安装、射手村已导入；用户已授权启用，但 Computer Use 安全策略禁止控制 `com.openai.codex`；待用户手动退出宿主后通过 Dream Skin 菜单安装引擎并启用（**未绕过，未开启 CDP 或重启宿主，实机效果未验**）。成品 `/Users/muniao/Downloads/MapleStory-Henesys-Codex-Skin/MapleStory-Henesys-Codex-Skin.zip`；见[交付记录](../history/2026-09-13/Codex射手村皮肤.md)。主代理制作，GPT-5.6 Luna / max 核查契约；**未改业务代码**。
- [ ] 创建装备目录缺失：修复已由统一入口加载，保留账号和数据库；请使用原角色登录入图确认不再出现 `equipment slot mismatch for 1051353 (persistence)`。根因、Luna / max 定向回归和规范/skill 见[交付记录](../history/2026-09-13/创建装备目录缺失导致无法入图修复.md)。
